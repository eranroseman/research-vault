# Ingest redesign, Part B (compile) — results

- **Date:** 2026-09-16
- **Method:** `docs/superpowers/plans/2026-09-07-ingest-redesign-b-compile.md` (Tasks 1–6, with Task 2b added on 2026-09-16) run under `superpowers:subagent-driven-development` across two controller sessions (2026-09-14 and 2026-09-16), with a fresh implementer and a two-stage review per task, a whole-branch review over the final diff with one fix wave and a scoped re-review, the four tracers on a scratch vault, and two attended sittings on the test instance. The plan's printed code was folded to the committed tree after each task closed by the plan owner, so the plan on `main` is what runs; the register (`docs/superpowers/plans/2026-09-07-ingest-redesign-b-deferred.md`) carries every departure from the printed text with its reason.
- **Spec:** 2026-09-06-import-redesign-design.md
- **Binds:** nothing — this records what the execution produced and measured; the spec is the current state, the plan the authority for what was built.

## What landed

Branch `worktree-ingest-b-compile`, merged into `main` as `d8ae4a3` (`--no-ff`, pushed 2026-09-16): 50 commits since `3f66b25`, 40 files, +3,758 / −293. Task 4 `6b48d0a..d6ed7fa`; Task 5's offline half `68a736e..b78e8b1`; Task 1 `ebd5a35` (the pin move to `32ac5a0`) and `d39c006` (the tracer results); Task 2b `bb05d48..710a820`; Task 2 `161cf64..41fdc4e`; Task 3 `59a5dd6..75e69d9`; Task 5's attended half `984e20a` and `5329cfa..b9ad2b4`; the post-review fix wave `27ccd18..8782215`; the register closed at `5015c60`.

Adopted: claude-obsidian at `32ac5a0` (v2.2.0) as the compile tool, installed from its marketplace, registered as a provision companion, probed by doctor's `compile-tool`. Built: the compile wrapper (`research_vault/compile.py`, the `compile` verb) — selection from the captured set, `fulltext/<attachment key>.md` as each source's locator, the source-ledger record filled from the provenance tuple, a transaction bundle claimed exclusively under `.research-vault/compile/`, the tool's own `transaction inspect` / `apply` gate driven with its approval hash, a stale record retired with `supersedes` when a text refreshes, per-key `not-captured` / `no-fulltext` rows for a selection that registers nothing; `evidence-layer`'s write legs under the writer attestation (Task 2b, from tracer T4), so a capture, a compile refresh or a propagate commits through the vault hook; the compile sections of `capture-source` and `setup-vault`, `synthesis-conventions` rewritten for the compiled layer under `wiki/`; `ZoteroClient.trash_item` / `delete_item`, `authorize` waiting `AUTHORIZE_TIMEOUT` for the person's click, the `live_write` marker, the replayable trashed fixture; docs, registries and the CRAP gate without `continue-on-error`.

## Verification, measured

| Command                                     | Result at `5015c60`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| offline suite (`-n auto`)                   | 2055 passed, 7 skipped                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ruff format --check`, `ruff check`, `mypy` | clean (`97 files already formatted`; `All checks passed!`; `Success: no issues found in 36 source files`)                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `crap4py --max-crap 30`, `drywall`          | exit 0 (highest CRAP 27.0; `cmd_compile` CC 8, `compile.py` at 100% on every function); drywall: no findings                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| read-only live legs (`RV_LIVE=1`)           | 22 passed, 2 skipped (the write-capable legs skip without `RV_LIVE_WRITE_BASE`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| mutation gate (`--base main`, six modules)  | `pass — no new survivors` — `__main__.py` ok (killed 1422, survived 82, no tests 2), `compile.py` ok (468 / 2 / 0), `inbox.py` ok (850 / 166 / 0), `lints.py` ok (952 / 95 / 0), `scaffold.py` ok (1133 / 175 / 0), `zotero.py` ok (462 / 222 / 0); the two `compile.py` survivors are the R21 rows (`check=False` → `None`, a ruff-mandated keyword the callee treats the same); `compile.py`'s other equivalent mutants were removed by rewriting their surface, and a survivor in a new module is never baselined outside the #42 classes and the R21 class |
| `git status --porcelain`                    | empty in the worktree before and after Step 1, and in the main checkout before the merge                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

**The tracers** (spec §4.3; scratch vault `rv-tracer-2026-09-14`, tool `32ac5a0`): T2–T4 pass on 2026-09-14 (`mode set`/`mode get` round-trip; the ingest bundle over three captured sources applied `complete` with `fulltext/<key>.md` locators, `literatures/` and `fulltext/` byte-identical, only `wiki/` changed, `wiki/log.md` without a `## ` heading; `verify --offline --surface commit` `{"MATCHED": 20, "SKIPPED": 23}` with the tool's pages alone on the surface). T1 and T5 pass on 2026-09-16 (a session with both plugins loaded ran `capture` and `doctor` at 33.5k/1m context, no compaction; Obsidian 1.13.7 opens `[[<Title>]]` at `wiki/sources/<Title>.md` and `[[<citation key>]]` at `literatures/<key>.md`, embeds rendering inline — measured through the Obsidian CLI). Run as printed, T4 exits 1 on capture's own refresh — the finding Task 2b closed.

**The attended sittings** (test instance `Tdoqsn2J4q4h`, Zotero 10.0.2 / Better BibTeX 9.0.64): the write leg passed twice on 2026-09-16 with one Always-Allow click each (`capture.add` → trash → delete; fixture `{"live": {"4E5FHBVD": 1721}, "trashed_items": {}, "trashed_trash": {"4E5FHBVD": 1722}}`); the first passing run's fixture was deleted by `tmp_path_retention_policy = "failed"` (the plan now says `-o tmp_path_retention_policy=all`); the dialog re-opens per fresh vault. The compile wrapper's live checks on the tracer vault: `rosemana` registered (`valid: true` → MATCHED → `captured-set` MATCHED), and after a simulated text refresh the retired record (`valid: true` → MATCHED → no `recompile-needed`). Records 452–453: `GET /api/users/0/items/trash?format=versions` answers the key→version map; `GET …/items/top?format=csljson&limit=1` answers 200 with `Content-Type: text/plain`.

## Review

Whole-branch review (`opus`, over `41d619d..ad12867`, 36 commits): **with fixes** — one Critical (after a text refresh the stale same-locator record failed the tool's `content_sha256` validation and wedged every later `compile`; a plan gap the printed merge and ruling R19 both carried), three Important (the `adopt --apply` sentence without `--operation-id`/`--generated-at`; empty and unknown selections exiting 0; `cmd_compile` without its siblings' error classes), eleven Minor, and a row-by-row triage of the register. One fix wave (`27ccd18..8782215`, eight commits) landed the Critical, the three Important and eight Minors, with its own self-review catching one regression (`_print_and_hold`'s check) inside the wave; the scoped re-review returned clean.

Per-task: every task passed spec compliance on first review; quality took zero or one fix round (Task 2 one, Task 3 one, Task 5's offline half one). Twelve register rows: five fixed or accepted with the commit named, seven open in issue #141 with the review's extras. Thirty-two controller rulings stand in the SDD ledger's record below.

## What moved in the spec because of this run

§4.1 pins `32ac5a0`; §4.3 records the tracers (2026-09-14 and 2026-09-16); §6 amended 2026-09-16 (the evidence-layer write legs take the writer attestation); §9's csljson row gains the 2026-09-16 reading (200, `text/plain`; decision 13 stands); `wiki/synthesis/` → `wiki/concepts/` at four sites (Part A residue). The plan gained Task 2b, the authorize wait, the R19/R30 merge, the new-module baseline classes (#42 plus the ruff-mandated kwarg), and the gate's dirty-tree note (#133).

## What remains

- Issue #141: the deferred findings the review ruled real but not merge-blocking.
- Issue #113: the upstream Zotero filing — not posted by the agent; the draft (version 10.0.2) is a comment on #113 for the operator to post, then link and close.
- Issue #133: the gate's false pass on a dirty tree.
- The scratch vault `~/scratch-vaults/rv-tracer-2026-09-14` (Obsidian registered it; it carries the simulated refresh) and `~/scratch-vaults/t1-wizard.sh` — throwaway.
