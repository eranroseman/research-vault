# Knowledge-Harness Foundation — Specification

Status: **APPROVED** by the author, 2026-08-16 (wayfinder ticket #15) — after two adversarial verification passes (44 findings resolved) and a seven-domain rethink audit (85 findings; three trust-boundary defects fixed).
Consolidates the resolutions of tickets #7–#14 of the [foundation map](https://github.com/eranroseman/knowledge-harness/issues/1). Written for a reader with no shared history; every design decision links its ticket, where the full rationale and research inputs live.

## 1. Purpose and success criterion

A Claude Code plugin for trust-first academic research on a personal knowledge vault. First workload: the academic-research arc (question → literature → synthesis → draft → submit), validated by a thin slice; analysis/reports, PKM daily loop, and long-form writing are later workload maps.

**Success criterion: trustworthy output.** Every claim traceable to a real source; zero fabricated citations; anything the harness helps produce is defensible under review. Knowledge work has no compiler, so the foundation manufactures one: every gate bottoms out in a mechanical check (a DOI resolves, a quote byte-diffs after normalization, a citekey exists in the exported bibliography), with LLM judgment confined to non-blocking, human-adjudicated findings.

## 2. Substrate and environment

- **Vault**: a fresh, private git repository, separate from this plugin repo, scaffolded by `setup-vault` from templates shipped in this repo ([#7](https://github.com/eranroseman/knowledge-harness/issues/7)). Plain markdown + YAML frontmatter; Obsidian-convention but not Obsidian-dependent — all machine operations work on files + git. **The vault is a structurally conformant OKF v0.2 bundle** (ADR 0001; author ruling 2026-08-20): root `index.md` carries `type: "index"` and `okf_version: "0.2"`; root `log.md` is the single-writer regeneration target (`okf.regenerate_log`) and carries `type: "log"`; every nested `index.md` remains reserved with no concept frontmatter; every other machine-owned concept Markdown file carries parseable frontmatter with a non-empty `type`. Our schema extensions use OKF's unknown-key liberality; fleeting human notes are the tolerated residual (frontmatter arrives at triage).
- **Reference source of truth**: local Zotero (v9 at time of writing) + Better BibTeX (BBT). Verified environment facts, with dates, live in [docs/environment.md](../../environment.md): local API reachable from WSL2 (mirrored networking), BBT JSON-RPC responding, Windows→WSL path translation via `wslpath` (live-verified).
- **Version-gated features**: Zotero 10 local writes (citekey pinning, marker write-back without plugin) are feature-detected, never assumed. On Zotero 9: citekey pinning is manual; no programmatic pin path exists (live-falsified — the native `citationKey` field populates even for unpinned items and cannot serve as a pin lint).
- **Preservation boundary**: the git vault preserves everything defensibility needs (managed regions, bibliography, events, tags); the evidence *artifacts* (PDFs, snapshots) live in Zotero storage, outside that boundary — recoverability depends on the user's Zotero sync/backup, and doctor persistently warns when the vault has no remote and when Zotero storage has no stated backup. (Solo scope: this is a stated boundary, not a compliance control.)

## 3. Vault structure ([#7](https://github.com/eranroseman/knowledge-harness/issues/7))

```
vault/
├── index.md       # reserved bundle root; type: "index" + okf_version: "0.2" frontmatter
├── log.md         # single-writer regeneration target (okf.regenerate_log); type: "log" frontmatter
├── inbox/         # fleeting inbox; citable sources NEVER enter here
├── literatures/   # evidence layer: citekey-keyed projections of Zotero
├── synthesis/     # LLM-owned synthesis pages and nested reserved index.md
├── log/           # harness-written append-only daily logs
├── projects/      # manuscripts/deliverables, one subfolder per piece
├── system/        # support artifacts: templates, bases, canvases; system/bibliography.json (BBT export)
└── AGENTS.md      # typed vault facts + routing, shipped by setup-vault
```

File conventions: daily log = `log/YYYY-MM-DD.md`, with `type: "daily"` and entries `- HH:MM <actor> — <action> [links]`; the shared **review inbox** = `inbox/review-queue.md`, with exactly `type: "review-queue"` frontmatter before its append-only body. Neither append-only ledger carries `generated`, because rewriting a header on append would violate the stronger byte-preservation contract. Every hold/warn/alert flow in §6–§7 writes here, and the inbox-drain convention reads it. Entry serialization: one markdown list line of inline fields — `- [id:: <check>/<target>/<date>] [check:: …] [target:: …] [result:: …] [actor:: …] [reason:: …]` — where `target` is a claim link `citekey#^id`, a file path, or a gate-run reference, and `id` is the composite `check/target/date`. **Acknowledgment is itself an append-only entry**: `- [ack:: <id>] [actor:: human:…] [reason:: …]`. A closure is bypassed iff a matching human acknowledgment exists; **ack scope is standing for that check+target pair until the target's content changes** (its `fixity-sha256` or file hash), then it must be re-acknowledged. **Inbox drain**: `project`'s resume orientation step surfaces unacknowledged entries **with count and age** (oldest-first; an age threshold escalates the display — the Whittaker inbox-rot guard: a warn queue nobody drains is a silent failure of the doctrine's liveness bound). **Acknowledgment affordance**: the acting skill composes the ack entry's syntax; the human act is consent plus reason text, never hand-written field grammar (forced formality gets circumvented — Shipman/Marshall).

- **Two-layer trust boundary**: `literatures/` is the evidence layer — machine-projected from Zotero, protected from LLM free-writing by **both** path rules (CI/hooks key on the folder) and frontmatter type (Bases-queryable). Defense in depth is deliberate.
- **Zotero is the only admission path for evidence.** The inbox is for fleeting notes; nothing becomes citable except by being admitted into Zotero (a human act) and imported. **Gate-design rule (ADR 0004, proposed): judgment calls group — one decision, one gate, however many items it covers; a gate exists at all only where the act is irreversible and the outcome undecided (the human could really say no).** Bulk admission of a human-curated set: one recorded approval bound to an inspection artifact (rendered worklist with per-item classifications, hash recorded); above a named batch-size bound the surface elicits per-item dispositions. Per-item admission remains the norm where per-item judgment is real.
- **Literature notes**: filename = citekey; a managed region (bridge-regenerated, full re-render, never hand-edited — policed by a warn hook) above a free region where human/agent prose survives regeneration.
- **Synthesis index**: `synthesis/index.md`, created by `setup-vault` — a reserved, frontmatter-free canonical index (one line per page: wikilink + one-line gist, sectioned) that registry-first dedup queries and orientation reads.
- **Synthesis pages**: one `type: "synthesis"` note kind with block-anchored claims (`^claim-id`), title-as-filename, `status: draft | stable | deprecated`, and generation metadata. The LLM creates pages without human approval, still subject to `synthesis-conventions` thresholds (§7), and logs creation in the daily note.
- **Rejected**: PARA for knowledge layers, Johnny.Decimal, folgezettel, per-claim atomic files, activity-state folders. (Rationale + survey: [research/pkm-vault-schemas.md](../../../research/pkm-vault-schemas.md).)

## 4. Zotero bridge ([#8](https://github.com/eranroseman/knowledge-harness/issues/8))

- **In-repo bibliography**: a single BBT auto-export of **Better CSL JSON** (sorted) into the vault repo — the canonical, CI-lintable citekey universe. **Export scope: the entire user library** (ID in docs/environment.md) — no curation step between Zotero admission and the citekey universe. **Provisioning is a one-time human creation of the whole-library auto-export in BBT Preferences, guided and verified by setup-vault/doctor** (live-falsified 2026-08-20: BBT 9.0.55's public JSON-RPC hardcodes collection scope — `autoexport.add("//", …)` 404s "path is too short" before registration storage — so programmatic whole-library registration is impossible; the harness is observation/commit-only for this contract and never calls the collection-only `autoexport.add`). The staleness lint's on-demand comparison export uses the same whole-library scope (a targeted collection is a later performance option, not a trust boundary). No machine paths in the repo, ever; PDF paths resolve at use time via BBT JSON-RPC + a `wslpath` shim with per-machine gitignored config. **Two-tier citability (ADR 0006, proposed):** the whole-library export is the single source universe (tier 1 — resolution and metadata for imports); **a claim's citekey is citable only if its literature note exists AND the key resolves in the current export** (the intersection — contract per batch item 17, enforcement pending); no second membership file. The BibLaTeX file, when LaTeX drafting starts, is a harness-regenerated projection over tier 2 (`log.md` artifact class; BBT stays sole writer of the one universe file).
- **Trigger and ownership**: after the human creates that auto-export, BBT is the sole writer of `system/bibliography.json` (ruled 2026-08-21; `x/` renamed in the terminology wave); the harness stages and commits genuine BBT output but never synthesizes or writes the export. Doctor waits up to 60 seconds for the auto-export to settle; a persistent absence or mismatch is reported UNMATCHED with the exact repair to perform in BBT Preferences — the harness never registers — and is re-verified on the next doctor run. The JSON-RPC on-demand export remains comparison evidence, not replacement file content. BBT's git mode stays off because its commits are not file-bound.
- **Note generation**: harness-owned (thin skill over JSON-RPC/local API) emitting ZotLit-convention notes — so ZotLit remains adoptable later as an interactive UI, never a dependency. Annotations extracted via BBT `item.attachments` (local API as fallback).
- **Citekey stability**: the git-diffed bibliography is the rename detector; keys are pinned manually on first citation; `item.regenerate_key`'s old→new mapping drives mechanical vault-wide renames — **rename propagation, driven by zotero-key identity match, covers every citekey-bearing surface**: filename, `[@citations]`, wikilinks, claim links in stance links, review-inbox targets and ack ids; Zotero 10 local writes become the programmatic pin path behind feature detection.
- **Direction**: one-way Zotero→vault. The only write-back is a presence marker (MarkDB-Connect `has-vault-note` tag). Content write-back is banned until Zotero 10 local writes AND per-event human approval. Two-way sync is rejected permanently (no successful prior art on this substrate).

## 5. Provenance schema ([#9](https://github.com/eranroseman/knowledge-harness/issues/9))

The vault adopts OKF's structural conventions while retaining the research-domain deviations that make its evidence boundary explicit: citekey/Pandoc attribution; `citekey`/`doi`/`url` rather than OKF `sources`/`resource`; typed `supports`/`disputes`; PRISMA screening states; and the richer project publication lifecycle.

| Kind | Required and optional frontmatter |
|---|---|
| Common concept | non-empty `type`; optional `description`; optional ISO-8601 `stale_after`; machine-written mutable concepts carry top-level `generated: {by: <actor>, at: <ISO-8601 datetime>}`, where `at` is the last meaningful content change |
| Literature (`type: literature`) | `citekey`; optional `doi`/`url`, `pmid`, `version`, `archive-url`, `authority`; day-one `accessed`; list `fixity-sha256`; `status: unscreened | included | excluded | superseded`; `superseded-by` when superseded |
| Synthesis (`type: synthesis`) | `status: draft | stable | deprecated`; generation metadata; no Appleton `growth`, `planted`, or `last-tended` fields |
| Project (`type: project`) | `status: draft | parked | published | corrected | withdrawn`; generation metadata; publication appends a verified event |
| Verification | `verified` is a list of `{by, at, check}` pass events; only genuine MATCHED checks append events |

`managed-sha256` is bridge/verifier-owned metadata over the exact managed-region bytes. It is compared with the current managed region, with HEAD during pre-commit, or with an explicit fetched/passed CI base for committed edits, deletes, and renames. It remains part of canonical content. Task 0 documents this witness; later Plan C tasks implement it.

Literature `fixity-sha256` values are OAIS whole-object fixity anchors and ack-scope triggers, never re-import change detectors; render-first managed-projection comparison governs no-op detection. `accessed` is preserved from day one. Optional `description` and `stale_after` pass through unchanged when present and remain absent when omitted; this adoption does not create a new staleness subsystem. `generated` participates in canonical content, while a byte-identical rerender preserves the earlier `generated.at` even when a later timestamp is supplied.

**Claims, inline, per claim** (never frontmatter-only — tags must survive per-claim relays). **Quotes are blockquotes** — the tag, citation, and anchor ride the claim line; the verbatim text sits in a blockquote under it:

```markdown
- (quote) [@smith2020, p. 12] ^c1
  > Mortality fell 12% (95% CI 8–16).
- (inference) The effect likely generalizes. [@smith2020] [confidence:: moderate] ^c2
```

Per-claim `status`: `live | deprecated` — deprecation is a **transition record** carrying `[deprecated-at:: <date>] [deprecated-by:: <actor>] [reason:: <code> …]` and, where a successor exists, `[superseded-by:: <claim link>]` (bi-temporal precedent: state changes get actor + date, matching the `verified`-event discipline). **Retraction acknowledgment** is an inline field on the citing claim — `[retraction-ack:: <code> …]` — defined here once; the update-notice gate, `publish`, and `evidence-conventions` all key on this single syntax. **Machine-written verification markers travel with the claim** (the relay principle applies to failures too): an UNMATCHED result stamps `[failed-verification:: <check>/<date>]` on the failing claim, cleared on a later MATCHED or human acknowledgment.

**Reason codes**: one small controlled vocabulary (defined in evidence-conventions; free-text elaboration allowed after the code) shared by import-source holds, deprecations, acknowledgments, and not-admitted records — so reasons are countable and Bases-queryable, never free text alone.

**Anchor durability (invariant)**: `^claim-id` values in managed regions derive from stable content (Zotero annotation key, else quote hash) — **never from render order** — so full re-render preserves every claim link. Quote claims capture Web-Annotation-shaped context at extraction time (`exact` + 32-char `prefix`/`suffix`) — nearly free at capture, unreconstructable later; the re-anchoring *cascade* stays deferred (§10).

- Evidence-boundary tag: `quote | paraphrase | inference | open-question` (open-question = a claim with no derivation edge — itself lintable).
- `[@citekey, locator]` carries pinpoints (parses losslessly to CSL `locator`+`label`).
- `^block-id` anchors claims; **`citekey#^claim-id` is the global claim link** (no adoptable donor among surveyed products as of 2026-08-16; the *model* has precedent — micropublications' Support/Challenge graphs, Wikidata's per-statement references — first implementation on this substrate, not first idea; qualified 2026-08-22).
- Synthesis claims add: `confidence` (inference-only), per-claim `status`+reason, generating agent + date, and typed `supports`/`disputes` stance links to claim links. *(Corrected 2026-08-22: an earlier draft's scite three-value `stance` field — supports/contrasts/mentions — survived here after the revision that settled on the two CiTO-sense links everywhere else; contrasts/mentions were never implemented and the field does not exist.)*

**Event integrity**: actor convention (`human:eran` / `claude-fable-5/<version>` / `process:<id>`) applies to every generating/verifying identity. Note-level checks (DOI, metadata, update-notice) append verified events to literature frontmatter; claim-level quote events include the claim link and comparison target (`managed-region` or `source-text`) in `check`; publish events attach to the project. Machine-confirmed trust requires all applicable note-level checks and quote claims to match; human-reviewed additionally requires a `human:` event. Deterministic gate surfaces alone write these events, and CI recomputes their passes.

**Invariants**: deprecate-with-reason, never delete; contradictions preserved, never silently resolved. **Upgrade path (deferred)**: per-quote prefix/suffix re-anchoring + a machine-owned claim ledger (Option C), gated on a consistency check, only if the slice shows prose-parsing bottlenecks. Exact inline-field syntax is validated against Dataview/Bases parsers at build time.

## 6. Trust gates ([#10](https://github.com/eranroseman/knowledge-harness/issues/10))

**Doctrine**: detect always-on and free; enforce opt-in per surface. **Mandatory, judgment-independent closure exists only at the publish boundary**; deterministic offline checks may additionally close at pre-commit/CI exactly where the table says so. Every closed surface ships a named bypass artifact + a liveness bound before enabling — missing either is a defect. **Precision is a warn-tier obligation (added 2026-08-22): a warn-tier check class whose observed inbox precision collapses is itself a defect — to tune or demote — because a noisy queue trains click-through and degrades every gate (the rubber-stamp rule, §3); the closing checks are exempt by construction (deterministic, near-zero false-positive).**

| Check | Deterministic | Surfaces | Fail policy |
|---|---|---|---|
| Citekey exists in bibliography | yes, offline | PostToolUse warn; pre-commit; CI | open interactively; closed at pre-commit (bypass: `--no-verify`, honest layer is CI replay) and CI (bypass: entry in the review inbox acknowledged by `human:` actor) |
| DOI existence (doi.org handle API) | yes, network | skill at citation-add; CI | closed at publish on UNMATCHED **or UNREACHABLE** (publishing waits); SKIPPED exempt |
| Metadata match (Crossref; content-negotiation fallback for non-Crossref DOIs) | mostly | skill at ingest; CI batch | mismatch → review inbox; **no closure** (per #10 — compared fields title/authors/year, same normalization pipeline as quotes) |
| Update-notice check (Crossref `updated-by` full taxonomy; nightly RW CSV batch matched on **DOI + PMID**) | yes | ingest; scheduled CI; publish | **Blocking class** {retraction, partial_retraction, removal, withdrawal}: standing alert, blocks publish unless acknowledged (`[retraction-ack:: <code> …]`, §5). **Warn class** {expression_of_concern, correction, corrigendum, erratum}: review inbox, never closure. Reinstatement clears a standing alert. Inbox entries record **both** the notice's publication date and the detection date (bi-temporal — the validation project itself needs world-time). **Registry routing**: non-Crossref DOIs route by registration agency (`doi.org/doiRA`) — DataCite/arXiv items check OpenAlex `is_retracted` + version status instead of silently passing; unroutable registries → explicit inbox record, never silence |
| Quote verification | yes, given normalization | extraction; CI; publish | NFKC + whitespace-collapse + dehyphenation then exact = pass; fuzzy-only (normalized Levenshtein ≥ 0.90) = review inbox; below = fail; closed at publish. Day-one comparison target: draft quote vs the literature note's managed region (extracted quotes/annotations); direct-PDF-text leg deferred (§10); no extractable source text = UNREACHABLE, never fabrication — with a **per-quote human attestation path** (an ack entry: "verified against the source by `human:`") because annotations are sparse in real practice and honest quotes from unannotated passages must not read as failures |
| Factored verification (LLM decompose-and-check) | no | skill at draft→review | never blocks; adjudicated findings to the review inbox; default budget one pass per claim, capped at 30 claims, user-overridable — **selection under the cap is deterministic**: inference/paraphrase claims lacking any `verified` event first, contested-adjacent claims boosted, quote claims last (already deterministically covered); the skipped set is recorded in the inbox, never silently dropped |
| Evidence-layer protection (LLM free-writes into `literatures/` outside import; edits inside managed regions) | yes, offline | PostToolUse warn; pre-commit; CI | warn interactively; closed at pre-commit/CI with the citekey row's bypasses |
| Integrity lints (git-mechanical): **append-only** on `log/` + `inbox/review-queue.md`; **claim-immutability** (`^claim-id` never disappears or mutates without a §5 deprecation record); **published-drift** (published-tagged project vs HEAD); **`screening-state`** (claim cites an `excluded`/`superseded` note → warn, with `superseded-by` pointer); **`disputed-claim`** surfacing (drafted claim whose link carries `disputes` links → inbox entry at draft→review and publish — disconfirmation must be seen, per ACH); **`web-archive`** (type-web item ⇒ `archive-url` present and resolves) | yes, offline/cheap | PostToolUse warn; pre-commit; CI; draft→review | all warn-tier (review inbox); none close individually; publish gate counts unacknowledged **blocking-class** entries only |

- **The closed gate**: a Stop-hook publish gate (8-block bound; bypass: a documented manual bypass token recorded in the review inbox). **"Publish" = the `publish` skill action**, not branch merge. Arming: the `publish` skill writes a state flag file the Stop hook checks; the hook runs the closed checks, clears the flag on pass or explicit bypass, and is inert when the flag is absent. CI is the async auditor. Publish's concrete effect: sets `status: published` in the project's frontmatter, appends a `verified` event, commits and tags (`published/<project>-<date>`). Project `status` enum: `draft | parked | published | corrected | withdrawn`. Day-one disposition menu: mark-published (full gate run + effects above) / park (sets `status: parked`, nothing else) / keep-draft (no-op); deletion only on explicit request plus typed "discard". **Post-publish lifecycle** (IFCN corrections principle): a blocking-class alert or claim deprecation that targets an already-published project does not merely block the next publish — it opens a correction disposition: re-publish as `corrected` (new gate run, new `verified` event, new tag) or mark `withdrawn`; the original tag is never deleted.
- **Four-state results everywhere**: MATCHED / UNMATCHED / UNREACHABLE / SKIPPED. An outage is never reported as fabrication. The publish gate fails closed on UNREACHABLE (publishing waits); UNREACHABLE warns everywhere else. **SKIPPED is automatic-only and never permanent by silence** — produced when the item lacks the field the check needs (no identifier → identifier checks SKIPPED; no quote claims → quote check SKIPPED); never user- or agent-settable. Before SKIPPED sticks, `import-source` attempts **identifier discovery** (Crossref bibliographic query by title/author/year, PubMed lookup — the Citation-bot pattern): a DOI-less retracted paper must not be structurally exempt from the update-notice gate. Unresolvable items land in the review inbox with a reason code; SKIPPED results from publish-gate runs are recorded there too, so the gate run stays auditable.
- **Offline audit isolation**: synthetic `--offline` network outcomes may appear in explicit output but never mutate trust, markers, events, or inbox state. A genuine attempted outage remains a persisted UNREACHABLE. Surface sets decide blocking only; detection, pass events, current failure projection, markers, and inbox auditing are otherwise surface-independent.
- **Metadata author comparison**: ordered normalized family names; given names match on first initial; any divergence → review inbox.
- Passing results append `verified` events per §5; non-passing results go to the review inbox (§3). Adjacent deterministic lints (wikilinks resolve, frontmatter parses, tags present, managed-region edit warnings) ride the citekey row's placement.
- No adoptable prior art existed for this linter **at audit time (2026-08-16**, four repositories: [research/adoptable-skills-audit.md](../../../research/adoptable-skills-audit.md)**)** — it was built greenfield to this table. *(Scoped 2026-08-22: a later sweep found MIT citation-verification tools — medsci verify-refs, bibverify, harcx, bibtex-updater, CheckIfExist, hallucinator, HALLMARK's baseline registry — none audited against this substrate; the historical claim stands only as dated.)*

## 7. Skill inventory ([#11](https://github.com/eranroseman/knowledge-harness/issues/11))

**Control model: user-driven (Pocock).** Entry points ship `disable-model-invocation: true` (never in the model catalog — dissolves process-skill collisions); guard/reference skills are model-invoked AND user-invocable; `user-invocable: false` is reserved for future machine contracts (none ship). Plain descriptive names. Invocation policy must be correct in shipped frontmatter — `skillOverrides` cannot patch plugin skills.

| Skill | Type | Scope |
|---|---|---|
| `setup-vault` | entry | idempotent scaffold + **doctor mode** (substrate verification + provisioning repair) + provisioning: detect → report → per-item consent → install scriptable companions (`claude plugin install`; restart-to-activate; the companion list is a build-time constant in the plugin's provisioning manifest — currently one candidate: kepano/obsidian-skills) → wizard-guide human-only installs (Zotero .xpi: BBT required, MarkDB-Connect optional) → verify. Ships templates, Bases (open-questions, trust-tier), vault AGENTS.md, the pre-commit hook, and read-only CI only under `--with-ci`; a distinct explicit flag is required for the scheduled write-capable RW workflow. |
| `project` | entry | **the real-life entry point** — start/resume: question framing (inline elicitation procedure — owned, no external references; a framed question must state: the question itself, scope bounds in/out, expected source types, and success criteria), gap analysis vs synthesis + bibliography, draft frame in `projects/` **carrying the Iron Law via evidence-conventions** |
| `find-sources` | entry | literature search upstream of Zotero admission. **Vendored** fork of K-Dense `paper-lookup` (MIT): renamed, provenance output shaped to §5, terminates in the admission step. **Search provenance (PRISMA-S)**: every run appends to a search log (query as run, source, date, hit counts), and candidates *not* admitted are recorded with a reason code — the trail a methods reviewer can reconstruct |
| `import-source` | entry | catalog (literature note per §3–4) + **integrate-at-import**: synthesis updates + stance links land immediately except surgical auto-hold on contradiction, low/absent confidence, or schema violation — **each hold emits a reason-code review record** to the review inbox; catalog/index/log always land; batch mode for backfills; **registry-first dedup** (check the synthesis index before creating, beyond the re-import no-op); re-import no-op decided by **render-first comparison of the managed projection** (build-time correction: annotations and metadata live in Zotero's database, so attachment hashes cannot detect their changes — hashes keep their §5 fixity and ack-scope roles); 2+-source page-creation threshold; no-op is a legitimate outcome; refresh mode for note-level maintenance; **identifier discovery** before any SKIPPED sticks (§6); **archive-at-import** for web sources (trigger Save Page Now or record an existing snapshot — rescue is impossible at rot time, so the deferred rot cron only ever detects) |
| `verify-citations` | entry | the deterministic suite of §6; four-state results; appends `verified` events |
| `factcheck-draft` | entry | LLM factored verification at draft→review; adjudicated findings, never auto-blocking |
| `publish` | entry | the closed-gate surface; fixed disposition menu |
| `evidence-conventions` | guard | §5 schema + **the Iron Law: no claim enters a draft without a verified source first** + rationalization table; supports annotations-scatter-into-drafts via claim links |
| `synthesis-conventions` | guard | synthesis rules: the 2+-source page-creation threshold (the only threshold), minimum-link discipline (≥2 outgoing wikilinks per synthesis page), orientation-first (read `synthesis/index.md` + recent log before operating) |

- **Maintenance**: no dedicated maintenance skill beyond the nine — doctor mode + refresh mode own it, speaking the **fresh / stale / orphaned** vocabulary. Contradiction-probing and staleness repair form a separate cron-shaped maintenance lane (designed at build time), never the import path.
- **Shipped architecture**: deterministic-Python-core + thin-prompt-skill split (claude-obsidian's proven shape) for every skill with a mechanical core. **CLI, not MCP**: gate surfaces (pre-commit, CI) run where no MCP session exists, so the deterministic core ships as a CLI serving skills, hooks, and CI alike — one binary, one exit-code contract; **dependency discipline** (author ruling 2026-08-20, corrected same day by rethink audit — [docs/2026-08-20-zero-dep-rethink.md](../../2026-08-20-zero-dep-rethink.md)): minimal, pinned, audited runtime dependencies admitted case-by-case by **contract match** — default to the library for commodity hard problems; homebrew only when the contract mismatch can be named out loud — with nothing heavy imported on the gate path (hooks invoke the CLI per tool call, so import weight is latency; capability deps lazy-import), optional extras (`[pdf]`) lazy and feature-detected, vendoring preferred where behavior must stay frozen. First admissions under the rule: defusedxml in (commodity-hard XML), pydantic out (coercing exception-model validation vs four-state reporting); **diagnosis budget: when a pinned tool starts failing undiagnosed, set a named bound before diagnosing (e.g. two crashes or three hours); at the bound, pilot the alternative in parallel while diagnosis continues;** an MCP wrapper for external vault clients (Claude Desktop / other hosts querying the vault without the plugin) is a later-map option, and consumed MCP servers stay rejected per #8; `verify-citations` and `factcheck-draft` fork scientific-writing's offline audit scripts (SHA-256 claim hashing, verified-evidence-only counting) as starting points.
- **Dependency doctrine**: load-bearing capability is owned inline — no if-available branching anywhere in shipped text. Personal skills (grilling, domain-modeling, handoff, …) have **no plugin relationship**; composition is user-space (typed invocation, or routing lines in the user's own vault AGENTS.md). Docs may say "pairs well with." Vendored forks coexist harmlessly with installed originals (namespaced, typed-only, gate-protected; doctor reports duplication).
- **Two-flow accounting**: information flow (find → human admits via Zotero → catalog → integrate) fully owned; project flow (question → gap analysis → acquire → draft-under-Iron-Law → verify → publish) owned at minimal viability. Rich pipeline machinery (charter/outline/weave analogs) belongs to the later full-arc map.
- Hooks (§6) ship as plugin hooks, not skills.

## 8. Plugin architecture ([#12](https://github.com/eranroseman/knowledge-harness/issues/12))

- **Globally enabled.** Coexistence with the dev harness holds structurally: the typed-only spine claims no conversation openings; guards carry vault-scoped descriptions + `paths` globs; the residual free-form gray zone inside vault repos is routed by the vault's AGENTS.md — which `setup-vault` ships, so the mitigation self-installs per vault and touches nothing global.
- **This repo doubles as plugin + its own marketplace** (obra pattern): `.claude-plugin/plugin.json` + `marketplace.json`; `skills/` (nine); `hooks/`; vault scaffold under `templates/`; planning artifacts (research/, analysis/, docs/, the map) remain in-repo. Install: `/plugin marketplace add eranroseman/knowledge-harness`. Private-OK solo; public if ever shared.
- **Deferred to build**: paths-glob behavior on vault markdown; per-repo superpowers-disable (only if the gray zone proves real in the slice). Dead ends honored: no `skillOverrides` on plugin skills, no per-component toggles, no hook-ordering games, no cache edits.

## 9. Validation slice ([#14](https://github.com/eranroseman/knowledge-harness/issues/14), project chosen in [#13](https://github.com/eranroseman/knowledge-harness/issues/13))

One trip around the project flow over two corpora, plus a synthetic gate drill.

- **Seed migration**: this repo's research corpus (all files under `research/` — 10 at time of writing — plus `sources/` PDFs and `analysis/`; ~40 cited web sources, one live contradiction) batch-imports into the fresh vault — the information flow at volume.
- **Validation project (re-ruled 2026-08-22, superseding the #13 choice)**: **re-ask one of this project's own workshop research questions under the harness's rigorous process** (candidates: the PKM vault-schema question, the trust-gates prior-art question) — real research with a real consumer, which makes §9's routing-around falsification clause an honest instrument (a synthetic exercise tolerates friction real work would not), folds the corpus-to-vault migration methodology (findings 11–12: re-author at the vault's bar) into the validation itself, and yields a with/without-harness baseline comparison as the deliverable. The machinery-exercise the retraction question uniquely offered (natural retraction firing, acknowledge-tag flow) is covered by the synthetic gate drill, which exists precisely as that decoupled home. The retraction question (*how much are retracted papers cited after retraction, and do notification tools reduce it?*) is demoted to an optional later exercise if natural-fire evidence is ever wanted beyond the drill.
- **Phases**: (0) scaffold + doctor → (1) seed migration → (2) `project` framing + gap analysis → (3) `find-sources` → human admits 15–25 papers → `import-source` with live hold policy → (4) 1,000–2,000-word evidence brief in `projects/` under the Iron Law, ~10 synthesis pages touched → (5) `verify-citations` + `factcheck-draft` adjudication → (6) `publish`. Minimum two sessions (forces one real cold resume).
- **Synthetic gate drill** (decoupled from the research): planted fabricated citekey; mutated quote; unacknowledged retracted citation; simulated API outage (publish must wait); killed auto-export (staleness lint must fire); **a DOI-less retracted paper** (identifier discovery + PMID matching must catch it) and **a DataCite-DOI item** (registry routing must not read it as clean).
- **Validated when**: 100% of brief claims resolve mechanically per their tag — quote = normalize-then-exact pass against the source's managed region; paraphrase/inference = citekey + locator resolve to a real item; open-question = exempt from the denominator; `verified` events recorded and trust tier derivable in a Base; all five drills caught per §6; seed migration lands with zero unresolved links; cold-resume works; the author judges the brief defensible. **Measured output (added 2026-08-22, ruling on the false-positive finding): the slice also records per-check-class review-inbox precision — author-adjudicated actionable findings over total findings — as data, not a pass bar; a numeric budget waits for this measured base rate (HALLMARK's rates are other detectors on other corpora).**
- **Falsified when**: any planted error survives to publish, OR the author routes around the vault to get the work done — friction failure is foundation failure.

## 10. Deferred register

Rot-watch cron (provenance recorded now supports it retrofit-free; archive-at-import means it only detects, never rescues) · Option C claim ledger + the quote **re-anchoring cascade** (context capture is day-one per §5; only the cascade defers) · direct-PDF-text quote-verification leg (day-one target is the managed region, §6; comparison target recorded in events so tiers upgrade retrofit-free; **primary text source when built: Zotero local API `/fulltext` endpoints** — core-extracted, zero new dependency — with pypdf as fallback for index lag and page-resolved locators; widened 2026-08-22: the same leg feeds ingest-side digest authoring, which is **full-text-only by author ruling** — no digest is ever authored from the abstract alone; until this leg lands the sanctioned route is reading the attachment directly via its Zotero storage path, and no reachable full text means no digest, stated plainly) · **digest lexical-faithfulness lint (ruled 2026-08-22, slice finding 15)**: numbers, units, and named entities in a free-region digest must appear in the source text — deterministic, dependency-light, high-precision for the fabrication class; advisory only (files findings, never blocks, never mints — a probabilistic-adjacent check may not wear MATCHED clothing); same trigger as the direct-PDF-text leg (needs `/fulltext`); full NLI/embedding faithfulness scoring REJECTED (weak on negation, model drift breaks reproducibility, §8 weight) · **skill-eval model-change trigger (recorded 2026-08-22)**: the behavioral skill-eval suite (polish-pass item 11 — RED-phase harness over rationalization-table scenarios) re-runs when the session model changes, because skill prose is model-relative (the no-op test's premise): a model change silently changes which prose is load-bearing, and form tests cannot see it · **confidence-axis split (recorded 2026-08-22)**: §5's `[confidence:: ...]` deliberately conflates two axes ICD 203 keeps apart — likelihood of the claim versus confidence in the judgment — because day-one usage (the integration hold keys on low/absent) needs only one signal; split trigger = slice friction, the moment a claim wants to say "likely, but I barely trust the judgment" · publish-time portable verification manifest (RO-Crate-shaped export of claim links, results, bibliography hash — portability, not trust) · PreToolUse blocking for the citekey lint (revisit only on evidence that warnings fail, staleness lint fixed first) · BibLaTeX projection (at LaTeX drafting; re-ruled 2026-08-22 — harness-regenerated over `literatures/` membership from the CSL universe, `log.md` artifact class; replaces the second-auto-export plan, see §4) · content write-back (Zotero 10 + per-event approval) · press-check Stop opt-in for factored verification · maintenance cron lane design · full-arc pipeline machinery · other workload pipelines (reports, PKM loop, long-form) · **mutation-testing baseline (post-Plan-T, parallelizable with Plan D)**: build mutate4py test-contexts DB, blanket run (~1,250 sites, ~10-15 min narrowed), commit sidecar manifests (never embedded mode), wire differential "no new survivors" CI advisory check — planned: docs/superpowers/plans/2026-08-20-plan-q-quality-lane.md — timing rule: baseline must follow the terminology wave (renames invalidate AST-hash manifests wholesale); may run in its own worktree parallel to Plan D — D is mostly markdown, and any core-Python overlap costs only a differential top-up at merge (see research/code-quality-tools-gabadi.md) — **mutate4py exit rule (pre-registered 2026-08-22, after seven defects surfaced in one night of baseline work — off-by-one selection decode, swallowed child-pytest output, evidence-destroying worker cleanup, measured parallel-path memory leak, uv-only provisioning, missing recycle/keep knobs, unexplained exit-4 class): decided at the post-deepening re-baseline, not before and not relitigated — upstream fixed the filed defects → upgrade and stay; upstream unresponsive AND exit-4 still unexplained → switch to mutmut, pay one re-baseline (cost measured 2026-08-22: contexts ~10 min, hybrid blanket under memory cap); vendoring rejected at every branch; **checkpoint method (pre-registered 2026-08-22): the decision is empirical, not datasheet — a bounded mutmut pilot over three representative modules (one that exit-4'd, one clean, one large) comparing survivor sets, wall-clock, and memory against the mutate4py baseline; survivor-set disagreement between engines is itself signal (different operator sets = richer test-gap map), so the pilot pays even on a stay verdict; cosmic-ray is the fallback only if mutmut's runner conventions clash with the suite; mutatest/poodle/pytest-mutagen/universalmutator rejected (stalled maintenance or wrong class); **pilot timing amended 2026-08-22 (new evidence, not re-derivation: the baseline landed 77% with the six-module hole over four trust-core modules — events, frontmatter, gitstate, outcome — and serial does not rescue the failure class): the pilot may run in any idle window after Plan Q merges, pre-collecting the comparison data; the DECISION stays at the checkpoint. Same day, diagnosis frozen alongside role: mutate4py is a passive instrument at the pin — banked baseline, exclusion-listed gate, low-exposure CI differential — no new experiments or patches; the filed upstream reports are the running experiment (maintainer responsiveness is the checkpoint's datum)**. Until that checkpoint mutate4py's role is frozen — no new lanes, checks, or workflows depend on it; the CI differential lane stays the low-exposure surface (small counts, short runs, no contexts) with cap-kill vs exit-4 failure attribution from day one** · **evidence-shape modeling (post-Plan-D)**: type the external JSON shapes (CSL-JSON item, Zotero/BBT responses, frontmatter) as `TypedDict`s (functional syntax for hyphenated CSL keys, `NotRequired` for the optional field space) with dataclasses only for internally-owned domain objects — ruled 2026-08-20: no pydantic/attrs/msgspec (contract mismatch, per the §8 dependency discipline: exception-based coercing validation can't feed four-state results in a byte-faithful projection layer); runtime validation stays in explicit checks; unlocks the mypy ratchet rungs recorded in Plan Q · **architecture deepening pass (post-§9-slice)**: run `/improve-codebase-architecture` once the validation slice has generated real friction — inputs also include: `validate_reason` moves into `outcome.py`, dissolving the cycle the deferred import routes around (recorded at Plan R Task 2); from Plan D's review (2026-08-22): `paths.project_dir` extraction, `finding_id` collision guard in `verify._file_effects`, the third `_git` copy, `publish.py`'s private verify imports, `searchlog`'s unused parser and unwired `durable=` — inputs: CONTEXT.md (exists post-Plan-T), ADRs, Plan Q's CRAP table (complexity-at-full-coverage = shallow-module signal) and mutation survivors (interfaces tests can't grip); accepted deepening absorbs into the mutation differential, re-baseline (~15 min) if wholesale · **uniform import hold-to-inbox wiring**: every `import-note`/import-flow failure exit emits a reason-coded review record (spec §121 contract) — wired uniformly when integrate-at-import lands (Plan D `import-source`), not piecemeal (ruled 2026-08-21 at Plan R Task 1: render rejections stay loud fail-closed stderr+exit until then); evaluate then: **blockquote-split repair** for U+2028/U+2029 in quote text (render splits `annotationText` on the full `splitlines()` set so each segment gets its `> ` prefix — content preserved, no hold) vs the current honest hold · **release gate — CORPUS-TO-VAULT (ruled 2026-08-21, superseding same-day projection ruling after author push-back; HARD blocker before any public listing)**: the corpus leaves rather than the payload being projected around it — solve by subtraction, and the harness's own doctrine names the destinations: `sources/` PDFs are evidence artifacts and belong in **Zotero storage outside any git boundary** (spec §2 preservation boundary — admitting them dissolves the copyright-in-history problem at its root); the record seam (`research/`, `analysis/`, audit records, completed plans — write-once, no machine consumer in-repo) is **knowledge work and belongs in a knowledge-harness-managed vault** — its migration IS a §9 validation-slice candidate (the author's original 'turn this folder into a vault', now with a real corpus and real stakes). What stays with the code: the living contracts development consumes in place (spec, terminology, CONTEXT.md, ADRs, active plans). Public flip then costs one coordinated history scrub scoped to `sources/` paths only (our own prose in history is publishable; the PDFs are not) — feasible now with two writers, prohibitive later. **Layout flip executed (re-ruled 2026-08-21; Plan L merged 2026-08-22): the package is at repo root.** After the corpus migration and scrub, this repo goes public in the target ecosystem's own convention (develop-in-public, per claude-obsidian/superpowers practice). **Sequencing**: the receiving vault requires Plan D's skills; until then nothing moves. **Fallback if the vault path stalls**: the projection model (private workshop + CI-projected read-only distribution repo, Symfony/Laravel-split-class practice) — recorded 85280ce, demoted not deleted · **Plan D landing checks (recorded 2026-08-22)**: the vault AGENTS.md template cites `evidence-conventions` — a dangling pointer until that skill ships; Plan D's skill-authoring tasks verify every skill name the template cites exists at merge, and any skill rename moves the template in the same commit (dialect-surface rule) · queued next research projects: post-#13-runner-up questions (claim-level provenance-model adoption; LLM citation-accuracy evaluations) · **ADR candidates parked** (author ruling 2026-08-20, "for now"): evidence-layer-is-projection (citability only through the reference manager of record; never free-written) and deterministic-only closure (probabilistic findings never block; bypasses are recorded data) — both judged entangled with the user-driven control model, which is expected to evolve; revisit once the control model itself is beyond reasonable doubt.

## 11. References

Research: [prior-art](../../../research/prior-art-knowledge-work-harness.md) · [PKM schemas](../../../research/pkm-vault-schemas.md) · [bridge design space](../../../research/zotero-bridge-design-space.md) · [provenance schemas](../../../research/provenance-schemas.md) · [trust gates](../../../research/trust-gates-prior-art.md) · [plugin mechanics](../../../research/plugin-packaging-mechanics.md) · [adoptable skills](../../../research/adoptable-skills-audit.md) · [skill-inventory gaps](../../../research/skill-inventory-gap-analysis.md) · [llm-wiki integration](../../../research/llm-wiki-integration-prior-art.md) · [Ideaverse structure](../../../research/ideaverse-lite-structure.md). Recovered primaries: [sources/](../../../sources/). Dev-harness anatomy: [analysis/](../../../analysis/). Decisions index: [the map](https://github.com/eranroseman/knowledge-harness/issues/1). Pre-approval audit: [rethink audit](../../2026-08-16-spec-rethink-audit.md) + [raw wide-sweep findings](../../../research/raw/wide-prior-art/).
