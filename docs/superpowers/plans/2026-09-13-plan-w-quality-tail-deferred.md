# Plan W deferred findings

Minor findings raised by the per-task reviews of `2026-09-13-plan-w-quality-tail.md`, and
concerns an implementer reported but could not act on, deliberately held out of their tasks' fix
loops. None blocks a merge.

Rows are appended by the controller as each finding is deferred, so the record exists at the
moment the finding is set aside rather than being assembled at the end.

**Task 25 Step 2b's results file dispositions every row.** Each ends **fixed**, naming the
commit, or **declined**, with the reason written into the row. Rows still open after the final
whole-branch review become one repository issue (`gh issue create --label ready-for-agent`) with
the open rows as its body.

## Deferred findings

| #   | Task | File                                                                                                                                       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                       | Disposition                                                                                           |
| --- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| 1   | 23   | `scripts/mutmut_shims/run_mutmut.py`                                                                                                       | Implementer concern: the launcher's `--help` failed with `FileNotFoundError` when `MUTANT_UNDER_TEST` was unset and no `[tool.mutmut]` existed, because the shim's config seed only runs inside a mutmut run.                                                                                                                                                                                                                                 | fixed — 259abd2 (Task 24 item g)                                                                      |
| 2   | 24   | `scripts/mutation_gate.py:45`                                                                                                              | Header's status list omitted mutmut's timeout (24/152/255/-24/36), segfault (-9/-11) and type-check (37) codes.                                                                                                                                                                                                                                                                                                                               | fixed — 5c8fd3e (same header edit as fix round 1)                                                     |
| 3   | 24   | `scripts/mutation_gate.py:156` vs `:171`                                                                                                   | `mutation_key` re-enters `_mutmut(ROOT)` per mutant (two `os.chdir` round-trips) while `read_module_results` takes `root`; the key tests therefore depend on the real `pyproject.toml` carrying `[tool.mutmut]`, against the test module's own docstring.                                                                                                                                                                                     | open                                                                                                  |
| 4   | 24   | `scripts/mutation_gate.py:298-303`                                                                                                         | A malformed `.meta` (truncated JSON, missing `exit_code_by_key`, a key without `__mutmut_`) tracebacks out of the module loop instead of classing that module `error`; later modules go unmeasured and the offender has no record. Never a silent `ok`.                                                                                                                                                                                       | open                                                                                                  |
| 5   | 24   | `scripts/mutation_gate.py:430-449`                                                                                                         | `_update_baseline`'s refusal path returns before the no-tests summary that gate mode prints unconditionally.                                                                                                                                                                                                                                                                                                                                  | open                                                                                                  |
| 6   | 24   | `tests/test_mutation_gate.py`                                                                                                              | Unpinned: the `--max-children` CLI-to-argv plumbing (fakes ignore the argument), the end-of-run `_summarise_no_tests` block, and `mutation_text`'s claim that a body line beginning `--` survives.                                                                                                                                                                                                                                            | open                                                                                                  |
| 7   | 24   | `scripts/mutation_gate.py:511-519`                                                                                                         | `--only` without `--out-dir` always refuses (every other module lands in "not measured"); an argparse error up front would say so before a run that cannot succeed.                                                                                                                                                                                                                                                                           | open                                                                                                  |
| 8   | 24   | `scripts/mutmut_shims/run_mutmut.py:43`                                                                                                    | `--help` is recognised only as the first argument; `run_mutmut.py <pattern> --help` still imports mutmut and dies outside a configured root (documented in the usage text).                                                                                                                                                                                                                                                                   | open                                                                                                  |
| 9   | 24   | `tests/test_mutation_gate.py:408`                                                                                                          | `assert proc.stderr == ""` in the launcher `--help` test is brittle to any warning the environment routes to stderr (`PYTHONWARNINGS`, a venv deprecation).                                                                                                                                                                                                                                                                                   | open                                                                                                  |
| 10  | 24b  | `tests/test_skill_contracts.py:233`                                                                                                        | Ragged wrap left by a rewrite: a 30-character line (`shape, second corpus: a bare`) followed by an unreflowed 78-character line.                                                                                                                                                                                                                                                                                                              | open                                                                                                  |
| 11  | 24b  | `tests/test_publish.py:617`                                                                                                                | `that is how the mismatch survives a green suite` — the test now pins the clock, so present tense states what the test contradicts; `would survive` keeps it a constraint about stubbed helpers.                                                                                                                                                                                                                                              | open                                                                                                  |
| 12  | 24b  | `research_vault/scaffold.py:424`                                                                                                           | `(the same reading zotero_base gets)` is hard to parse on first read; `(as zotero_base is read)` says the same thing.                                                                                                                                                                                                                                                                                                                         | open                                                                                                  |
| 13  | 24b  | commit `32d931a` body                                                                                                                      | The `Kept (constraints)` block lists 28 entries for "27 stay as written" and the `Deleted` block counts rewrites under a header that reads as whole-hit deletion.                                                                                                                                                                                                                                                                             | declined — the body is history; rewriting a landed SHA to reword it is not worth the churn            |
| 14  | 24c  | `pyproject.toml:185`                                                                                                                       | "one run retained ~249 MB of vaults" is a kept measurement with no method or date, unlike `tests/conftest.py:73`; the Task 24b doctrine asks for both.                                                                                                                                                                                                                                                                                        | open                                                                                                  |
| 15  | 24c  | `tests/conftest.py:304-305`                                                                                                                | The `live`/`live_net` marker branches of `_no_socket` are unreachable (the collection hook skips marked items when the flag is unset; `_live_flags_set()` short-circuits when it is set); the docstring and `docs/testing.md:13` present the marker as the mechanism.                                                                                                                                                                         | fixed — 7324e11 (Step 4d: the block is decided by the markers alone)                                  |
| 16  | 24c  | `tests/test_config_validity.py:699-702`                                                                                                    | `test_a_misspelled_marker_is_a_collection_error` asserts `getoption("strict_markers") is True`; the name promises a collection error the body never provokes.                                                                                                                                                                                                                                                                                 | open                                                                                                  |
| 17  | 24c  | `tests/conftest.py:288-325`                                                                                                                | Inherent to a `connect` monkeypatch: DNS resolution still happens in libc before the block fires, and UDP `sendto` is not intercepted. No package transport uses either without a subsequent TCP connect.                                                                                                                                                                                                                                     | declined — no transport reaches either path; documenting it would describe a hole that does not exist |
| 18  | 24c  | `docs/testing.md:15`                                                                                                                       | Names `tests/test_dispositions.py`, which does not exist at HEAD (pre-existing).                                                                                                                                                                                                                                                                                                                                                              | fixed — 24d, `chore: version currency + CI hardening` (SHA not self-referenceable in one commit)      |
| 19  | 24c  | `docs/testing.md:13`                                                                                                                       | The doctrine line should say that a subprocess CLI test is hermetic only through `dead_base` (the in-process block cannot reach it), so a future subprocess test knows it must take the fixture.                                                                                                                                                                                                                                              | fixed — 24d, `chore: version currency + CI hardening` (SHA not self-referenceable in one commit)      |
| 20  | 24d  | `pyproject.toml:124` vs `:180`                                                                                                             | `ANN` sits in the "Deliberately NOT selected" block but not in `ignore`, so the structural claim at `:135` (skip families sit in `ignore` as prefixes no later default expansion can re-enable) does not cover it. Harmless today (ANN absent from ruff 0.16.7's defaults).                                                                                                                                                                   | open                                                                                                  |
| 21  | 24d  | `.github/workflows/quality.yml:43`                                                                                                         | "pinned exact and digest-checked, like every other tool in this lane" overstates: pip-installed tools are pinned exact but not digest-checked, and `actions/*@v7` are major tags. The commit subject repeats it.                                                                                                                                                                                                                              | open                                                                                                  |
| 22  | 24d  | `.github/workflows/quality.yml:54-62,71-75`                                                                                                | The download/verify/install steps carry no `set -euo pipefail` (GitHub's default `bash -e` still aborts on a digest mismatch because `sha256sum -c` ends each pipe); inconsistent with the drift job's explicit `set -euo pipefail` at `:108`.                                                                                                                                                                                                | open                                                                                                  |
| 23  | 25   | `scripts/mutation_gate.py` header, `.github/workflows/quality.yml` gate step comment                                                       | "children × cap … is the guarantee that the OOM killer never fires" overclaims: the mutmut parent and every test-spawned subprocess are capped too, so the strict bound is `(children + 1 + concurrent test subprocesses) × cap`; say "bound", not "guarantee".                                                                                                                                                                               | open                                                                                                  |
| 24  | 25   | report accepted-reason wording                                                                                                             | Two accepted survivors carry a wrong premise with the right conclusion (`captured._structural` `[:10]→[:11]` — `refresh_due` fixtures are instants, equivalence holds because the first ten characters agree; `scaffold._trackable_paths` check-ignore — "not ignored from any cwd" is true only of a fresh vault; the honest statement is "the ignore branch is untested logic, baselined wholesale as pre-Part-A"). Dispositions unchanged. | open                                                                                                  |
| 25  | 25   | `tests/test_scaffold.py:178-181`                                                                                                           | Docstring says scaffold "still reads the vault's own ignore rules"; the assertion passes because `_LOCAL_ONLY_PATHS` skips `machine.json` before any git call, not because check-ignore was consulted.                                                                                                                                                                                                                                        | open                                                                                                  |
| 26  | 25   | `tests/test_propagate.py:653-676`, `tests/test_searchlog_cli.py:289-291`, `tests/test_publish.py:238-240`, `tests/test_finding_cli.py:315` | Kill tests pin verbatim stderr prose where the branch flip is the target; `startswith(<code>)` / `in` would keep the kills without freezing sentences the #42 ruling's class (a) says need not be pinned.                                                                                                                                                                                                                                     | open                                                                                                  |
| 27  | 25   | `tests/test_mutation_gate.py:568,611`                                                                                                      | The two real-git isolation tests build their probe env from `os.environ`, not `_git_env`; a runner with `GIT_DIR` exported would perturb them.                                                                                                                                                                                                                                                                                                | open                                                                                                  |
| 28  | 25   | `scripts/mutation_gate.py` `_isolate_git`                                                                                                  | An own `mutants/.git`'s contents (hooks, config) persist across local runs — verification checks identity, not contents; the `rm -rf mutants/` operator note stands.                                                                                                                                                                                                                                                                          | open                                                                                                  |
| 29  | 25   | `research_vault/scaffold.py` (hook install)                                                                                                | Product-side containment: scaffold's hook install has no guard against a vault argument that is not a vault (a mutant with `None`/`""`/`"."` wrote the vault pre-commit template into the repository's shared `.git/hooks` on 2026-09-14T02:13Z); refuse a hook path outside the vault's own `.git`, and a destination that is a worktree of another repository.                                                                              | open — ready-for-agent                                                                                |
| 30  | 25   | `research_vault/verify.py` snapshot core                                                                                                   | 13 functions / 50 in-scope mutation survivors not dispositioned (`_claim_anchor_hash` 13, `_identifier_hash` 12, `_plan_state` 6, `_mutate_marker`, `clear_marker_for`, `surface_decision` 3 each, …) — the plan-sanctioned remainder, listed in the results file.                                                                                                                                                                            | open — ready-for-agent                                                                                |
