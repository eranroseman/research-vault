# Plan W deferred findings

Minor findings raised by the per-task reviews of `2026-09-13-plan-w-quality-tail.md`, and
concerns an implementer reported but could not act on, deliberately held out of their tasks' fix
loops. None blocks a merge.

Rows are appended by the controller as each finding is deferred, so the record exists at the
moment the finding is set aside rather than being assembled at the end.

**Task 25 Step 2b's results file dispositions every row.** Each ends **fixed**, naming the
commit, or **declined**, with the reason written into the row. Rows still open after the final
whole-branch review become one repository issue (`gh issue create --label ready-for-agent`) with
the open rows as its body.

**Closed 2026-09-14 (Task 26 Step 1, the fix wave after the whole-branch review):** 46 rows —
34 fixed, 10 declined, 2 filed as #131 (rows 29 and 30, with the eight new modules' outside-scope
survivor counts from the results file,
`docs/superpowers/specs/2026-09-13-plan-w-quality-tail-results.md`).

## Deferred findings

| #   | Task | File                                                                                                                                       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                       | Disposition                                                                                                                                                                                                                                                                                                                          |
| --- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 23   | `scripts/mutmut_shims/run_mutmut.py`                                                                                                       | Implementer concern: the launcher's `--help` failed with `FileNotFoundError` when `MUTANT_UNDER_TEST` was unset and no `[tool.mutmut]` existed, because the shim's config seed only runs inside a mutmut run.                                                                                                                                                                                                                                 | fixed — 259abd2 (Task 24 item g)                                                                                                                                                                                                                                                                                                     |
| 2   | 24   | `scripts/mutation_gate.py:45`                                                                                                              | Header's status list omitted mutmut's timeout (24/152/255/-24/36), segfault (-9/-11) and type-check (37) codes.                                                                                                                                                                                                                                                                                                                               | fixed — 5c8fd3e (same header edit as fix round 1)                                                                                                                                                                                                                                                                                    |
| 3   | 24   | `scripts/mutation_gate.py:156` vs `:171`                                                                                                   | `mutation_key` re-enters `_mutmut(ROOT)` per mutant (two `os.chdir` round-trips) while `read_module_results` takes `root`; the key tests therefore depend on the real `pyproject.toml` carrying `[tool.mutmut]`, against the test module's own docstring.                                                                                                                                                                                     | fixed — 7374808 (`mutation_key(..., root=ROOT)`; the key tests run under a tmp root)                                                                                                                                                                                                                                                 |
| 4   | 24   | `scripts/mutation_gate.py:298-303`                                                                                                         | A malformed `.meta` (truncated JSON, missing `exit_code_by_key`, a key without `__mutmut_`) tracebacks out of the module loop instead of classing that module `error`; later modules go unmeasured and the offender has no record. Never a silent `ok`.                                                                                                                                                                                       | fixed — 7374808 (`MetaReadError` names the file and the fault; the module classes `error` on its own line, is recorded, and the loop goes on)                                                                                                                                                                                        |
| 5   | 24   | `scripts/mutation_gate.py:430-449`                                                                                                         | `_update_baseline`'s refusal path returns before the no-tests summary that gate mode prints unconditionally.                                                                                                                                                                                                                                                                                                                                  | fixed — 7374808 (the refusal path ends with the no-tests block)                                                                                                                                                                                                                                                                      |
| 6   | 24   | `tests/test_mutation_gate.py`                                                                                                              | Unpinned: the `--max-children` CLI-to-argv plumbing (fakes ignore the argument), the end-of-run `_summarise_no_tests` block, and `mutation_text`'s claim that a body line beginning `--` survives.                                                                                                                                                                                                                                            | fixed — 7374808 (three pins: the launch flags in both modes, the exact end-of-run block, a `--` body line)                                                                                                                                                                                                                           |
| 7   | 24   | `scripts/mutation_gate.py:511-519`                                                                                                         | `--only` without `--out-dir` always refuses (every other module lands in "not measured"); an argparse error up front would say so before a run that cannot succeed.                                                                                                                                                                                                                                                                           | fixed — 7374808 (`--only requires --out-dir`, argparse, before anything runs)                                                                                                                                                                                                                                                        |
| 8   | 24   | `scripts/mutmut_shims/run_mutmut.py:43`                                                                                                    | `--help` is recognised only as the first argument; `run_mutmut.py <pattern> --help` still imports mutmut and dies outside a configured root (documented in the usage text).                                                                                                                                                                                                                                                                   | declined — `--help` only as the first argument is the documented usage; beyond it the shim stays verbatim from the pilot, and a positional before `--help` is a run, not a help request                                                                                                                                              |
| 9   | 24   | `tests/test_mutation_gate.py:408`                                                                                                          | `assert proc.stderr == ""` in the launcher `--help` test is brittle to any warning the environment routes to stderr (`PYTHONWARNINGS`, a venv deprecation).                                                                                                                                                                                                                                                                                   | fixed — 7374808 (asserts no traceback on stderr, not an empty stderr)                                                                                                                                                                                                                                                                |
| 10  | 24b  | `tests/test_skill_contracts.py:233`                                                                                                        | Ragged wrap left by a rewrite: a 30-character line (`shape, second corpus: a bare`) followed by an unreflowed 78-character line.                                                                                                                                                                                                                                                                                                              | fixed — 7374808 (reflowed)                                                                                                                                                                                                                                                                                                           |
| 11  | 24b  | `tests/test_publish.py:617`                                                                                                                | `that is how the mismatch survives a green suite` — the test now pins the clock, so present tense states what the test contradicts; `would survive` keeps it a constraint about stubbed helpers.                                                                                                                                                                                                                                              | fixed — 7374808 (`would survive`)                                                                                                                                                                                                                                                                                                    |
| 12  | 24b  | `research_vault/scaffold.py:424`                                                                                                           | `(the same reading zotero_base gets)` is hard to parse on first read; `(as zotero_base is read)` says the same thing.                                                                                                                                                                                                                                                                                                                         | fixed — bf19870 (`(as zotero_base is read)`, the scratch branch's wording, so the local and runner mutant sets are identical)                                                                                                                                                                                                        |
| 13  | 24b  | commit `32d931a` body                                                                                                                      | The `Kept (constraints)` block lists 28 entries for "27 stay as written" and the `Deleted` block counts rewrites under a header that reads as whole-hit deletion.                                                                                                                                                                                                                                                                             | declined — the body is history; rewriting a landed SHA to reword it is not worth the churn                                                                                                                                                                                                                                           |
| 14  | 24c  | `pyproject.toml:185`                                                                                                                       | "one run retained ~249 MB of vaults" is a kept measurement with no method or date, unlike `tests/conftest.py:73`; the Task 24b doctrine asks for both.                                                                                                                                                                                                                                                                                        | fixed — 7374808 (`measured 2026-08-24: du -sh /tmp/pytest-of-<user> after one serial run`)                                                                                                                                                                                                                                           |
| 15  | 24c  | `tests/conftest.py:304-305`                                                                                                                | The `live`/`live_net` marker branches of `_no_socket` are unreachable (the collection hook skips marked items when the flag is unset; `_live_flags_set()` short-circuits when it is set); the docstring and `docs/testing.md:13` present the marker as the mechanism.                                                                                                                                                                         | fixed — 7324e11 (Step 4d: the block is decided by the markers alone)                                                                                                                                                                                                                                                                 |
| 16  | 24c  | `tests/test_config_validity.py:699-702`                                                                                                    | `test_a_misspelled_marker_is_a_collection_error` asserts `getoption("strict_markers") is True`; the name promises a collection error the body never provokes.                                                                                                                                                                                                                                                                                 | fixed — 7374808 (renamed `test_strict_markers_is_on_so_a_misspelled_marker_cannot_collect`; the docstring says the collection error is pytest's own)                                                                                                                                                                                 |
| 17  | 24c  | `tests/conftest.py:288-325`                                                                                                                | Inherent to a `connect` monkeypatch: DNS resolution still happens in libc before the block fires, and UDP `sendto` is not intercepted. No package transport uses either without a subsequent TCP connect — but `urllib` resolves first, so a leaked hostname is a real DNS query on one machine and a `gaierror` outage on another.                                                                                                           | fixed — 08cd9e6 (no transport uses UDP directly; the resolver step is blocked at `getaddrinfo`, under the same marker gate and with the same error as the connect block)                                                                                                                                                             |
| 18  | 24c  | `docs/testing.md:15`                                                                                                                       | Names `tests/test_dispositions.py`, which does not exist at HEAD (pre-existing).                                                                                                                                                                                                                                                                                                                                                              | fixed — d402858 (`chore: version currency + CI hardening`)                                                                                                                                                                                                                                                                           |
| 19  | 24c  | `docs/testing.md:13`                                                                                                                       | The doctrine line should say that a subprocess CLI test is hermetic only through `dead_base` (the in-process block cannot reach it), so a future subprocess test knows it must take the fixture.                                                                                                                                                                                                                                              | fixed — d402858 (`chore: version currency + CI hardening`)                                                                                                                                                                                                                                                                           |
| 20  | 24d  | `pyproject.toml:124` vs `:180`                                                                                                             | `ANN` sits in the "Deliberately NOT selected" block but not in `ignore`, so the structural claim at `:135` (skip families sit in `ignore` as prefixes no later default expansion can re-enable) does not cover it. Harmless today (ANN absent from ruff 0.16.7's defaults).                                                                                                                                                                   | fixed — 7374808 (`"ANN"` in `ignore`, one clause on the comment; pyproject-fmt round-trip unchanged)                                                                                                                                                                                                                                 |
| 21  | 24d  | `.github/workflows/quality.yml:43`                                                                                                         | "pinned exact and digest-checked, like every other tool in this lane" overstates: pip-installed tools are pinned exact but not digest-checked, and `actions/*@v7` are major tags. The commit subject repeats it.                                                                                                                                                                                                                              | fixed — 7374808 (comment: pinned exact, the downloaded binaries digest-checked, pip pins and `actions/*` majors named as what they are; the commit subject stays as history)                                                                                                                                                         |
| 22  | 24d  | `.github/workflows/quality.yml:54-62,71-75`                                                                                                | The download/verify/install steps carry no `set -euo pipefail` (GitHub's default `bash -e` still aborts on a digest mismatch because `sha256sum -c` ends each pipe); inconsistent with the drift job's explicit `set -euo pipefail` at `:108`.                                                                                                                                                                                                | fixed — 7374808 (`set -euo pipefail` opens both download/verify blocks — the row named two and there are two)                                                                                                                                                                                                                        |
| 23  | 25   | `scripts/mutation_gate.py` header, `.github/workflows/quality.yml` gate step comment                                                       | "children × cap … is the guarantee that the OOM killer never fires" overclaims: the mutmut parent and every test-spawned subprocess are capped too, so the strict bound is `(children + 1 + concurrent test subprocesses) × cap`; say "bound", not "guarantee".                                                                                                                                                                               | fixed — 7374808 (header and workflow comment: "bound", the strict `(children + 1 + concurrent test subprocesses) × cap` form)                                                                                                                                                                                                        |
| 24  | 25   | report accepted-reason wording                                                                                                             | Two accepted survivors carry a wrong premise with the right conclusion (`captured._structural` `[:10]→[:11]` — `refresh_due` fixtures are instants, equivalence holds because the first ten characters agree; `scaffold._trackable_paths` check-ignore — "not ignored from any cwd" is true only of a fresh vault; the honest statement is "the ignore branch is untested logic, baselined wholesale as pre-Part-A"). Dispositions unchanged. | declined — the task report is ephemeral (the SDD workspace is deleted at Finish); the results file carries the corrected wording for both survivors, and the `_trackable_paths` check-ignore branch is now tested outright (row 25)                                                                                                  |
| 25  | 25   | `tests/test_scaffold.py:178-181`                                                                                                           | Docstring says scaffold "still reads the vault's own ignore rules"; the assertion passes because `_LOCAL_ONLY_PATHS` skips `machine.json` before any git call, not because check-ignore was consulted.                                                                                                                                                                                                                                        | fixed — bf19870 (docstring names the local-only rule; `test_scaffold_skips_a_created_path_the_vaults_own_gitignore_ignores` carries the check-ignore claim and kills the CI-exposed pair)                                                                                                                                            |
| 26  | 25   | `tests/test_propagate.py:653-676`, `tests/test_searchlog_cli.py:289-291`, `tests/test_publish.py:238-240`, `tests/test_finding_cli.py:315` | Kill tests pin verbatim stderr prose where the branch flip is the target; `startswith(<code>)` / `in` would keep the kills without freezing sentences the #42 ruling's class (a) says need not be pinned.                                                                                                                                                                                                                                     | fixed — 7374808 (exit code, `startswith(<code prefix>)` and the branch's own token; the kills are kept — every mutmut string operator breaks the prefix — and seven of the mutants were re-run against the edited tests to show it; no re-measure, test-only edits in baselined modules)                                             |
| 27  | 25   | `tests/test_mutation_gate.py:568,611`                                                                                                      | The two real-git isolation tests build their probe env from `os.environ`, not `_git_env`; a runner with `GIT_DIR` exported would perturb them.                                                                                                                                                                                                                                                                                                | fixed — 7374808 (`_git_env(root)` + `_isolate_git(root)` in both real-git tests)                                                                                                                                                                                                                                                     |
| 28  | 25   | `scripts/mutation_gate.py` `_isolate_git`                                                                                                  | An own `mutants/.git`'s contents (hooks, config) persist across local runs — verification checks identity, not contents; the `rm -rf mutants/` operator note stands.                                                                                                                                                                                                                                                                          | declined — identity is verified before every run (`_is_own_repository`); the contents of an own `mutants/.git` persist by design across local runs, and `rm -rf mutants/` is the operator note the gate header carries                                                                                                               |
| 29  | 25   | `research_vault/scaffold.py` (hook install)                                                                                                | Product-side containment: scaffold's hook install has no guard against a vault argument that is not a vault (a mutant with `None`/`""`/`"."` wrote the vault pre-commit template into the repository's shared `.git/hooks` on 2026-09-14T02:13Z); refuse a hook path outside the vault's own `.git`, and a destination that is a worktree of another repository.                                                                              | issue — #131                                                                                                                                                                                                                                                                                                                         |
| 30  | 25   | `research_vault/verify.py` snapshot core                                                                                                   | 13 functions / 50 in-scope mutation survivors not dispositioned (`_claim_anchor_hash` 13, `_identifier_hash` 12, `_plan_state` 6, `_mutate_marker`, `clear_marker_for`, `surface_decision` 3 each, …) — the plan-sanctioned remainder, listed in the results file.                                                                                                                                                                            | issue — #131                                                                                                                                                                                                                                                                                                                         |
| 31  | 25   | `docs/superpowers/plans/2026-09-13-plan-w-quality-tail-deferred.md` rows 18–19                                                             | Still read "fixed — 24d, `chore: version currency + CI hardening` (SHA not self-referenceable in one commit)"; `d402858` has landed, so both rows can carry the SHA like every other fixed row.                                                                                                                                                                                                                                               | fixed — `docs: deferred register closed at 43 rows; results file carries the review verdict and what moved in the spec` (both rows carry `d402858`)                                                                                                                                                                                  |
| 32  | 25   | `docs/superpowers/specs/2026-09-13-plan-w-quality-tail-results.md:25`                                                                      | "0.41 mutants/s (`clock.py`, 7 mutants, 50 s warm)" reads as 0.14 to a reader who divides; 0.41 is mutmut's mutant-phase rate with ≈30 s of per-module fixed cost excluded — one clause.                                                                                                                                                                                                                                                      | fixed — `docs: deferred register closed at 43 rows; results file carries the review verdict and what moved in the spec` (the clause: mutmut's mutant-phase rate; the 50 s include ≈30 s of per-module fixed cost)                                                                                                                    |
| 33  | 25   | `docs/superpowers/specs/2026-09-13-plan-w-quality-tail-results.md`                                                                         | The exemplar has a "What moved in the spec because of this run" section; the §10.5 closing sentence is mentioned only inside the Task 25 line of "What landed".                                                                                                                                                                                                                                                                               | fixed — `docs: deferred register closed at 43 rows; results file carries the review verdict and what moved in the spec` (the section sits between Review and What remains, as in the exemplar)                                                                                                                                       |
| 34  | 25   | `tests/conftest.py:126-129`                                                                                                                | `_per_test_home` does not `delenv` `GIT_CONFIG_GLOBAL`; on a machine exporting it git bypasses the temp `.gitconfig` and the pin fails loudly — a one-line `monkeypatch.delenv("GIT_CONFIG_GLOBAL", raising=False)` makes the isolation hold instead of merely detect.                                                                                                                                                                        | fixed — 08cd9e6 (`_make_home` owns the family — GIT_CONFIG_GLOBAL / _SYSTEM / _NOSYSTEM, the GIT_CONFIG_COUNT triples, GIT_AUTHOR_\* / GIT_COMMITTER_\*, GIT_TEMPLATE_DIR; `_per_test_home` removes it per test and `_home_env` builds the session templates' environment without it; pinned with a commit under exported overrides) |
| 35  | 25   | `tests/test_scaffold.py:591`                                                                                                               | The test name says `as_utf8` but the ASCII fixture deliberately leaves `encoding="UTF-8"` / `encoding=None` accepted; the docstring explains the kill, the name overclaims the codec.                                                                                                                                                                                                                                                         | fixed — 741516c (renamed `test_installed_plugins_reads_the_registry_under_home_or_answers_empty`; the docstring says the codec is not pinned and why)                                                                                                                                                                                |
| 36  | 25   | `scripts/mutation_gate.py` `count_mutants`                                                                                                 | A libcst `ParserSyntaxError` or a mutmut `PragmaParseError` over a changed module tracebacked past `main()`'s single `[gate] ABORT:` line (no `try` around the generator; `main()` catches only `GateAbortError`).                                                                                                                                                                                                                            | fixed — e617db1 (`ModuleParseError`, naming the module, the parser's class and its message; pinned over a syntax error and an unmatched `# pragma: no mutate end`, and through `main()`)                                                                                                                                             |
| 37  | 25   | `scripts/mutation_gate.py` `_over_budget`                                                                                                  | The under-budget info line (`[gate] N mutants across M changed modules, within the CI budget of B`) is new normal-path output.                                                                                                                                                                                                                                                                                                                | declined — keep: the line records the count the budget was compared against; on a passing run it is the only place that basis is visible                                                                                                                                                                                             |
| 38  | 25   | `scripts/mutation_gate.py` `_over_budget`                                                                                                  | "1 changed modules" — the count was never pluralised.                                                                                                                                                                                                                                                                                                                                                                                         | fixed — e617db1 (`1 changed module` / `2 changed modules` on both budget lines; the pins spell the noun)                                                                                                                                                                                                                             |
| 39  | 25   | `.github/workflows/quality.yml` gate step                                                                                                  | The 0.6 mutants/s basis of `--max-mutants 9000` was provisional (assumed, not measured).                                                                                                                                                                                                                                                                                                                                                      | declined — moot: measured 0.61 mutants/s at two children on run 34827110718 (results file, "The rate and the arithmetic"); the budget stands on the measurement                                                                                                                                                                      |
| 40  | 25   | Phase 2 task report §E item 2                                                                                                              | The report called the `finding_id` keys class (c) survivors; they were killed (the baseline carries no `inbox.py::func/finding_id` key). The report is ephemeral, so the corrected statement had no durable home.                                                                                                                                                                                                                             | fixed — `docs: deferred register closed at 43 rows; results file carries the review verdict and what moved in the spec` (one clause in the results file's triage section)                                                                                                                                                            |
| 41  | 25   | `docs/superpowers/plans/2026-09-07-ingest-redesign-b-compile.md`                                                                           | Part B's plan still instructed deleting the `.py.manifest.json` sidecars Task 25 removed (an implementer concern outside this plan's files).                                                                                                                                                                                                                                                                                                  | fixed — 2c64bb7 (plan owner, on main: the sidecar bullet became "deleting a module or function prunes its `mutation-baseline.txt` rows in the same commit"; the pycache bullet, "every mutation run goes through `scripts/mutation_gate.py`")                                                                                        |
| 42  | 26   | `scripts/mutation_gate.py` header, per-child cap paragraph                                                                                 | `RLIMIT_AS` is enforced on Linux (WSL2 and the runner, measured); whether macOS enforces it is unverified, so a developer there may run uncapped without a warning.                                                                                                                                                                                                                                                                           | declined — a macOS developer measures it before a sentence is written; environment facts carry a method and a date                                                                                                                                                                                                                   |
| 43  | 26   | `scripts/mutation_gate.py` `--max-mutants`                                                                                                 | The mutant-count budget can still degrade into a job timeout: 9,000 mutants of slow-covering modules exceed 300 min (per-mutant cost spans 0.41–10.9/s across modules locally), and a job timeout reads as a failure, not "not measured".                                                                                                                                                                                                     | declined for this plan — a time budget from mutmut's per-mutant `estimated_time_of_tests` is a later task, recorded in the results file's "What remains"                                                                                                                                                                             |
| 44  | 26   | `scripts/mutation_gate.py` `changed_modules`, `_isolate_git`                                                                               | A missing `git` binary raises `FileNotFoundError` from `subprocess.run` — not the `CalledProcessError` the abort line now catches — so that one case still tracebacks (exit 1 either way).                                                                                                                                                                                                                                                    | declined — without `git` nothing in the gate can run and the traceback names the missing binary; one `except OSError` closes it whenever the header's "the gate's own line" is wanted unconditional                                                                                                                                  |
| 45  | 26   | `docs/superpowers/plans/2026-09-07-ingest-redesign-b-compile.md:915`                                                                       | Still shows `mutation_gate.py --lcov lcov.info --max-workers 1`, flags the mutmut gate no longer has (`--max-children`, no LCOV).                                                                                                                                                                                                                                                                                                             | fixed — d8c0755 (plan owner, on main: Part B's Task 6 Step 1 prints the mutmut gate's invocation)                                                                                                                                                                                                                                    |
| 46  | 26   | `tests/test_config_validity.py` `test_offline_tests_cannot_resolve_a_name_outside_loopback`                                                | The `::1` pass-through assertion resolves through the real `getaddrinfo`; a runner without IPv6 loopback would fail that one line (unverified — the push-to-main run is the first observation).                                                                                                                                                                                                                                               | declined until observed — the merge's push run executes the suite; if the line fails there, drop the `::1` assertion (the block itself does not depend on it)                                                                                                                                                                        |
