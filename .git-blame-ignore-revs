# Commits ignored by git blame (canonical-form churn only — never content).
# Activate locally: git config blame.ignoreRevsFile .git-blame-ignore-revs
# (GitHub's blame view honours this file automatically; local checkouts must run
# the command once. Enrollment is a habit, not a mechanism — append on every
# form-only churn commit; audits catch strays.)

# mdformat+ruff canonical-form churn. NOT wholly form-only: a `|` inside a code
# span broke a table parse and silently dropped three spec rows — restored by
# 2330b70. Blame skips the form churn; anyone chasing the rows follows 2330b70.
8e3c25078b4844ee81c5b00c79293ac6521b7f7e

# canonicalize markdown added after the first churn; content-preserving
# (verified by the table-truncation guard).
4be802c506cb76377f4a7c28a831a27ef2380b80

# canonicalize the two registers after row edits (alignment only, 18/18 symmetric)
634e0fe62d45ce678379103cd6221d0988a2e8ba

# ruff format the --out-dir tests ("no behaviour change")
36d6791a592b09c504a0a1ead2da2cffe2118a51
