# Ingest redesign: design

Status: draft for author review (2026-09-04). Nothing here is decided. Four labels are used: **chosen** (the author picked it in the 2026-09-04 brainstorm, and it can be re-picked), **measured** (probed live on the date given), **proposed** (the design's suggestion), and **open** (not yet answered). A label is written where a reader could not otherwise tell which one applies. Unlabelled prose is proposed. A fact read from source or from documentation rather than probed says which. This document supersedes foundation spec §4 (Zotero bridge).

**What binds this spec.** Very little, and deliberately. `CONTEXT.md` and the ADR register are non-binding: the author set them aside because assumptions under them were disproved. The existing code and tests in `research_vault/` are not binding either, and for the same reason. They were written under those same assumptions, so their existence is not an argument for their survival, and the adversarial review that revised this document found working code implementing a contract §6 retires. "We already built it" is not a reason to keep something, and under the priority order in §0 it is not a reason to prefer building over adopting.

Two things do bind. The **measured environment**: what Zotero, Better BibTeX, and the Claude Code plugin loader actually do, established by live read-only probe or by reading their source, with the date and the method recorded in §9. And the **author's choices**, listed in §0, which are re-pickable but are not this document's to overturn.

Existing code enters here in two other roles, neither of them authority. As **cost**, because naming what a change would touch prices the proposal, which is why §6 counts dependents. And as **evidence**, because code records what was tried, and a mechanism that failed in practice is worth knowing about. Where this spec contradicts working code, it names the code and says what changes.

Consumers: the implementation plan for this iteration; the later specs for search (PRISMA-S), scoping review (PRISMA-ScR), and the workflow-component audit.

## 0. Purpose and boundary

Ingest is the process that gets a source from outside the vault into the vault. It is the first of three garbage-in gates (setup, lint, ingest). This spec covers the whole ingest path and the setup and lint machinery ingest depends on. It follows one priority order throughout: adopt an existing component as-is, else adapt one, else build. Capture and its linter are mechanical and built here; the compile step is adopted.

Chosen scope (2026-09-04):

- Source classes: scholarly and grey-literature documents, meaning anything Zotero can hold as an item with a file or DOI (papers, preprints, standards, reports, books, theses). Web pages, repositories, and product documentation are deferred.
- Post-capture events covered: identity changes, removal, and content drift. Substrate absence (Zotero not running, a clone with no Zotero, CI with no Zotero) is deferred, so a live Zotero at every mechanical check is assumed.
- Trigger: capture on demand; a drift check at every mechanical chokepoint. Nothing writes the vault unattended.
- Greenfield: no existing vault content must survive.
- Derived full text lives in the vault as a gitignored cache, regenerable from Zotero.
- Screening decisions belong to a review, not to the source. The hand-edited note status is retired.

Terms in this document are the terms proposed in §1.1. Each is traced to `docs/research/2026-09-04-import-terminology.md`, either to a primary source or, where the note records that no field names the concept, to that gap: **ingest** (the whole process), **selection** (the human decision), **add** (creating the Zotero item), **capture** (the deterministic copy into the vault), **compile** (the LLM step), **drift** and **refresh** (change detection and re-copy), **item key** and **citation key** (identity and name). The brainstorm's working labels were import, admission, capture, and digest; §1.1 records why each stays or goes.

## 1. Source lifecycle

The design object is the source's relationship with the vault over time, not the files. States, the event that moves a source between them, and the owner of each move (proposed):

| State                          | Meaning                                                                                                                                                                                                 | Entered by                                                                    | Owner                                                                                  |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| identified, screened, selected | The review found the source, screened it, and chose it for retrieval. Lives in the review's selection log.                                                                                              | Search and screening                                                          | The scoping review workflow, specified separately                                      |
| added                          | A Zotero item exists. Zotero assigned its item key; Better BibTeX assigned its citation key.                                                                                                            | A person adds the item, or the agent adds it on the person's instruction (§3) | Person, with the agent as operator                                                     |
| captured                       | The literature note exists with a metadata snapshot and a provenance tuple. Identity is fixed here, once.                                                                                               | The capture verb (§2)                                                         | Capture verb                                                                           |
| compiled                       | The source page and concept-page updates exist; the compile input hash is recorded.                                                                                                                     | The adopted wiki (§4)                                                         | Adopted wiki                                                                           |
| current                        | Live item version equals the recorded version.                                                                                                                                                          | The linter finds no change                                                    | Linter                                                                                 |
| drifted                        | Any object the note depends on carries a version other than the recorded one: the item, an attachment, or an annotation. Each versions independently (§9), so the item's own version is not the signal. | Linter                                                                        | Linter files a finding; the capture verb refreshes on demand                           |
| re-keyed                       | Live citation key differs from the recorded one.                                                                                                                                                        | Linter                                                                        | Linter records the old-to-new mapping; propagation rewrites every citation-key surface |
| merged                         | The item key is gone and another item's `dc:replaces` names it.                                                                                                                                         | Linter                                                                        | Linter records the successor; the note points forward                                  |
| trashed                        | The item key is in Zotero's trash.                                                                                                                                                                      | Linter                                                                        | Note kept; finding filed                                                               |
| deleted                        | The item key is absent from the library and the trash.                                                                                                                                                  | Linter                                                                        | Note kept, state recorded; finding filed                                               |
| retracted or corrected         | A registry notice names the source.                                                                                                                                                                     | Existing update-notice check                                                  | Standing recorded on the note                                                          |
| database changed               | The Zotero server id differs from the recorded one; Zotero's documentation: a different id means a different database.                                                                                  | Linter                                                                        | Linter stops and reports; every recorded version is void                               |

Invariants (proposed):

1. Identity is assigned exactly once, at capture, and never reused.
2. Every provenance tuple carries the Zotero server id. Versions and keys are meaningful only within one server id.
3. No transition deletes a note. Records deprecate; they do not vanish.
4. Every transition is visible: a finding in the review queue or a line in the log.
5. The linter runs at capture, at verify, and at pre-commit, through one code path.
6. A mechanical step without a named linter is incomplete.

Cost, measured 2026-09-05: `GET /api/users/0/items?since=0&format=versions` returns all 2,846 object versions in one response (54 KB, 0.045 s), children and annotations included; `GET /api/users/0/items/trash?format=versions` returns the 267 trashed keys in one response. `/items/top` returns only 1,459 and would hide every child, so the linter reads `/items`. A whole-vault classification is those two calls and no others: every version the linter compares is already in the first response. Fetches are needed only to refresh a note that classified as drifted.

### 1.1 Glossary (proposed)

The glossary in `CONTEXT.md` is rewritten around eight concepts (proposed). Each term is traced to `docs/research/2026-09-04-import-terminology.md`: to the primary source it was read from, or, where the note records that no field names the concept, to that gap. Where two fields use one word for different things, the collision is named so the glossary can say which sense applies.

| Concept                                                                                                | Proposed term                                                                                                                                                           | Field and primary source                                                                                                                                                                                                                                                                                                                                                                   | Working label, and why it goes                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The whole process from outside the vault into the vault                                                | **ingest**                                                                                                                                                              | LLM wiki (Karpathy's gist: "Ingest. You drop a new source into the raw collection and tell the LLM to process it"); digital preservation (OAIS Ingest functional entity)                                                                                                                                                                                                                   | import: Zotero uses it for bringing a bibliographic file into Zotero, which is a step inside ingest, and the connector endpoint is named `/connector/import`                                                                                                                                                                                                                          |
| The human decision that a source is accepted                                                           | **selection**, with the outcomes **included** and **excluded**                                                                                                          | Evidence synthesis (JBI: "selection (also known as screening) of evidence sources"; PRISMA-ScR item 9; PRISMA 2020 item 16b)                                                                                                                                                                                                                                                               | admission: found only in the llmwikis handbook, for agent-memory records; no review guideline uses it                                                                                                                                                                                                                                                                                 |
| Creating the library record in Zotero                                                                  | **add** (to Zotero); **import** when Zotero parses a bibliographic file                                                                                                 | Reference managers (Zotero: "Adding Items to Zotero"; the connector: "Clicking the save button will create an item in Zotero")                                                                                                                                                                                                                                                             | admission (the act): same as above; archival "accession" is the field term but no Zotero user says it                                                                                                                                                                                                                                                                                 |
| The deterministic copy of metadata, attachments, annotations, and text into the vault, with provenance | **capture**                                                                                                                                                             | LLM wiki (hermes llm-wiki: "Capture the raw source"), the only source the terminology note records using the word in this sense                                                                                                                                                                                                                                                            | keeps its label; collisions: PREMIS "capture" is the acquisition pull that precedes ingest ("Note that the Capture process precedes the Ingest process"), so it names the producer side of the boundary, nearer §3's add than §2's copy; SAA "capture" is making surrogate copies such as scans; change data capture in data engineering means capturing changes, which is drift here |
| The LLM step that turns a captured source into wiki pages                                              | **compile**, producing a **source page** and updated **concept pages**                                                                                                  | LLM wiki (nvk/llm-wiki: "Transform raw sources into wiki articles"; llmwikis handbook: "the agent turns it into a source summary, updated concept pages")                                                                                                                                                                                                                                  | digest: nvk uses it for a memory record, no other field for this step                                                                                                                                                                                                                                                                                                                 |
| Detecting that the Zotero item changed, vanished, or was re-keyed, and re-copying                      | **drift** (the detection) and **refresh** (the re-copy)                                                                                                                 | No field names this concept (terminology note, Gaps K6). **drift** and **refresh** are borrowed from LLM wiki (hermes: "flag drift when it has changed"; nvk: "refresh: Freshness check ... re-fetches ... detects changes"), where both name upstream-URL freshness rather than a library record. Zotero's own mechanism words stay verbatim: version, `Last-Modified-Version`, `?since=` | sync: Zotero's sync is client to zotero.org; using it here would name the wrong thing                                                                                                                                                                                                                                                                                                 |
| The stable identifier across a source's life, and its name                                             | **item key** (identity) and **citation key** (name)                                                                                                                     | Reference managers (Zotero API: `<itemKey>`; Zotero schema field "Citation Key"; Better BibTeX: "the citation key is the piece of data that connects your bibliography")                                                                                                                                                                                                                   | citekey: a Better BibTeX synonym, not the schema name. BBT's own pages use it ("Configurable citekey generator", "Generating citekeys for your items"). The Zotero schema field is `citationKey` and its label is "Citation Key", so the vault standardises on the schema spelling                                                                                                    |
| A source's standing after it was added                                                                 | **trashed**, **deleted**, **merged** (Zotero's words); **retracted**, **corrected** (Retraction Watch, Cochrane); **superseded** (nvk status; EndNote keeps the record) | Reference managers and evidence synthesis                                                                                                                                                                                                                                                                                                                                                  | screening state on the note: excluded and included belong to the review's selection, not to the source                                                                                                                                                                                                                                                                                |

Kept as they are, with their field: **item** (Zotero's record), **record** (PRISMA 2020: a title or abstract indexed in a database), **source of evidence** (PRISMA-ScR), **literature note** (Obsidian community: ZotLit, Obsidian Zotero Integration), **fixity** (OAIS, PREMIS).

Entries retired: Admission, Import as currently defined, Screening state, Bibliography export, Citable as defined through the note status, and the citekey spelling.

## 2. Capture

### 2.1 Identity and name (chosen: bundle 1)

The vault's identity for a source is the Zotero item key qualified by the Zotero server id. Item keys are assigned by Zotero, are not user-editable, are unique within a library, and survive metadata edits and citation-key changes. The citation key is the source's name: it names the file and appears in prose citations and the CSL file. A citation-key change is a rename of a thing whose identity did not change, so propagation is mechanical.

This answers the two objections to ADR 0004: a third party cannot change the identity, and uniqueness is Zotero's guarantee within one server id. A freed suffix (`chen2025a` reused for a different source) is detected because the item key differs from the one recorded under that name.

Filename (chosen): `literatures/<citation key>.md`. The title is carried in frontmatter and as the H1, and in `aliases`, so Obsidian resolves `[[Title]]` as well as `[[citation key]]`. The file renames only on a re-key.

### 2.2 Record home (chosen: both, one writer)

Two files carry bibliographic data, and the capture verb is the sole writer of both.

**The literature note's frontmatter** carries:

- a metadata snapshot: a fixed subset of the Zotero item's data fields, copied verbatim under Zotero's own field names (`itemType`, `title`, `creators`, `date`, `DOI`, `url`, `publicationTitle`, `volume`, `issue`, `pages`, `publisher`, `ISBN`, `language`, `abstractNote`), so no field is renamed and no mapping is ours;
- a provenance tuple: `zotero-server-id`, `zotero-item-key`, `zotero-item-version`, `citationKey`, `attachments` (a list of `{key, version, md5, contentType, filename}`), `annotations` (a list of `{key, version}`), `fulltext` (a list of `{attachment-key, version, sha256}` over the cached text), the compile input's `sha256`, and `generated: {by, at}` (OKF §5.2 shape, already adopted). Every object the note depends on contributes its own key and version, because Zotero versions objects independently: an attachment or annotation change never moves the item's version (§9). A single `annotations-version` would name nothing Zotero serves;
- `title` and `aliases` for Obsidian; `type: literature` for OKF.

Frontmatter is machine-owned above the managed region marker; the free region below the managed close marker is never touched by capture. The current `%%rv-managed%%` markers stay.

**The CSL JSON file** (`system/bibliography.json` today; name follows the glossary) holds one entry per captured item, rendered by Better BibTeX's `item.export` with the Better CSL JSON translator, sorted by citation key. It exists for pandoc. Its scope is the captured set, not the library. It is regenerated whole by the capture verb; no auto-export, no observation, no byte-compare against a third-party writer.

The capture verb writes both from the same Zotero read, so they cannot disagree with each other. The linter diffs both against live Zotero.

### 2.3 The capture verb

One verb, per item or batch, keyed by citation key or item key. For each item it:

1. reads the item JSON from the local API (`/api/users/0/items/<key>?format=json`);
2. reads the children (`/items/<key>/children?includeTrashed=1`): attachments with `md5`, `mtime`, `contentType`, `filename`, and the file URL from `/items/<attachment>/file/view/url`; notes. **Annotations are not children in the API's sense and this route does not return them** (measured, §9): an annotation's `parentItem` is the attachment, but `/children` on the attachment returns nothing. They are read either from `/items?itemType=annotation` filtered on the attachment key, or from Better BibTeX's `item.attachments`, which returns them nested per attachment with key, type, page label and text. The plan picks one and records why; `md5` exists only for stored-file attachments, so a linked attachment carries no fixity and the tuple says so rather than omitting the field;
3. reads the attachment's indexed full text (`/items/<attachment>/fulltext`) into the gitignored cache. PDFs report `indexedPages` and `totalPages`; other content types report `indexedChars` and `totalChars`; the cache entry records whichever pair it received. A 404 means Zotero has no indexed text. **A 200 is not by itself a complete text**: Zotero truncates PDF indexing at `extensions.zotero.fulltext.pdfMaxPages`, default 100 in the installed 10.0.1 (source read), and reports the truncation as success, so `indexedPages < totalPages` is a partial index and the cache entry is marked partial. Capture never treats a partial or absent index as full text;
4. asks Better BibTeX for the item's CSL entry (`item.export([citation key], "Better CSL JSON")`). This one call is keyed by the name rather than the identity, because that is the only key the method takes; the citation key used is the one just read from the item JSON in step 1, so it cannot be a stale one. The method takes a list and fails the whole batch on one unknown key, so a batch capture calls it per item, or chunks and retries the failing chunk singly, and reports which key was rejected;
5. renders the note: frontmatter, managed region (title, creators, venue, identifiers, attachments, annotations as a list with page labels and verbatim highlight text), and the preserved free region;
6. writes the CSL file entry and the cache;
7. compares the rendered projection with what is on disk and reports NOOP when nothing changed.

Steps 1 to 4 are four reads through two subsystems, not one atomic read, so an edit made between them would be captured half-old. Capture therefore re-reads the item's version after step 4 and, where it moved, starts the item again. The tuple records the version that was current across the whole read.

Every mechanical outcome is four-state. UNREACHABLE (Zotero not answering) writes nothing and files an outage finding. A malformed response writes nothing.

### 2.4 The lifecycle linter (proposed)

One check, one code path, run at capture (before rendering), at verify, and at pre-commit. It:

1. reads every literature note's provenance tuple;
2. sends the recorded server id as the `Zotero-Server-ID` request header on every call, which Zotero answers with `412 Precondition Failed` when it is talking to a different database. The linter does not compare the value itself; it reports database-changed on the 412 and stops. Zotero's documentation directs clients to partition cached versions by that id, which is what the tuple does;
3. fetches the whole key set with `?since=0&format=versions` and the trashed set with `items/trash?format=versions`. The full set is required, not `?since=<lowest recorded version>`: a `since` window returns only items that changed, so a key that vanished appears in neither the window nor the trash and would read as current. One response carried 2,846 versions on 2026-09-04, so the full set costs nothing;
4. classifies every note by comparing **every** key in its tuple, the item and each attachment and each annotation, against the versions map and the trash set: current (all match and none trashed), drifted (any version differs), re-keyed (live `citationKey` differs), merged (item key absent and a live item's `dc:replaces` names it), trashed (any key in the trash set), deleted (item key in neither set). The comparison is per object because Zotero versions objects independently; classifying on the item's version alone would report a swapped PDF or a new annotation as current;
5. on a drifted note, names the cause from which key moved, and where an attachment moved, compares its `md5` and the cached text's sha256 to say whether the file changed or only its metadata did;
6. files one finding per non-current note with the transition's reason code, and blocks nothing except database-changed.

This costs a running Zotero at every commit, which follows from the scope in §0: substrate absence is deferred. Until it is taken up, a commit made with Zotero closed reports the linter UNREACHABLE, and the pre-commit hook treats an outage as it treats any other: it files the finding and does not block. The plan states that behaviour as a test, so an outage never silently reads as a clean classification.

Reason codes, proposed: `drift`, `re-keyed`, `merged`, `trashed`, `deleted`, `database-changed`, `outage`. `drift` already exists in the registry. `superseded-note` and `not-admitted` are retired with the note status.

### 2.5 Re-key propagation (proposed)

A re-key is detected by the linter alone: the item's live `citationKey` differs from the recorded one under an unchanged item key. Better BibTeX's `item.regenerate_key` is a **mutating** call that assigns new keys, so it is never a detection source; if it is ever used deliberately, it is the person's act and its returned old-to-new mapping feeds the same propagation pass. Propagation is one mechanical pass driven by the recorded mapping: rename the file, rewrite the CSL entry id, rewrite `[@old]` citations, rewrite `[[old]]` wikilinks, rewrite the frontmatter `citationKey`. The mapping is appended to a rename log so a later reader can follow any key backwards. The identity never changes, which is what makes the pass safe to repeat.

Propagation is a mechanical step and so carries its own linter, per invariant 6: after a pass, no citation-key-bearing surface may still name a key the rename log maps away, and no note's filename may differ from its recorded `citationKey`. That check runs in the same code path as the lifecycle linter and files a finding rather than repairing silently.

### 2.6 Paths and the text cache (proposed)

No absolute paths in the repository. The file URL from Zotero is a Windows `file://` path, percent-encoded; the existing `wslpath` shim resolves it at use time. The text cache lives beside the notes under a gitignored folder, one file per attachment, named by attachment key, with its sha256 recorded in the note. The cache is regenerable from Zotero and never committed.

Zotero's index is the source of truth for extracted text. The cache is a copy of it, and the compile input is a copy of the cache; both are derived and both are regenerable, so a disagreement is always resolved by re-reading Zotero, never by preferring a local copy. The note's recorded sha256 is what makes a disagreement visible.

### 2.7 API use (measured 2026-09-04)

- Zotero 10.0.1 local API, version 3, serves item JSON with `key`, `version`, `citationKey`, `dateModified`, `relations`; children with `md5` and `mtime`; `/file/view/url`; per-attachment `fulltext`; `fulltext?since=`; `?since=&format=versions`; `items/trash`; a `Zotero-Server-ID` header on every response. It has no `/deleted` endpoint.
- Every translator-based format on the local API (`format=csljson`, `format=bibtex`, `include=csljson`) returns HTTP 500 on 10.0.1. Better BibTeX 9.0.63 `item.export` is the working CSL renderer.
- Better BibTeX JSON-RPC is used for exactly two things: CSL rendering and re-key mappings. Its auto-export is not used.
- The Zotero web API is not used. The bib-file export is not used.

## 3. Selection and adding to Zotero

Selection is the human act, recorded where it is made: the review's selection log, or an ad hoc request. The keystrokes that add the item are not the decision, so the agent may perform them.

Zotero 10 offers two write paths, and the plan picks between them with one attended trial rather than this document asserting the answer.

**Path A, the documented local API write** (`POST /api/local/authorize` for a key, then ordinary write requests). It is authorized, per-application, and revocable; the person sees a dialog naming the application and chooses Allow, Always Allow, or Deny. Its cost is that the caller supplies Zotero item JSON, so the vault would own a metadata mapping it otherwise never touches. That cost disappears where the record already exists in Zotero's own shape.

**Path B, the connector server**, which is what a browser extension uses (source read 2026-09-04 from `server_connector.js` at zotero/zotero main; treat this as source-read, not as a measurement on this machine):

- `POST /connector/import?session=<id>` with an RIS, BibTeX, or CSL JSON body runs Zotero's own import translator, saves the items into the currently selected library or collection, and returns HTTP 201 with the created items as JSON, including their keys.
- For a DOI, the body comes from content negotiation on doi.org (`Accept: application/x-research-info-systems` or `application/x-bibtex`). For an identifier-less document, the agent composes a minimal RIS from the metadata it holds. In both cases Zotero parses the record and the vault never builds Zotero item JSON, which is the whole attraction of this path.
- Its costs are real and were found by review, not assumed. **No consent dialog guards it**, so anything running on the machine can write to the library; that is a fact about Zotero, not a property this design adds, but it means Path B gives up the audit trail Path A's authorization provides. A session id is minted by the import call and cannot be reused, so attaching a file afterwards is constrained by that session's lifetime and by the numeric item id the attachment endpoint expects, which is not the item key the rest of this design uses. And the save target is whatever collection is selected in the Zotero window.

**Proposed**: Path A for creating the record, because the authorization dialog is the audit trail this vault wants and the item JSON for a fresh record is small. Path B stays available for the case where a translator does the work no mapping of ours could. The plan settles it with one attended trial of each, and records which endpoints each needs.

Capture runs on the returned keys at once, so adding and capture are one conversation.

Record creation by the person (browser connector, drag-in, add by identifier) stays valid and reaches capture the same way.

No citation-key pinning. Under item-key identity a later key change is a rename, not a lost address.

Open, to be settled in the plan with one attended write test on a scratch collection: where the new item lands. Under Path A the caller names the collection. Under Path B the target is whatever is selected in the Zotero window, and `/connector/getSelectedCollection` reports a numeric collection id that the local API keys by string, so reading the target back is not a one-line check.

## 4. Compile

Chosen outputs (2026-09-04): a source page and cross-source concept pages, both in the adopted wiki's own shape. Claims with locators are dropped as a vault syntax; the requirement that a quoted passage be traceable to a page and byte-checkable on demand is carried by Zotero annotations captured at ingest. Charting belongs to the scoping review and is not a compile output, so the earlier requirement D2 (a caller-supplied per-source template) leaves this set and moves to the scoping review spec.

### 4.1 Requirement set the adoption was screened against

Floors: D1 a per-source page; D3 concept pages with an index, an append-only log, and contradictions kept rather than resolved; D4 the capture of the source file is separable from compiling it, so the tool accepts a raw file another process wrote; D6 installable as a Claude Code plugin or skill as-is; D10 a licence that permits use and modification. Not floors: D5 idempotent re-compile; D7 works on an existing layout or states its own; D8 references a source by a caller-supplied stable id; D9 documents its human gate.

The evidence is in `docs/research/2026-09-04-import-sourcing.md`: 43 candidates read after a triage of 295 discoveries, with every drop counted by reason. The candidate descriptions below are drawn from that note's records. Where its adversarial verify pass had not confirmed a row when this section was written, the note marks it, not this document.

### 4.2 Candidates that meet every floor as-is

Two Claude Code plugins meet D1, D3, D4, D6, and D10 without modification. Both are MIT, both were pushed in the last ten days of August 2026, both are Python underneath.

**AgriciDaniel/claude-obsidian** (v2.1.1, pin `ad67087`, 14,631 stars, 42 commits in 90 days). Integration is a transaction: read-only workers propose a bundle of page writes with expected content hashes, the person approves the plan by its hash, and an engine applies it atomically or not at all. Source pages carry key claims; a claims ledger marks contested claims and refuses contradictory evidence without an adjudication note. Sources enter through a configured inbox directory, and paths outside the vault are refused. Layout: a `wiki/` directory beside an `.obsidian/` directory. The ledger key is a content hash, so the citation key rides in the source locator and the page properties, not in the ledger id. Components at the pin: 15 skills, 3 agents, a SessionStart and a Stop hook, and the `claude_obsidian/` Python engine the skills call. `plugin.json` declares no components, so component discovery is by directory.

**nvk/llm-wiki** (v0.24.4, pin `7c94c9b`, 1,034 stars). Ingest and compile are separate commands: ingest writes an immutable raw page with a fixed six-key frontmatter, compile turns uncompiled raw pages into concept, topic, and reference pages, with a nudge at five uncompiled sources. A raw page written by another process is accepted if it carries those keys, which is the capture-then-compile split of §1 exactly. Layout: `raw/<type>/` and `wiki/`, imposed by a lint that relocates files by their frontmatter. Components: 1 skill (403 lines plus 22 reference files), 27 commands, no hooks.

Candidates that fell on a floor, for the record: ussumant/llm-wiki-compiler (Claude and Codex plugin, no per-source page); kfchou/wiki-skills (contradictions must be resolved before commit, the opposite of D3); pumblus/okf-harness (OKF-native, Apache-2.0, no append-only log by design); the hermes-agent llm-wiki skill (a Hermes skill, not a Claude Code one; its schema template is the strongest pattern donor); atomicstrata/llm-wiki-compiler and SwarmVault (Node CLIs, not plugins); llmwikis.org (prompts and a starter bundle, scripts without a licence).

### 4.3 Proposed adoption: claude-obsidian, generic mode, our names under `wiki/`

**Chosen 2026-09-05.** The compile step adopts AgriciDaniel/claude-obsidian at pin `ad67087`, installed whole, configured through its own mode system, with no edit to the tool. Every accommodation is on this vault's side, which is where nothing binds.

The route the earlier draft proposed, a curated marketplace entry taking four skills and leaving eleven, is **unreachable** and is recorded here so it is not proposed again. Measured against the installed Claude Code 2.1.260: the plugin ships a `plugin.json` at its repo root, so a marketplace entry that also lists components is rejected outright, with the loader emitting "has conflicting manifests: both plugin.json and marketplace entry specify components. Set strict: true in marketplace entry or remove component specs from one location." At `strict: true` the list supplements directory discovery rather than replacing it, which the official reference states as a rule ("Custom paths supplement default directories - they don't replace them"), so all fifteen skills, three agents and both hooks load anyway. A `git-subdir` source pointed at `skills/` would curate, but it severs the `claude_obsidian/` Python engine every one of those skills invokes by path. So the plugin arrives whole or not at all, and §4.2's component inventory is what a session carries.

**Modes.** The tool ships four filing modes, and their folder names are configuration: `generic` (`sources_folder`, `entities_folder`, `concepts_folder`, `sessions_folder`), `lyt`, `para`, and `zettelkasten`. All four validate through `mode_config.validate_wiki_folder`, which requires the first path segment to be `wiki`, so no mode escapes that parent. `generic` is the structural match: its sources-to-concepts split is this vault's per-source-to-cross-source split. Configured names, confirmed by a round trip through `mode get`:

| Layer                           | Path                               | Written by                         |
| ------------------------------- | ---------------------------------- | ---------------------------------- |
| Captured record, one per source | `literatures/<citation key>.md`    | The capture verb, and nothing else |
| Per-source page                 | `wiki/sources/<title>.md`          | The tool                           |
| Cross-source pages              | `wiki/synthesis/<slug>.md`         | The tool                           |
| Entity and session pages        | `wiki/entities/`, `wiki/sessions/` | The tool                           |

`literatures/` stays outside `wiki/`, where the tool provably cannot write: its lint performs no filesystem writes at all, verified byte-identical before and after with `sha256sum`, and its ingest operation is scope-refused from any path outside `wiki/` and `.raw/`. The vault gives up the `wiki/` parent segment and keeps every name inside it. That is the whole accommodation on the layout axis.

`para` mode names the same four things the vault's `projects/` layer does. Whether one tool can carry both layers is left to the workflow-component audit; this spec configures `generic` only.

**What the tool cannot read, and why that is fine.** Of its seven capture adapters exactly one is implemented, and it accepts markdown, text, JSON, CSV, YAML and HTML. PDF, EPUB and images are `metadata-only`, which its own manifest defines as identifying and describing the input without extracting its contents; reading them needs an external runner the repository does not ship. Two consequences, both binding on the seam in §4.4: the compile input this vault writes is **markdown text, never a path to an attachment**, and the tool's own `capture` command is not used at all, because its one working input is the file capture already produces. Zotero's index does the extraction, which is work the tool would otherwise not be able to do.

**What the vault changes, all of it in non-binding territory.** Three conflicts were measured, and each is resolved on our side rather than in the tool:

1. `wiki/index.md` carries six required frontmatter fields; the vault's `okf-structure` check requires a nested `index.md` to carry none. The vault stops applying that rule under `wiki/`. It came from an OKF reading in a non-binding ADR, and the alternative is forking the tool's lint.
2. The tool's `capture` treats a folder named `inbox` as its own drop zone and, run bare, planned to copy this vault's fleeting notes into its raw store. Since `capture` is unused (above), this is avoided by not running it; the plan also records the `--inbox` flag and the `.vault-meta/capture/config.json` key as the belt and braces.
3. `.raw/captured/*.md` copies carry no frontmatter and would fail `okf-frontmatter` wherever the vault's walker reaches them, which it does, since verify rglobs everything but `.git`. The vault gitignores `.raw/` and `.vault-meta/` and excludes them from the walk. Note the tool's `adopt` will not install its own gitignore rules over an existing file without `--force`, so this is the vault's edit to make.

**Tracers, before any compile task is written.** Each is one command on a scratch vault: that the plugin installs whole and the fifteen skills and two hooks are tolerable in a real session; that `mode set` writes the two folder names and `mode get` reads them back; that a compile run over three captured sources writes only under `wiki/` and leaves `literatures/` byte-identical; and that a page the tool writes passes `verify --surface commit` once the two exclusions above are in place.

**If a tracer fails.** The fallback is not nvk/llm-wiki, which the layout probe found worse on every axis: its interior layout is module constants with no configuration path, and one `--fix` pass moved this vault's root `index.md` into a quarantine folder and took the `okf-frontmatter` failure count from zero to thirteen. The fallback is to defer compile to its own spec and ship capture, the linter and setup alone, which is a complete iteration on its own.

### 4.4 The seam between capture and compile

- Capture is the only writer of the literature note and the text cache. It also writes the compile input: a **markdown** file in the tool's configured inbox directory, carrying the attachment's extracted text under a metadata header. Markdown is not a convenience here, it is the tool's only implemented input format (§4.3).
- Where Zotero has no indexed text, or only a partial index, capture writes no compile input and files a finding. Nothing is compiled from an abstract alone, and nothing is compiled from a page range that Zotero truncated.
- The compile input's filename must not equal `<citation key>.md`, and its frontmatter must carry no `title` or `aliases`. Both were measured to make every wikilink to that source ambiguous across two or three files, from the tool's index and from every page it writes, which would bury the real lint signal at one finding per source.
- The tool's `.raw/captured/` store holds a second, content-addressed copy of that text. It is gitignored with the cache and excluded from the vault's walk (§4.3).
- `wiki/synthesis/` is the cross-source layer. That is the same role `synthesis/` was always intended to serve, so the layer keeps its meaning and its name and moves one level down.
- **The per-source page lives in `wiki/sources/`, not in the literature note.** The vault would have preferred it in the note's free region, directly under the machine-owned managed region, keeping one file per source. Neither candidate can do that: the tool's engine has only whole-file create and replace, so applying one would rewrite the managed region along with the free region, which is the exact boundary the managed region exists to protect. Splicing it in outside the tool would cost the tool its wikilinks, its index entry and its ledger locator, and its next lint would report both as broken. Two files per source, in two trees, is the price of not editing the tool.
- Compile runs when the person asks, after capture, for one source or a batch. The tool's own gate is the human gate: inspect the proposed bundle, approve it by its plan hash, apply it atomically or not at all. No second gate is added.
- The lifecycle linter records the compile input's sha256 in the note. A refreshed note whose input hash changed is reported as needing recompile, which the tool then performs.
- Reading across the boundary already works: a page under `wiki/` carrying `[[<citation key>]]` resolves into `literatures/` with no dead link. Writing across it does not, in either direction, which is what keeps the two writers separate.

## 5. Setup and lint integrity (proposed)

Doctor is setup's linter. It probes Zotero version, Better BibTeX version, local API reachability, server id, translator-format health, fulltext index presence, and the path shim, and reports what it found.

The server id has exactly one authoritative home: the running Zotero, which returns it on every response. Doctor reports it and the note's tuple records the value that was live at capture, but nothing caches it as a third copy to be reconciled. The linter never compares two stored copies; it sends the recorded id as a request header and lets Zotero answer 412 (§2.4).

The plugin repository's `docs/environment.md` stays a developer document, and its version rows are the ones that went stale: the table recorded Zotero 9.0.6 and Better BibTeX 9.0.57 while 10.0.1 and 9.0.63 were running (measured 2026-09-04). The remedy is to delete the duplicated facts rather than to add a mechanism that watches them. Version rows a probe can answer come out of the table and are read from `probe` on demand; what stays is what no probe can produce, which is the reasoning and the dated findings.

Retired from setup: the whole-library auto-export step and its staleness probe. Kept: the human-only wizard steps for Zotero plugin installs.

Every mechanical step in this spec names its linter: setup has doctor; capture, the CSL file, and the text cache have the lifecycle linter through recorded hashes and versions; adding has capture, which runs immediately on the returned keys and reports what it could not read.

## 6. What changes in the existing machinery

Retired by this spec (proposed):

- the Better BibTeX auto-export contract: observation, byte-compare, the `staleness` verb, the `autoexport` doctor probe, and the `bibliography.py` machinery around them;
- the note-level screening status, `lint_screening_state`, and the `superseded-note` reason code;
- claim lines as an ingest output, and anchors derived from annotations.

Replaced rather than superseded: the ADR register is non-binding while it is re-derived, so this spec does not negotiate with ADR 0004 or 0005. It states the two positions directly, and whether either becomes a record again is the register's business: identity is the Zotero item key qualified by the server id, and the citation key is the name; capture is the sole writer of the vault's bibliographic record. The text cache is a new derived artifact outside the git boundary, the same class of thing as Zotero's own attachment store. Foundation spec §4 is superseded by this document. The glossary is rewritten per §1.1, as a fresh derivation rather than an amendment.

Kept: four-state results; findings and the review queue; deprecate-never-delete for vault records; the managed region above a free region; the path shim; `find-sources` until the search spec, which is why the `not-admitted` reason code stays in service; verify's DOI, metadata, and update-notice checks, reading the frontmatter snapshot.

Frozen, dispositioned in the workflow-component audit: the quote check, factcheck, stance links, the publish gate, project-flow, and archive-source.

**The synthesis layer keeps its folder and gains a writer.** `synthesis/` is the compiled concept-page layer (§4.4), so nothing about the vault's structure contract changes: the folder stays in `scaffold.VAULT_DIRS`, `structure.check_tree` keeps attesting it, `structure` keeps mapping it to the `synthesis` note type, the Bases view and the templates keep working. What changes is who writes it. Today `synthesis-conventions` tells an agent to compose pages by hand carrying `[[citation key#^claim-id]]` claim links; under this spec the adopted compile step maintains them and claim links are no longer the arrangement's currency. That skill is therefore rewritten, not frozen, and its 2+-source threshold and index-registration rules are checked against whatever the adopted tool enforces so the vault does not carry two conflicting sets of rules for one folder.

Stance links remain frozen pending the workflow-component audit. They live on synthesis pages, so the audit decides whether the compiled pages carry them or whether disputes are recorded the adopted tool's way.

The same reservation covers the evidence-layer check: it is frozen, not retired, and this spec does not remove the producer it reads without the audit saying so.

Rewritten skills: import-source and setup-vault. Others untouched until their own spec.

## 7. Testing (proposed)

What already exists (source read 2026-09-05): `tests/conftest.py` builds the `tmp_vault`, `fixture_vault`, and `net_vault` fixtures and gates the live suite in `pytest_collection_modifyitems`, which skips the `live` marker without `RV_LIVE=1` and the `live_net` marker without `RV_LIVE_NET=1`. Seven marked live tests already run under that gate, across `tests/test_cli_live.py`, `tests/test_zotero.py`, `tests/test_scaffold_live.py`, `tests/test_archive.py`, and `tests/test_verify_cli.py`. Every network response in the 24,571-line suite is hand-written and injected with `monkeypatch`: `FakeTransport` in `tests/test_zotero.py` cans the Better BibTeX JSON-RPC payloads, `_fake_network` in `tests/test_archive.py` cans the HTTP ones. Nothing is recorded from a live service, and the repository holds no recorded response JSON; its `*.manifest.json` files are form-owner manifests.

- Offline suite on hand-written fixtures, in the style the suite already uses. The transition payloads are small: `?since=0&format=versions` and `items/trash?format=versions` are flat `{"KEY": version}` maps, and `/items/<key>/fulltext` is `{content, indexedPages, totalPages}` or `{content, indexedChars, totalChars}` (§9). Five of the six transitions can be taken from a live read: drifted, re-keyed, merged via `dc:replaces`, trashed, and database changed. Database changed no longer needs a second Zotero, because §2.4 detects it as a `412 Precondition Failed`, which one read-only GET carrying a bogus `Zotero-Server-ID` header elicits from the running instance (measured 2026-09-05, §9). **Deleted** is the one that cannot be read live, because a read-only session cannot remove an item from both the library and the trash; its fixture is hand-edited from a recorded baseline, one key dropped from the versions map and from the trash map.
- What a recorder would add is only the bulky item JSON, and it is not needed to reach the six fixtures. If the plan still wants record-and-replay, `pytest-recording` or `vcrpy` in the dev extra does it with redaction hooks; neither is a dependency today. A recorder verb inside `research_vault` is the more expensive option, because it puts a production surface under the coverage, mutation, and form gates for good.
- Live legs against Zotero 10 on a scratch collection, under the existing `live` marker: capture round-trip, linter classification after a live edit, one connector import of an RIS string. The first write leg runs once with the author present.
- Gates unchanged: coverage, mutation baseline, form owners.

## 8. Open points

1. Connector save target (§3).
2. Preprint-to-published supersession: a Zotero relation, a vault fact, or out of scope for this iteration.
3. Whether the OCR path (scanned PDFs with no text layer) is in this iteration. The premise that the endpoint flags them is false: all 3 scanned PDFs in this library return 200 with `indexedPages` equal to `totalPages` and 62 to 126 characters of masthead (§9), so a status-code test does not find them and a content test has to name a floor.
4. Verb and file names, after the glossary is fixed, including the CSL file's own name.
5. The `index.md` collision: the vault's OKF bundle root reserves it, and the adopted tool writes its own under `wiki/`. Whether one file serves both, or the bundle root moves, is settled in the plan against the tool's actual behaviour.
6. The exact shape of the compile input file: which metadata fields head it, and how the literature note path is carried so the tool's source page can be traced back to the citation key.
7. Whether the compiled layer's pages carry a `type` the vault's own structure check accepts: the adopted tool writes its own page shape and its lint requires a `type` on every wiki page, while `research_vault/structure.py` requires a non-empty `type` on every non-fleeting note and derives no expected value for `wiki/`, so the open part is whether the vault takes the tool's vocabulary as it stands or adds `wiki/` to `_FOLDER_TYPES` and then demands one derived value.
8. How a compiled page is prevented from citing a source the vault never captured: no linter in this spec ties a `[[citation key]]` or `[@citation key]` under `wiki/` back to a `literatures/<citation key>.md` that capture wrote, and the adopted tool's own dangling-link check resolves against every file in the vault rather than against the captured set.

## 9. Facts and how each was established (this machine)

Each row records how it was established. Counts are readings on the date given, not constants: the library is live, and a read later on 2026-09-05 returned 2,844 objects at `Last-Modified-Version: 565` against the 2,846 at 540 below.

| Fact                                 | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Established                                   |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| Zotero                               | 10.0.1 on Windows, local API enabled, reachable from WSL2                                                                                                                                                                                                                                                                                                                                                                                                                                            | live probe 2026-09-04                         |
| Better BibTeX                        | 9.0.63; JSON-RPC `api.ready` answers                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | live probe 2026-09-04                         |
| `Zotero-Server-ID`                   | present on every local API response; `6LpvURP2E933` on this machine                                                                                                                                                                                                                                                                                                                                                                                                                                  | live probe 2026-09-04                         |
| `Zotero-Server-ID` scope             | stored in the Zotero database, so it follows the user's data rather than the installation and stays the same across restarts and upgrades; a different id means a different database, and therefore a different set of object versions and keys. It is not minted per launch                                                                                                                                                                                                                         | documentation (Zotero local API, Server ID)   |
| `Zotero-Server-ID` precondition      | a mismatched id returns `412 Precondition Failed`, and that check guards `/api/` paths only; `/connector/ping` and `/better-bibtex/json-rpc` answer 200 with a mismatched header                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-05                         |
| `/items?since=0&format=versions`     | 2,846 keys in one response, `Last-Modified-Version: 540`; children and annotations included                                                                                                                                                                                                                                                                                                                                                                                                          | live probe 2026-09-04 and 2026-09-05          |
| `/items/top?since=0&format=versions` | 1,459 keys, top level only. The 1,387 children are invisible to it, so the linter reads `/items`                                                                                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-05                         |
| object versions                      | 2,724 of the 2,846 objects sit at version 0; only 122 are nonzero, in the range 4 to 540. A `?since=` window above 0 therefore returns almost nothing and can never surface a version-0 object. Per-key comparison of recorded against live over the full map still works, because an edited object moves off 0                                                                                                                                                                                      | live probe 2026-09-05                         |
| child versions                       | children carry their own versions and do not roll up: of 318 sampled parent-child pairs, 26 have a child version above its parent's (attachment `FFGWM8B6` at 540 under item `27MDQHEI` at 539). A parent's version is not a signal for its children                                                                                                                                                                                                                                                 | live probe 2026-09-05                         |
| `items/trash?format=versions`        | 267 keys                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | live probe 2026-09-04                         |
| `/deleted`                           | 404, no endpoint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-04                         |
| citation keys                        | full scan of `/items/top`: 1,452 distinct citation keys across 1,459 top items, with no duplicate; 7 top items carry no citation key                                                                                                                                                                                                                                                                                                                                                                 | live probe 2026-09-05                         |
| `relations.dc:replaces`              | on 126 of 300 sampled top items (merge history)                                                                                                                                                                                                                                                                                                                                                                                                                                                      | live probe 2026-09-04                         |
| attachment child JSON                | carries `md5`, `mtime`, `contentType`, `filename` for stored files only. Full sweep: 1,311 of 1,312 attachments carry `md5` (1,052 `imported_url`, 259 `imported_file`); the one exception is the library's single `linked_url` attachment, which carries no `md5`, no `mtime`, and no `filename`                                                                                                                                                                                                    | live probe 2026-09-05                         |
| `/items/<attachment>/file/view/url`  | returns a `file:///D:/Zotero/storage/...` URL for a stored file; 400 for the `linked_url` attachment                                                                                                                                                                                                                                                                                                                                                                                                 | live probe 2026-09-04 and 2026-09-05          |
| `/fulltext?since=0`                  | 1,350 attachment entries, 432 of them at version 0, maximum 35,545. Fulltext versions are a different number space from item versions, whose library maximum is 540; the two are never comparable                                                                                                                                                                                                                                                                                                    | live probe 2026-09-05                         |
| `/items/<attachment>/fulltext`       | sweep of all 1,350 listed keys: 1,303 return 200 with `indexedPages` and `totalPages`, 15 return 200 with `indexedChars` and `totalChars`, 32 return 404. The response body carries no `version` key                                                                                                                                                                                                                                                                                                 | live probe 2026-09-05                         |
| fulltext coverage                    | a 404 is not the only not-indexed signal. 43 of the 1,303 page-indexed attachments have `indexedPages < totalPages`, because Zotero stops PDF indexing at `extensions.zotero.fulltext.pdfMaxPages` (default 100 in the installed 10.0.1) and serves the truncated text as 200. All 3 scanned PDFs in this library return 200 with `indexedPages` equal to `totalPages` and 62 to 126 characters of masthead, so a status code does not separate a usable index from an unusable one                  | live probe 2026-09-05; the cap by source read |
| annotations                          | 1 highlight in the library, carrying `parentItem`, `annotationPageLabel`, `annotationText`. `/children` returns it from neither end: the item's `/children` returns the attachment alone, and the attachment's returns nothing. It is reachable by an `itemType=annotation` query, on `/items` and on `/items/<key>/children` from either the item key or the attachment key, and from Better BibTeX `item.attachments`, which nests annotations per attachment with key, type, page label, and text | live probe 2026-09-05                         |
| `format=csljson`, `format=bibtex`    | HTTP 500 on every item and on `/items/top`                                                                                                                                                                                                                                                                                                                                                                                                                                                           | live probe 2026-09-04                         |
| BBT `item.export` Better CSL JSON    | works; entry carries `citation-key`                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | live probe 2026-09-04                         |
| connector endpoints                  | `/connector/import`, `/connector/saveAttachment`, `/connector/saveStandaloneAttachment`, `/connector/getSelectedCollection`, `/connector/ping` (200)                                                                                                                                                                                                                                                                                                                                                 | live probe 2026-09-04                         |
| `POST /api/users/0/items` malformed  | 400; does not discriminate write support                                                                                                                                                                                                                                                                                                                                                                                                                                                             | live probe 2026-09-04                         |

## 10. Deferred

Substrate absence; web pages, repositories, product documentation; the claims layer and pinpoint citations (long-form spec); charting (scoping review spec); search (PRISMA-S spec); the workflow-component audit. The `import-source` and `find-sources` skill names are current names and are renamed with the glossary.
