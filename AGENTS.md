# knowledge-harness

Merge back to main locally and push main to origin in the same motion. Fetch before claiming something is absent from origin. Plan documents are amended on main mid-flight: read the plan from `origin/main` at each task start (`git show origin/main:<plan path>`), never from your worktree's frozen copy.

`research/`, `analysis/`, completed plans, and accepted ADRs stand as written — content, internal paths, and file location.

Testing — including the live legs that offline runs silently skip: `docs/testing.md`.
