# Ingest redesign: design

Disposition: current (2026-09-07)

Status: **the active spec** (2026-09-07), amended from the 2026-09-04 draft. This is lane 1 of `2026-09-05-assembly-design.md`, promoted into the one active-spec slot decision 22 allows beside the open decomposition. Its scope is wider than lane 1's capture→compile seam and deliberately so: ingest is one cohesive theme and it contains the seam.

Status of the 2026-09-04 draft: nothing there was decided. Four labels are used: **chosen** (the author picked it in the 2026-09-04 brainstorm, and it can be re-picked), **measured** (probed live on the date given), **proposed** (the design's suggestion), and **open** (not yet answered). A label is written where a reader could not otherwise tell which one applies. Unlabelled prose is proposed. A fact read from source or from documentation rather than probed says which. This document supersedes foundation spec §4 (Zotero bridge).

**What binds this spec.** Very little, and deliberately. `CONTEXT.md` and the ADR register are non-binding: the author set them aside because assumptions under them were disproved. The existing code and tests in `research_vault/` are not binding either, and for the same reason. They were written under those same assumptions, so their existence is not an argument for their survival, and the adversarial review that revised this document found working code implementing a contract §6 retires. "We already built it" is not a reason to keep something, and under the priority order in §0 it is not a reason to prefer building over adopting.

Two things do bind. The **measured environment**: what Zotero, Better BibTeX, and the Claude Code plugin loader actually do, established by live read-only probe or by reading their source, with the date and the method recorded in §9. And the **author's choices**, listed in §0, which are re-pickable but are not this document's to overturn.

Existing code enters here in two other roles, neither of them authority. As **cost**, because naming what a change would touch prices the proposal, which is why §6 counts dependents. And as **evidence**, because code records what was tried, and a mechanism that failed in practice is worth knowing about. Where this spec contradicts working code, it names the code and says what changes.

Consumers: the implementation plan for this iteration; the later specs for search (PRISMA-S), scoping review (PRISMA-ScR), and the workflow-component audit.

## Amendments, 2026-09-07

Seven changes, all additive except three corrections. The 2026-09-04 draft survives intact everywhere else, and that is the point: re-deriving §9's forty live-probe rows because the document was three days old would be the reflex this project exists to correct.

1. **§2.8 is new: the sourcing screen for capture.** The draft screened the *compile* step against 43 candidates (§4.1) and never screened capture at all — it went straight to building. `docs/agents/sourcing.md` requires a `build` verdict to name the numbered must no candidate met, and that record did not exist. It does now.
2. **Annotations defer to lane 4** (decomposition decision 28), on a measurement: 2,844 objects in the library, **one** annotation. This narrows §2.2's provenance tuple, §2.3 step 2 and §2.4's per-object comparison for this iteration. Nothing is retired.
3. **§2.7's "The Zotero web API is not used" gains its reason.** It was asserted in a list of measured facts, with no argument anywhere in a document that otherwise gives one for every retirement.
4. **Correction — `Extra` is not byte-faithful in the CSL file.** Better BibTeX's CSL translator destructures recognised `Key: value` lines out of `Extra` into CSL fields, unconditionally. §2.2's "no field is renamed and no mapping is ours" holds for the note, whose snapshot comes from the local API, and fails for `bibliography.json`.
5. **Correction — a shipped code path is dead on this stack.** `research_vault/zotero.py`'s `export_csl(None)` pages `/api/users/0/items/top?format=csljson`, and §2.7 measures every translator format returning HTTP 500 on 10.0.1. The per-citekey branch goes through Better BibTeX `item.export` and works.
6. **Citation keys are database state, and a fresh Better BibTeX does not rebuild them.** Measured 2026-09-07 by installing Better BibTeX 9.0.63 into the test profile, whose key store was empty and whose citekey pattern was BBT's default rather than this library's: all 1,452 shared items kept the key they carry on the production instance, and none was regenerated. §2.5.1's "keys native and synced" is confirmed from the other direction, and §9's pattern-stability row stops being a preferences reading.
7. **A second Zotero now runs on this machine, and it changes four sections.** The WSL instance was unsynced and bound to port 23129 on 2026-09-07, giving the design a real second database for the first time. It converts §3's write-guard claims from documentation to measurement, supplies §7 with a substrate that removes the second-Zotero obstacle, and produces one correction: **full-text versions are assigned by sync, not by indexing**, so neither `fulltext?since=` nor a recorded-version diff can see a re-index. §2.6 gains the signal that does work.
8. **The attended sitting ran and closed §7's remaining gaps.** Six stages against the test instance: the authorize dialog, a human metadata edit, a re-index confirmed to have happened, a Path A create, and a trash-then-permanent-delete that produced the `deleted` fixture this spec said a read-only session could not reach. It also found a transition nobody had asked about — Zotero empties the trash on a 30-day timer, so **deletion is not always a deliberate act** (§2.5).

## 0. Purpose and boundary

Ingest is the process that gets a source from outside the vault into the vault. It is the first of three garbage-in gates (setup, lint, ingest). This spec covers the whole ingest path and the setup and lint machinery ingest depends on. It follows one priority order throughout: adopt an existing component as-is, else adapt one, else build. Capture and its linter are mechanical and built here; the compile step is adopted.

Chosen scope (2026-09-04):

- Source classes: scholarly and grey-literature documents, meaning anything Zotero can hold as an item with a file or DOI (papers, preprints, standards, reports, books, theses). Web pages, repositories, and product documentation are deferred.
- Post-capture events covered: identity changes, removal, and content drift. Substrate absence (Zotero not running, a clone with no Zotero, CI with no Zotero) is deferred, so a live Zotero at every mechanical check is assumed.
- Trigger: capture on demand; a drift check at every mechanical chokepoint. Nothing writes the vault unattended.
- Greenfield: no existing vault content must survive.
- Derived full text lives in the vault as a gitignored cache, regenerable from Zotero.
- Screening decisions belong to a review, not to the source. The hand-edited note status is retired.

Terms in this document are the terms proposed in §1.1. Each is traced to `docs/research/2026-09-04-import-terminology.md`, either to a primary source or, where the note records that no field names the concept, to that gap: **ingest** (the whole process), **selection** (the human decision), **add** (creating the Zotero item), **capture** (the deterministic copy into the vault), **compile** (the LLM step), **drift** and **refresh** (change detection and re-copy), **item key** and **citation key** (identity and name). The brainstorm's working labels were import, admission, capture, and digest; §1.1 records why each stays or goes.

## 1. Source lifecycle

The design object is the source's relationship with the vault over time, not the files. States, the event that moves a source between them, and the owner of each move (proposed):

| State                          | Meaning                                                                                                                                                                                                       | Entered by                                                                    | Owner                                                                                  |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| identified, screened, selected | The review found the source, screened it, and chose it for retrieval. Lives in the review's selection log.                                                                                                    | Search and screening                                                          | The scoping review workflow, specified separately                                      |
| added                          | A Zotero item exists. Zotero assigned its item key; Better BibTeX assigned its citation key. No mechanical detector: the linter reads notes, so it cannot see an item that has none. Exempt from invariant 4. | A person adds the item, or the agent adds it on the person's instruction (§3) | Person, with the agent as operator                                                     |
| captured                       | The literature note exists with a metadata snapshot and a provenance tuple. Identity is fixed here, once.                                                                                                     | The capture verb (§2)                                                         | Capture verb                                                                           |
| compiled                       | The per-source page and the cross-source page updates exist. Capture records the compile input's hash, because capture writes that input; the tool records nothing in the note.                               | The adopted wiki (§4)                                                         | Adopted wiki                                                                           |
| current                        | Every key in the tuple matches the live versions map, none is in the trash set, and the live citation key is unchanged.                                                                                       | The linter finds no change                                                    | Linter                                                                                 |
| drifted                        | Any object the note depends on carries a version other than the recorded one: the item, an attachment, or an annotation. Each versions independently (§9), so the item's own version is not the signal.       | Linter                                                                        | Linter files a finding; the capture verb refreshes on demand                           |
| re-keyed                       | Live citation key differs from the recorded one.                                                                                                                                                              | Linter                                                                        | Linter records the old-to-new mapping; propagation rewrites every citation-key surface |
| merged                         | A live item's `dc:replaces` names the item key, whether that key is trashed or absent. This takes precedence over trashed.                                                                                    | Linter                                                                        | Linter records the successor; the note points forward                                  |
| trashed                        | A key in the tuple is in Zotero's trash and no live item's `dc:replaces` names it. Merge takes precedence, because Zotero trashes the predecessor of a merge.                                                 | Linter                                                                        | Note kept; finding filed                                                               |
| deleted                        | The item key is absent from the library and the trash.                                                                                                                                                        | Linter                                                                        | Note kept, state recorded; finding filed                                               |
| retracted or corrected         | A registry notice names the source.                                                                                                                                                                           | Existing update-notice check                                                  | Standing recorded on the note                                                          |
| database changed               | The Zotero server id differs from the recorded one; Zotero's documentation: a different id means a different database.                                                                                        | Linter                                                                        | Linter stops and reports; every recorded version is void                               |

Invariants (**chosen 2026-09-07**):

1. Identity is assigned exactly once, at capture, and never reused.
2. Every provenance tuple carries the Zotero server id. Versions and keys are meaningful only within one server id.
3. No transition deletes a note. Records deprecate; they do not vanish.
4. Every transition is visible: a finding in the review queue or a line in the log. `added` is the one exemption, and §1's table says so.
5. The linter runs at capture, at verify, and at pre-commit, through one code path. **The pre-commit leg is held**, and the hold has a stated trigger rather than being an omission: the hook runs `verify --offline` today and this linter needs a live Zotero, so §6 prices the hook going online. Measured 2026-09-07, the quality lane had been red for twelve days unread and `main` carries no branch protection — adding a non-blocking signal to a channel with a demonstrated history of being ignored buys nothing. **Capture and verify bind now; pre-commit binds when the write-side gate is settled.**
6. Every mechanical step must be **falsifiable** — by a linter, by a test, by four-state honesty, or by a design in which the failure cannot occur. A linter is one instrument, not the requirement.

Invariant 6 is a reframing of the 2026-09-04 draft's *"a mechanical step without a named linter is incomplete"*, and the change is deliberate. Read literally, the original requires a checker for every step, which is a rule that manufactures mechanisms — the opposite direction from this repository's own ladder. The evidence is local and recent: the disposition system was 2,033 lines of exactly that instinct, a linter guarding a marker, and deleting it lost nothing because the purge eliminated the problem the marker managed. What survives is the part worth keeping — no mechanical step gets to be unverifiable — without prescribing the instrument.

The reframing has a consequence worth naming. Under the original, §2.5's propagation linter existed because the invariant demanded one. Under invariant 6 it must earn itself by naming what would otherwise be unfalsifiable, which for a vault-wide rewrite of human-authored content it can: after a pass, no citation-key-bearing surface may still name a key the rename log maps away, and nothing else in the vault can attest that.

**The full-map read is a requirement, not a cost.** The 2026-09-04 draft framed three reads as a price worth paying; measurement makes them the only correct route. `?since=` cannot substitute, because a window returns only what changed and a key that vanished appears in neither the window nor the trash. The full-text map is worse still: measured 2026-09-07, **449 of 1,367 entries sit at version 0**, and `?since=1` returns 918 of them with none at version 0 — so a third of this library's indexed attachments are unreachable by any incremental sweep. Zotero's local API charges nothing for the full read: it *"does not impose a default or maximum limit"* and *"does not currently apply rate limiting"*, and `/fulltext?since=0` returned all 1,367 entries in one 29 KB response.

Cost: a whole-vault classification is three reads, not two, and §9 records each. The two versions maps answer current, drifted, trashed and deleted: `/items?since=0&format=versions` for every key including children and annotations, and `items/trash?format=versions` for the trashed set. `/items/top` would hide every child, so the first read is `/items`. Those two maps cannot answer re-keyed or merged, because a key-to-version map carries neither the live citation key nor `dc:replaces`, so a third read of `/items/top?format=json` supplies both fields. Top-level scope is right there, since only top-level items carry either. Refresh fetches are needed only for a note that classified as drifted.

### 1.1 Glossary (**vocabulary chosen 2026-09-07; the rename is task 0**)

The vocabulary below is ratified. **Executing it is the implementation plan's task 0**, not part of this ratification, and it runs in one order: **retire, then rename.** §6 retires the auto-export machinery, parts of `bibliography.py`, claim lines as an ingest output and the managed region — renaming identifiers in code about to be deleted is waste, and deleting first shrinks the surface.

Measured cost 2026-09-07: **200 `citekey` identifiers** across `research_vault/*.py`, the `CONTEXT.md` term, **10 skill files**, and the frontmatter key in every existing note. The last is a migration rather than a substitution and is the only part carrying real risk, so it is testable on its own.

The rename tracks reality rather than taste, and today strengthened that: Better BibTeX's own documentation records that `citationKey` is now **Zotero's native field** — *"With the advent of Zotero 8, items have a Zotero-native citation key field. This has replaced the BBT citation key field"* — so the schema spelling names the authoritative store, and `citekey` names a Better BibTeX synonym for a field Better BibTeX no longer owns.

The glossary in `CONTEXT.md` is rewritten around eight concepts (proposed). Each term is traced to `docs/research/2026-09-04-import-terminology.md`: to the primary source it was read from, or, where the note records that no field names the concept, to that gap. Where two fields use one word for different things, the collision is named so the glossary can say which sense applies.

| Concept                                                                                                | Proposed term                                                                                                                                                           | Field and primary source                                                                                                                                                                                                                                                                                                                                                                   | Working label, and why it goes                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The whole process from outside the vault into the vault                                                | **ingest**                                                                                                                                                              | LLM wiki (Karpathy's gist: "Ingest. You drop a new source into the raw collection and tell the LLM to process it"); digital preservation (OAIS Ingest functional entity)                                                                                                                                                                                                                   | import: Zotero uses it for bringing a bibliographic file into Zotero, which is a step inside ingest, and the connector endpoint is named `/connector/import`                                                                                                                                                                                                                          |
| The human decision that a source is accepted                                                           | **selection**, with the outcomes **included** and **excluded**                                                                                                          | Evidence synthesis (JBI: "selection (also known as screening) of evidence sources"; PRISMA-ScR item 9; PRISMA 2020 item 16b)                                                                                                                                                                                                                                                               | admission: found only in the llmwikis handbook, for agent-memory records; no review guideline uses it                                                                                                                                                                                                                                                                                 |
| Creating the library record in Zotero                                                                  | **add** (to Zotero); **import** when Zotero parses a bibliographic file                                                                                                 | Reference managers (Zotero: "Adding Items to Zotero"; the connector: "Clicking the save button will create an item in Zotero")                                                                                                                                                                                                                                                             | admission (the act): same as above; archival "accession" is the field term but no Zotero user says it                                                                                                                                                                                                                                                                                 |
| The deterministic copy of metadata, attachments, annotations, and text into the vault, with provenance | **capture**                                                                                                                                                             | LLM wiki (hermes llm-wiki: "Capture the raw source"), the only source the terminology note records using the word in this sense                                                                                                                                                                                                                                                            | keeps its label; collisions: PREMIS "capture" is the acquisition pull that precedes ingest ("Note that the Capture process precedes the Ingest process"), so it names the producer side of the boundary, nearer §3's add than §2's copy; SAA "capture" is making surrogate copies such as scans; change data capture in data engineering means capturing changes, which is drift here |
| The LLM step that turns a captured source into wiki pages                                              | **compile**, producing a **source page** and updated **concept pages**                                                                                                  | LLM wiki (nvk/llm-wiki: "Transform raw sources into wiki articles"; llmwikis handbook: "the agent turns it into a source summary, updated concept pages")                                                                                                                                                                                                                                  | digest: nvk uses it for a memory record, no other field for this step                                                                                                                                                                                                                                                                                                                 |
| Detecting that the Zotero item changed, vanished, or was re-keyed, and re-copying                      | **drift** (the detection) and **refresh** (the re-copy)                                                                                                                 | No field names this concept (terminology note, Gaps K6). **drift** and **refresh** are borrowed from LLM wiki (hermes: "flag drift when it has changed"; nvk: "refresh: Freshness check ... re-fetches ... detects changes"), where both name upstream-URL freshness rather than a library record. Zotero's own mechanism words stay verbatim: version, `Last-Modified-Version`, `?since=` | sync: Zotero's sync is client to zotero.org; using it here would name the wrong thing                                                                                                                                                                                                                                                                                                 |
| The stable identifier across a source's life, and its name                                             | **item key** (identity) and **citation key** (name)                                                                                                                     | Reference managers (Zotero API: `<itemKey>`; Zotero schema field "Citation Key"; Better BibTeX: "the citation key is the piece of data that connects your bibliography")                                                                                                                                                                                                                   | citekey: a Better BibTeX synonym, not the schema name. BBT's own pages use it ("Configurable citekey generator", "Generating citekeys for your items"). The Zotero schema field is `citationKey` and its label is "Citation Key", so the vault standardises on the schema spelling                                                                                                    |
| A source's standing after it was added                                                                 | **trashed**, **deleted**, **merged** (Zotero's words); **retracted**, **corrected** (Retraction Watch, Cochrane); **superseded** (nvk status; EndNote keeps the record) | Reference managers and evidence synthesis                                                                                                                                                                                                                                                                                                                                                  | screening state on the note: excluded and included belong to the review's selection, not to the source                                                                                                                                                                                                                                                                                |

Kept as they are, with their field: **item** (Zotero's record), **record** (PRISMA 2020: a title or abstract indexed in a database), **source of evidence** (PRISMA-ScR), **literature note** (Obsidian community: ZotLit, Obsidian Zotero Integration), **fixity** (OAIS, PREMIS).

Entries retired: Admission, Import as currently defined, Screening state, Bibliography export, Citable as defined through the note status, and the citekey spelling.

## 2. Capture

### 2.1 Identity and name

The vault's identity for a source is the Zotero item key qualified by the Zotero server id. Item keys are assigned by Zotero, are not user-editable, are unique within a library, and survive metadata edits and citation-key changes. The citation key is the source's name: it names the file and appears in prose citations and the CSL file. A citation-key change is a rename of a thing whose identity did not change, so propagation is mechanical.

This answers the two objections the author raised against making the citation key the identity: that a third party can change it, since Better BibTeX regenerates keys from metadata, and that its uniqueness is not guaranteed, since a suffix freed by a deletion can be handed to a different source later. Neither holds for the item key, which Zotero assigns, does not expose for editing, and does not reuse within a database. A freed suffix (`chen2025a` reused for a different source) is detected because the item key differs from the one recorded under that name.

Filename (chosen): `literatures/<citation key>.md`. The title is carried in frontmatter and as the H1, and in `aliases`, so Obsidian resolves `[[Title]]` as well as `[[citation key]]`. The file renames only on a re-key.

### 2.2 Record home (chosen: both, one writer)

Two files carry bibliographic data, and the capture verb is the sole writer of both.

**The literature note's frontmatter** carries:

- a metadata snapshot: a fixed subset of the Zotero item's data fields, copied verbatim under Zotero's own field names (`itemType`, `title`, `creators`, `date`, `DOI`, `url`, `publicationTitle`, `volume`, `issue`, `pages`, `publisher`, `ISBN`, `language`, `abstractNote`, and **`tags`**), so no field is renamed and no mapping is ours. `tags` is in that list for a specific reason: the Zotero plugins §6 adopts the DOI and metadata checks away to report their verdicts by tagging the item, with strings like `⚠️ Invalid DOI`, `⛔ No DOI found` and `MetadataHunter: No DOI`, removed again when a later run succeeds. Snapshotting tags is what keeps that verdict visible to the vault after the check itself leaves;
- a provenance tuple: `zotero-server-id`, `zotero-item-key`, `zotero-item-version`, `citationKey`, `attachments` (a list of `{key, version, md5, contentType, filename}`), `annotations` (a list of `{key, version}`) — **deferred to lane 4 for this iteration**, see below, `fulltext` (a list of `{attachment-key, sha256}` over the cached text; the sha256 is the comparison, because the endpoint's response body carries no version), the compile input's `sha256`, and `generated: {by, at}` (OKF §5.2 shape, already adopted). Every object the note depends on contributes its own key and version, because Zotero versions objects independently: an attachment or annotation change never moves the item's version (§9). A single `annotations-version` would name nothing Zotero serves;
- `title` and `aliases` for Obsidian; `type: literature` for OKF.

**Annotations are deferred to lane 4** (decomposition decision 28). Measured 2026-09-07: the library holds **2,844 objects and one annotation**. The tuple's `annotations` list, §2.3 step 2's `?itemType=annotation` route and its two fallbacks, and §2.4's per-annotation comparison all serve that single highlight. Nothing is retired — the `history-notes` guideline's method is built on annotations and the workflow implies them — but a renderer shaped now would be shaped against an unobserved usage pattern. Fetching them is one call whenever lane 4 wants it.

The literature note becomes wholly machine-written, and the `%%rv-managed%%` markers retire with the free region they delimited (§6). The whole body below the frontmatter is capture's, compared byte for byte on every run.

**The vault-owned fields, enumerated, because the snapshot does not cover them.** The subset above is Zotero's, copied under Zotero's names. `research_vault/notes.py` also writes seven fields that are the vault's own, and a spec that defines the record home must say what becomes of each rather than leave the reader to diff the code:

| Field           | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `accessed`      | **Kept, and it is not Zotero's `accessDate`.** Ours is the day the vault first captured the source, written once and never overwritten (`notes.py:238`). Zotero's is the day the *item* was retrieved. Two different facts; the vault keeps its own                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `pmid`          | **Kept. No extractor is needed, and neither is one for `PMCID`.** Corrected twice on 2026-09-07. Better BibTeX's CSL translator lifts **both** `PMID:` and `PMCID:` out of `Extra` into CSL `PMID` and `PMCID` fields — the label table documents a `pmcid` row exactly parallel to `pmid`, and `epstein.etal2016` renders `PMID='28503678'` and `PMCID='PMC5428074'` from an `Extra` reading `PMCID: PMC5428074\nPMID: 28503678\nshort-doi: 10/bjjd`, with the unrecognised `short-doi` line left as residue in `note`. An intermediate correction here claimed PMCID was dropped entirely; that was a measurement error — the probe printed a hardcoded key list that omitted `PMCID` and absence was inferred from it. Recorded because the wrong version was committed and a reader may have seen it |
| `version`       | **Renamed.** Superseded by the tuple's `zotero-item-version`, which is the same number under the name §2.2 gives it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `fixity-sha256` | **Split.** Its fixity half becomes the tuple's per-attachment `md5` and per-cache `sha256`. Its second role — the **acknowledgment scope**, the hash an ack is scoped to so a changed target re-fires the finding — is not a fixity concern and is recorded as open point 14                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `status`        | **Retired** with the note-level screening state (§6). Screening belongs to the review, not to the source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `authority`     | **Retired.** No writer and no reader in `research_vault/`; the field is declared in the foundation spec and never populated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `stale_after`   | **Retired.** OKF-optional, never written, never read, and §5 of the foundation spec said its adoption "does not create a new staleness subsystem" — so nothing is lost by dropping the field with the subsystem that never existed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

Frontmatter is not wholly capture's, and the spec says so rather than implying it: `archive-url`, the `verified` event list, and any human-set field are written by other verbs above where the marker used to sit. What retires is the two-owner split inside the body, not the idea of machine ownership.

**The CSL JSON file** (`system/bibliography.json` today; name follows the glossary) holds one entry per captured item, rendered by Better BibTeX's `item.export` with the Better CSL JSON translator, sorted by citation key. It exists for pandoc. Its scope is the captured set, not the library.

**`Extra` does not survive this file byte-faithfully, and §2.2's "no mapping is ours" does not extend to it** — though the loss is narrower than an intermediate draft of this paragraph claimed (read 2026-09-07, `retorquere/zotero-better-bibtex translators/csl/csl.ts`). The CSL translator runs `Object.assign(item, getExtra(item.extra, 'csl'))` unconditionally, destructuring recognised `Key: value` lines out of `Extra` into CSL fields and leaving only the residue in `csl.note`. This library carries PMCID and PMID as `Extra` lines, so they are destructured here. The literature note is unaffected — its snapshot comes from the local API, which serves the field as typed — so the two files disagree about `Extra` by construction, and the note is the faithful one. It is regenerated whole by the capture verb at the end of a run, batch or single, rather than entry by entry, so the file is never left in a partial state. A source that leaves Zotero keeps its note under invariant 3 but loses its CSL entry, because the file's contract is that every entry resolves; the note's lifecycle state is what records the departure. No auto-export, no observation, no byte-compare against a third-party writer.

The capture verb writes both from the same version-checked read pass (§2.3), which restarts if the item moves mid-read, so the two files cannot disagree with each other. The linter diffs both against live Zotero.

### 2.3 The capture verb

One verb, per item or batch, keyed by citation key or item key. For each item it:

1. reads the item JSON from the local API (`/api/users/0/items/<key>?format=json`);

2. reads the children (`/items/<key>/children`): attachments with `md5`, `mtime`, `contentType`, `filename`, and the file URL from `/items/<attachment>/file/view/url`; notes. **A plain `/children` call omits annotations** (measured, §9): it returns none from the item key and none from the attachment key, even though an annotation's `parentItem` is the attachment. `/children?itemType=annotation` does return them, from either key, and that is the route capture uses. Two alternates stand if it disappoints: `/items?itemType=annotation` filtered on the attachment key, and Better BibTeX's `item.attachments`, which returns them nested per attachment with key, type, page label and text. `md5` exists only for stored-file attachments, so a linked attachment carries no fixity and the tuple says so rather than omitting the field. Zotero child notes are read here too, and unlike today they are rendered (step 5);

3. reads the attachment's indexed full text (`/items/<attachment>/fulltext`) into the gitignored cache. PDFs report `indexedPages` and `totalPages`; other content types report `indexedChars` and `totalChars`; the cache entry records whichever pair it received. A 404 means Zotero has no indexed text. **A 200 is not by itself a usable text**, and two separate tests are needed. The page test: Zotero truncates PDF indexing at `extensions.zotero.fulltext.pdfMaxPages`, default 100 in the installed 10.0.1 (source read), and reports the truncation as success, so `indexedPages < totalPages` marks the cache entry partial. The content test: a scanned PDF with no text layer returns 200 with `indexedPages` equal to `totalPages` and a near-empty body, so the page test alone would pass it as complete. A content floor is therefore also required, and §8 point 3 carries what that floor should be. Capture never treats a partial, empty, or absent index as full text;

4. contributes to the CSL file. **Chosen 2026-09-07: one whole-library read, not a call per item.** `GET /better-bibtex/library?/<library name>.json` returns the entire library as Better CSL JSON in a single request — measured 2026-09-07 at **0.46 s, 2.3 MB, 1,452 items, every `id` equal to its `citation-key`**, which matches the independently counted 1,452 bibliographic items exactly. Capture filters that to the captured set. This removes the draft's batch-failure mode outright: `item.export` *"takes a list and fails the whole batch on one unknown key"*, so a batch capture had to call per item or chunk-and-retry.

   **The route is undocumented, and the spec says so rather than discovering it later.** It appears in no Better BibTeX documentation page; it was found by reading how `obsidian-reference-map` reaches Better BibTeX. An undocumented endpoint is an unpinnable dependency of exactly the class §2.8 prices, so the fallback is named here: `item.export([citation key], "Better CSL JSON")` per item, which is the documented call and what the 2026-09-04 draft specified. If the library route breaks, capture degrades to it and reports which route it used.

   Original reasoning, retained: `item.export`'s single call is keyed by the name rather than the identity, because that is the only key the method takes. This one call is keyed by the name rather than the identity, because that is the only key the method takes; the citation key used is the one just read from the item JSON in step 1, so it cannot be a stale one. The method takes a list and fails the whole batch on one unknown key, so a batch capture calls it per item, or chunks and retries the failing chunk singly, and reports which key was rejected;

5. renders the note whole: frontmatter, then title, creators, venue, identifiers, attachments, annotations as a list with page labels and verbatim highlight text, and the item's Zotero child notes. Rendering the child notes is new, and it is what gives a person's own prose about a source a home now that the free region retires: they write it in Zotero, where they already annotate, and capture projects it like everything else;

6. writes the cache, and contributes its entry to the CSL file, which is regenerated whole at the end of the run;

7. compares the rendered projection with what is on disk and reports NOOP when nothing changed.

Steps 1 to 4 are four reads through two subsystems, not one atomic read, so an edit made between them would be captured half-old. Capture therefore re-reads the item's version after step 4 and, where it moved, starts the item again. The tuple records the version that was current across the whole read.

Every mechanical outcome is four-state. UNREACHABLE (Zotero not answering) writes nothing and files an outage finding. A malformed response writes nothing.

### 2.4 The lifecycle linter (**chosen 2026-09-07**)

One check, one code path, run at capture (before rendering), at verify, and at pre-commit. It:

1. reads every literature note's provenance tuple;
2. sends the recorded server id as the `Zotero-Server-ID` request header on every call, which Zotero answers with `412 Precondition Failed` when it is talking to a different database. The linter does not compare the value itself; it reports database-changed on the 412 and stops. Zotero's documentation directs clients to partition cached versions by that id, which is what the tuple does;
3. fetches the whole key set with `?since=0&format=versions` and the trashed set with `items/trash?format=versions`. The full set is required, not `?since=<lowest recorded version>`: a `since` window returns only items that changed, so a key that vanished appears in neither the window nor the trash and would read as current. The full set arrives in one response, at the size §9 records, so it costs nothing;
4. classifies every note by comparing **every** key in its tuple, the item and each attachment and each annotation, against the versions map and the trash set: current (all match and none trashed), drifted (any version differs), re-keyed (live `citationKey` differs), merged (item key absent and a live item's `dc:replaces` names it), trashed (any key in the trash set), deleted (item key in neither set). The comparison is per object because Zotero versions objects independently; classifying on the item's version alone would report a swapped PDF or a new annotation as current;
5. on a drifted note, names the cause from which key moved, and where an attachment moved, compares its `md5` and the cached text's sha256 to say whether the file changed or only its metadata did;
6. files one finding per non-current note with the transition's reason code, and blocks nothing except database-changed.

This costs a running Zotero at every commit, which follows from the scope in §0: substrate absence is deferred. Until it is taken up, a commit made with Zotero closed reports the linter UNREACHABLE, and the pre-commit hook treats an outage as it treats any other: it files the finding and does not block. The plan states that behaviour as a test, so an outage never silently reads as a clean classification.

Reason codes, proposed: `drift`, `re-keyed`, `merged`, `trashed`, `deleted`, `database-changed`, `outage`. `drift` already exists in the registry. `superseded-note` is retired with the note status. `not-admitted` stays in service, because `find-sources` still files it (§6).

### 2.5 Re-key detection — propagation moved to its own spec

**Detection stays here. The vault-wide rewrite does not.**

A re-key is detected by the lifecycle linter alone: the item's live `citationKey` differs from the recorded one under an unchanged item key. Better BibTeX's `item.regenerate_key` is a **mutating** call that assigns new keys, so it is never a detection source; if it is ever used deliberately, it is the person's act and its returned old-to-new mapping feeds the propagation pass. §2.4 classifies the note `re-keyed`, files a finding with that reason code, and blocks nothing. The item key is unchanged throughout, so the identity is never in doubt and the finding is a rename report rather than a loss.

**Why propagation is not in this spec.** Everything else here is a one-directional projection *into* `literatures/`: capture writes literature notes and the CSL file, and touches nothing a person wrote. Propagation is the opposite — a vault-wide mutation over human content, rewriting `[@old]` citations in drafts, `[[old]]` wikilinks anywhere, the CSL entry id, and the review-queue and acknowledgment targets that name a key. Different blast radius, different risk class, and a design question this document is the wrong home for: the rename log has no site, its own linter must read it, and where it lives is a vault-structure decision (§8.3, open point 21).

> [!WARNING]
> **The propagation spec must be written and implemented immediately after this one, before anything else in lane 1.**
>
> §2.1 chooses `literatures/<citation key>.md` — the filename is the source's *name*, and the name changes. That choice is sound **only** because a re-key propagates. Until it does, every re-key leaves a note whose filename contradicts its own recorded `citationKey`, and every `[@key]` and `[[key]]` naming it dangles. §2.4 reports each one and repairs none, so the findings accumulate and the vault degrades quietly while still passing every check it has.
>
> **Corrected 2026-09-07 on the measured trigger set.** An earlier draft of this warning said the debt "grows with every metadata edit Better BibTeX regenerates a key from". That mechanism does not exist under this configuration: `resetKeyOnChange` defaults to **false** and is unset on this machine, so editing metadata does *not* re-key. The real vectors are all deliberate — an explicit right-click Refresh, `item.regenerate_key` (which §2.5 never calls), and cloning an item, which clears the key before `fillKeyAfter` mints a new one.
>
> That makes re-keys **rare, not absent**, and it changes the urgency rather than the order. A rare event with no repair is still a permanent divergence: the first one leaves a note whose filename contradicts its own recorded `citationKey`, and nothing ever fixes it. Propagation still runs next.

Detection's own obligation is discharged here: `re-keyed` is a lifecycle state (§1), it has a reason code (§2.4), and the linter that finds it runs at capture, at verify and at pre-commit through one code path (invariant 5).

**Deletion is not always a deliberate act, measured 2026-09-07.** `extensions.zotero.trashAutoEmptyDays` defaults to **30** and is unset on this machine, so Zotero purges aged trash on its own: the test instance's trash held 504 keys at 11:47 and 212 at 12:02, and a database copy taken at 11:42 still carried 487 rows in `deletedItems` — so the items were present before this session's restarts, and about **280 of them left the library during one, with no user involvement**. The purge is this instance's own, not something inherited from the copied library. Two consequences. A design reading a disappearance as intent would misread all 292; and because the local API has no `/deleted` endpoint (§2.7), nothing distinguishes a purge from a considered removal after the fact. Detection is therefore a key-set diff, and what it finds is a *fact*, never a decision — which is why the vault deprecates its own record rather than deleting it.

### 2.5.1 What Better BibTeX's own documentation says about keys (read 2026-09-07)

**Measured 2026-09-07, and the measurement is stronger than the documentation.** The test instance was given a copy of this library, a fresh profile whose Better BibTeX key store was empty, and — unintentionally at first, which is what makes it a test — a *different* citekey pattern: BBT's default `auth.lower + shorttitle(3, 3) + year` against the library's own `authEtal2.lower.len + year`. If keys were derived from metadata at read time, every key would have come back in the other shape. All **1,452** shared bibliographic items returned the key production reports, disambiguation suffixes included (`graham.etal2019a`). Then the pattern was set to the library's and the keys were re-read: unchanged again. Two patterns, in both directions, zero re-keys. So a citation key is **stored state that travels with the database**, not a projection of the metadata — which is why §2.1 can use it as a filename and why another machine reproduces these names by syncing rather than by matching a Better BibTeX configuration.

Read at the **v9.0.63 tag**, matching the measured build, rather than from `master`.

**Pinning no longer exists, and the key is Zotero's own field.** BBT's front page: *"With the advent of Zotero 8, items have a Zotero-native citation key field. This has replaced the BBT citation key field… Zotero will have moved all pinned keys out of the `extra` field into the native field… The concept of pinning keys is gone; keys are always pinned now… Upside to all of this is that keys will sync."* Measured here: `autoPinMigrated` is true, every sampled item carries a populated native `citationKey`, and `extra` is empty. The `pinned()` formula function is documented as a no-op kept only for legacy formulas.

Two consequences for this design, both favourable and neither planned:

- **`zotero.py` reads the authoritative store.** §2.3 step 1 takes `citationKey` from the local API's item JSON, which is now Zotero's own synced field rather than a Better BibTeX projection. The vault's detection route survived the largest architectural change in BBT's history by accident, and nothing needs to parse `Extra` for a pinned key.
- **§2.1's stated objection has weakened.** It answers the worry that *"a third party can change it"* by moving identity to the item key. That answer is still right, but the worry is smaller than it was: keys now sync, so a change made on one machine propagates rather than diverging silently.

**The regeneration triggers, measured rather than assumed.** `resetKeyOnChange` (*"Regenerate citation key when item changes"*) defaults to **false** and is unset on this machine, so a metadata edit does not re-key. `fillKeyAfter` defaults to **2** seconds and only fills a key for an item that has none — it mints, it does not change. BBT's own summary is *"BBT is conservative about citation key changes"*, and editing the key formula explicitly does not retro-apply: *"A new formula takes effect for items changed from that point forward."*

So the live vectors are three, all deliberate: an explicit right-click **Refresh**, **`item.regenerate_key`** over JSON-RPC (which this design never calls, per §2.7), and **cloning** an item, which clears the key before it is re-filled.

**This lowers the rate and leaves the design unchanged.** §2.1's item-key identity still holds, because a key that changes rarely still changes. §2.4's detection still holds. And the ordering in the decomposition's decision 29 still holds, on the argument that a rare divergence with no repair is permanent rather than tolerable.

### 2.6 Paths and the full-text layer (**chosen 2026-09-07**)

No absolute paths in the repository. The file URL from Zotero is a Windows `file://` path, percent-encoded; the existing `wslpath` shim resolves it at use time. The shim works because this host runs WSL2 with `networkingMode=mirrored`, which is also why a Windows-side Zotero answers on localhost at all.

**The extracted text becomes a vault layer, `fulltext/<attachment key>.md`, gitignored and OKF-conformant.** One file per indexed attachment, carrying frontmatter with a `fulltext` type, its `sha256` recorded in the literature note's provenance tuple.

Four properties decide it, and the 2026-09-04 draft's "gitignored cache folder" had only the last:

- **Obsidian indexes it.** Gitignored is a git concept, not an Obsidian one, so the text of every paper is searchable in Obsidian's own search box. Nothing else on the table delivers that: `qmode=everything` answers a *Zotero* query, and reading Zotero's cache at use time is invisible to the vault.
- **The path is vault-relative**, so it does not depend on where Zotero's data directory happens to live on a given machine.
- **Frontmatter earns it a walk.** Because the files are OKF-conformant they need no exclusion from `verify`'s rglob, unlike `.raw/captured/`, which §4.3 has to gitignore *and* exclude.
- **Derived, so never committed.** 1,335 files, 103.6 MB, mean 79 KB — measured 2026-09-07. Regenerable in one pass, so git carries none of it, and §12's cutover does not inherit another 100 MB the way it inherited 260 MB of transcripts.

**Named by attachment key, not item key.** Measured 2026-09-07: 1,306 parent items each carry exactly one indexed attachment and none carries two, so item-key naming would work today — but Zotero does not guarantee it, an item with two PDFs would collide, and the failure mode is a silently overwritten file. The attachment key is collision-proof by construction, it is already the key the tuple records, and it gives the 5 indexed attachments that have no parent a home.

**The recorded hash is machine-local, measured 2026-09-07.** The same attachment indexed on the two instances does not yield the same bytes: `568BWIT7` returns 34,024 bytes on production and 33,822 here, with different hashes. So the `sha256` in the provenance tuple certifies *this machine's* derivation against *this machine's* Zotero. That does not weaken the mechanism — the hash is only ever checked against the instance that produced it, which is the sole comparison §2.4 makes — but the tuple's hash is not a portable fact and must not be synced as though it were.

**A second machine derives it rather than receiving it.** Full-text sync is off and attachment storage is WebDAV (measured 2026-09-07), so Zotero syncs the *items* and the *files* but never the extracted text — the cache belongs to the machine that indexed. Another machine therefore syncs, downloads, indexes, and regenerates this layer locally, at a first-run cost. That is a property of the configuration, not of the design: turning full-text sync on would change it.

Zotero's index is the source of truth for extracted text and `.zotero-ft-cache` is its store; **verified 2026-09-07, byte-identical to `GET /items/<key>/fulltext` in 4 of 4 sampled attachments**, so either route yields the same bytes and the choice between them is about convenience, not fidelity. This layer is a projection of that, and the note's recorded `sha256` is what makes a disagreement visible. A disagreement is always resolved by re-reading Zotero, never by preferring the local copy.

**The change signal is the content, not a version.** Measured 2026-09-07 on the test instance: all 1,142 rows of `fulltextItems` sit at `version` 0, and the column's schema default is 0. That instance indexed every one of its attachments locally after the profile was unsynced, so **local indexing assigns no version** — the nonzero values on the production instance (up to 35,545, in the *library* version space, whose value in the test database is 35,774) are stale numbers left by a sync that is now off. Two consequences follow and they point the same way: `fulltext?since=N` for any `N ≥ 1` returns **nothing at all** on the test instance while 1,115 entries exist, and recording each entry's version and diffing it later inherits exactly that blindness, because a re-index moves the version from 0 to 0. So this layer ignores `version` entirely and compares the `sha256` it already records against a fresh read. That is affordable rather than clever: fetching the whole corpus is **26 seconds and 97 MB across 1,367 attachments** (measured 2026-09-07, 19 ms per attachment), so a full pass needs no incremental mechanism to guard it. Attachment `md5` and `mtime` are available as a cheaper gate — present on 1,311 of 1,312 attachments (§9) — and are not used, because a 26-second pass does not need one and a gate that can be wrong is worse than a scan that cannot.

**Page boundaries survive.** The served text carries form feeds at page breaks — verified 2026-09-07, `content.split('\f')` yields exactly `indexedPages` segments in order. Page attribution therefore needs no second extraction pass and no PDF library, which is what §5's quote-provenance machinery had assumed.

### 2.7 API use (measured 2026-09-04)

- Zotero 10.0.1 local API, version 3, serves item JSON with `key`, `version`, `citationKey`, `dateModified`, `relations`; children with `md5` and `mtime`; `/file/view/url`; per-attachment `fulltext`; `fulltext?since=`; `?since=&format=versions`; `items/trash`; a `Zotero-Server-ID` header on every response. It has no `/deleted` endpoint — re-measured 2026-09-07 on **two** instances, so this is a property of the local API and not of one database's state, and deletion detection is therefore a key-set diff or nothing.

- **The local API mints its own version numbers.** The versions it serves are not the ones in the database behind it: measured 2026-09-07, the test instance's API reports item versions in 0–1,681 while its own `items.version` column runs to 14,595 and its library sits at 35,774. Zotero's instruction to partition cached data by server id is therefore not advice about two libraries drifting — the numbers are per-instance by construction, which is why §2.2's provenance tuple carries the id rather than the version alone.

- Every translator-based format on the local API (`format=csljson`, `format=bibtex`, `include=csljson`) returns HTTP 500 on 10.0.1. Better BibTeX 9.0.63 `item.export` is the working CSL renderer.

- **A shipped path is dead on this stack.** `research_vault/zotero.py`'s `export_csl(None)` — the whole-library branch — pages `/api/users/0/items/top?format=csljson`, which is a translator format, so it raises `local API HTTP 500`. The per-citekey branch goes through Better BibTeX `item.export` and works. §2.2 regenerates the file from the captured set rather than the library, so the branch has no caller in this design; it is recorded because it is live in the tree today.

- Better BibTeX JSON-RPC is used for one thing: rendering the CSL entry. Its auto-export is not used, and `item.regenerate_key` is never called, because it mutates (§2.5). `item.attachments` is the named fallback for reading annotations.

- The Zotero web API is not used, for two reasons that are worth separating. **Corrected 2026-09-07**: an earlier draft of this line said the two are "the same API at two base URLs" and that "what differs is the data behind each". The second half is wrong, and Zotero's own documentation is the refutation — the local-API page exists to enumerate *"the notable differences from the Web API"*, and lists nine.

  **The surface differs.** `format=atom` returns 501. `q` uses Zotero's local quicksearch, so *"the set of items returned by a given `q` value may not match the Web API exactly"*. The local API *"does not impose a default or maximum limit"* and *"does not currently apply rate limiting"* — both in our favour, and both meaning code written against the Web API's ceilings is dead weight here. It answers 403 when the local-API preference is off, and 501 for an unsupported API version. And *"in Zotero 10+, local versions are unrelated to the versions in this API, so clients must partition cached data by server ID"* — `must`, the strongest modal on the page.

  **It also serves things the Web API does not**, and one of them is load-bearing: `<userOrGroupPrefix>/items/<itemKey>/file` and `/file/view/url` are **local-only**. §2.6's path shim and §9's `file:///D:/Zotero/storage/…` probe both depend on that endpoint, so repointing the base URL at `api.zotero.org` does not degrade the design — it breaks the attachment path outright. **Device independence is therefore an architecture, not a configuration change**, which an earlier draft of this spec got backwards. `searches/<searchKey>/items` is likewise local-only: the Web API exposes search metadata but never executes a search.

  **The data differs too**, and that half stands. `extensions.zotero.sync.fulltext.enabled` is **false** and `sync.storage.protocol` is **webdav** (measured 2026-09-07), so no extracted text was ever uploaded and the web endpoint returns nothing for the one input capture most needs. Flipping that preference would fix the data half and leave the surface half untouched.

- The bib-file export is not used.

### 2.8 The sourcing screen for capture (2026-09-07)

The 2026-09-04 draft screened compile against 43 candidates and built capture without screening it. `docs/agents/sourcing.md` puts capture in the **mature tool** tier, where a `build` verdict must name the numbered must no candidate met. This is that record.

**The bounded question.** What can project a Zotero item into a vault markdown note, supplying everything §2.2's record home needs?

**Floors.** Three, after the author cut four from a first draft that had encoded the incumbent's shape as the requirement:

- **C1** — reads the library programmatically, no human export step.
- **C2** — yields the **Zotero item key** and the citation key distinguishably. Without both, §2.5's re-key is indistinguishable from a new source.
- **C4** — yields all item metadata byte-faithful, `Extra` included.

Scored, not floors: **C3** attachment full text; **C5** tags; **C6** whether the tool's own note could be adopted wholesale, which scores *highest* — adopting their note is more adoption, not less.

**Candidates screened: 17**, from the Obsidian community registry (7,367 plugins, 60 matching), the GitHub API, and four community workflow guides. Zotero-side plugins, Obsidian-side plugins, Zotero translators, standalone CLIs and a starter vault. Every floor failure was adversarially verified; 11 of 12 survived.

**Four clear C1, C2 and C4:** `obsidian-zotero-integration` (Better BibTeX JSON-RPC), `aidenlx/zotlit` (SQLite direct), `vanakat/zotero-bridge` (local API, but a 526-line library that writes no note), `wenzhi-ding/zotero-direct` (raw file read with a WAL overlay).

**None clears C3. That is the verdict's whole basis.**

| Candidate                     | Full text                                                      |
| ----------------------------- | -------------------------------------------------------------- |
| `obsidian-zotero-integration` | zero hits for `fulltext`, `pdftotext`, `extractText` in `src/` |
| `aidenlx/zotlit`              | never reads `.zotero-ft-cache`, where the extracted text lives |
| `vanakat/zotero-bridge`       | excludes attachments outright — `itemType: '-attachment'`      |
| `wenzhi-ding/zotero-direct`   | no full text, and no annotations either                        |

**Verdict: `build`, on floor C3.** Capture must hand the compile step extracted text, because the adopted engine has one implemented adapter and cannot read a PDF (§4.3). No candidate supplies it. Zotero's own index does, and **tracer T7 measured that it beats the alternative rather than merely substituting for it**: against independent PyMuPDF extraction of five random papers, character parity 0.99–1.00, and the entire word-recall gap is PyMuPDF's hyphenation fragments against Zotero's correctly rejoined words. Zotero resolves soft hyphens across line breaks; the library does not.

Once that client exists, the rest is the same round trip. `/items/<key>`, `/children`, `/fulltext`, `?format=versions` and `items/trash` are one version-checked read pass, so metadata and the provenance tuple cost almost nothing beyond the text that had to be fetched anyway.

**Recorded so they are not re-screened.** `argenos/zotero-mdnotes` is archived, frozen at 2022, and its maintainer declined Zotero 7. `hans/obsidian-citation-plugin` has not shipped since 2022. `anoopkcn/obsidian-reference-map` is within-note and stale. `mgmeyers/pdfannots2json` is **Zotero-orthogonal** — it parses the PDF's own `/Annots` arrays, and Zotero 7+ keeps annotations in `zotero.sqlite`. `daeh/zotero-markdb-connect` flows Obsidian→Zotero, writes one tag and no files, and is a **complement to any generator including this one** rather than a candidate. Two repositories in this ecosystem carry a recent `pushed_at` over frozen code and were silently archived, so activity was judged on the last commit touching source.

**What building costs, priced rather than left implicit.** The maintenance of a client against a versioned REST API, which is the low-churn surface — ZotLit needs daily maintenance precisely because it reads a private SQLite schema instead. No user-editable template, which this vault does not want, having one shape. No annotation image extraction, deferred with annotations. And no offline read: §0 defers substrate absence, and ZotLit's ability to read with Zotero closed is the capability that deferral postpones.

**What no longer counts against building.** The local API and the Zotero Web API are one API at two base URLs, so device independence is a configuration change rather than an architecture. `zotero-api-client` already exposes `apiScheme` and `apiAuthorityPart`; ZotFlow simply never passes them.

## 3. Selection and adding to Zotero

Selection is the human act, recorded where it is made: the review's selection log, or an ad hoc request. The keystrokes that add the item are not the decision, so the agent may perform them.

Zotero 10 offers two write paths, and the plan picks between them with one attended trial rather than this document asserting the answer.

**Path A, the documented local API write** (`POST /api/local/authorize` for a key, then ordinary write requests). It is authorized, per-application, and revocable; the person sees a dialog naming the application and chooses Allow, Always Allow, or Deny. Its cost is that the caller supplies Zotero item JSON, so the vault would own a metadata mapping it otherwise never touches. That cost disappears where the record already exists in Zotero's own shape.

**Path B, the connector server**, which is what a browser extension uses (source read 2026-09-04 from `server_connector.js` at zotero/zotero main; treat this as source-read, not as a measurement on this machine):

- `POST /connector/import?session=<id>` with an RIS, BibTeX, or CSL JSON body runs Zotero's own import translator, saves the items into the currently selected library or collection, and returns HTTP 201 with the created items as JSON, including their keys.
- For a DOI, the body comes from content negotiation on doi.org (`Accept: application/x-research-info-systems` or `application/x-bibtex`). For an identifier-less document, the agent composes a minimal RIS from the metadata it holds. In both cases Zotero parses the record and the vault never builds Zotero item JSON, which is the whole attraction of this path.
- Its costs are real and were found by review, not assumed. **No consent dialog guards it**, so anything running on the machine can write to the library; that is a fact about Zotero, not a property this design adds, but it means Path B gives up the audit trail Path A's authorization provides. A session id is minted by the import call and cannot be reused, so attaching a file afterwards is constrained by that session's lifetime and by the numeric item id the attachment endpoint expects, which is not the item key the rest of this design uses. And the save target is whatever collection is selected in the Zotero window.

**Chosen 2026-09-07: Path A**, and Zotero's own documentation settles it rather than the balance of costs. `POST /api/local/authorize` is the documented mechanism for local writes — a dialog naming the application with *"Allow"*, *"Always Allow"* and *"Deny"*, returning `{"key": "<32-character key>", "remember": false}`, with keys that *"can't be created in advance"*. Path B is what browser **connectors** use, and Zotero 10 now drops requests that look browser-like *"without a response"*, which says who that path is for.

Three mechanics the attended trial must carry, all read 2026-09-07. `Zotero-Server-ID` is **required** on writes — 428 without it, 412 on mismatch — where on reads it is optional. **Measured 2026-09-07 against the test instance, and the ordering is the useful part**: a write with no id returns 428, a write carrying the *other* instance's id returns 412 `Zotero-Server-ID does not match this server`, and only a write carrying the correct id reaches 401 `API key required`. A wrong id is therefore refused **before any key is presented**. That is what was measured; whether Zotero evaluates identity before authentication as a matter of architecture is an inference these three status codes do not settle, and nothing here needs it. So writing to the wrong Zotero is not a hazard this design must guard against with a rule — the server refuses it, unauthenticated, before the request means anything. **The guard holds only for a client that sends the id it recorded**, as §5 requires. A client that reads the live header and echoes it back passes against any instance, which is the obvious lazy implementation and defeats the whole mechanism; `research_vault/zotero.py` today sends no id at all, so this is a fork the plan has to take deliberately rather than a hypothetical. `Zotero-Write-Token` is supported but *"cached in memory, so they're forgotten when Zotero restarts"*. And the authorize endpoint is rate-limited to five dialog-producing requests per minute.

**The undo question is new and belongs in the trial.** Zotero 10 supports undo *"for modifications to existing objects — creating or permanently deleting an object isn't undoable (trashing is, since it just sets the object's `deleted` flag)"*, and the `undoAction` label that makes a change undoable is a **plugin API** parameter on `saveTx`, unavailable to an HTTP caller. So an item created over the local API is not undoable by construction. That is not an argument for Path B, which has no consent gate at all — but the trial should see it rather than discover it.

Original reasoning, retained: Path A for creating the record, because the authorization dialog is the audit trail this vault wants and the item JSON for a fresh record is small. Path B stays available for the case where a translator does the work no mapping of ours could. The plan settles it with one attended trial of each, and records which endpoints each needs.

**Path A was tried end to end on 2026-09-07 and it works.** The dialog offered the three documented buttons; **Always Allow** returned a 32-character key with `remember: true`, persisted to the profile's `localAPIKeys.json` under the `appName` the caller supplied. That one key then carried a `POST /collections`, a `POST /items`, a `PATCH` trashing the item (204) and a `DELETE` (204), with no re-authorization. **Better BibTeX assigned the new item a citation key by itself** — `sitting2026`, under the library's own pattern — but **not in time to ride the create response**, and that detail is a step in the plan. The response body carries `citationKey: null`; the key was there when the item was re-read six seconds later. Better BibTeX fills keys after a delay it calls `fillKeyAfter`, two seconds by default on both instances. So §2.3's capture cannot read the citation key off the create response: it re-reads the returned item keys, and needs a wait or a retry for the field to be populated. This is what the attended trial existed to find; asserting the convenient version would have shipped a capture step that intermittently wrote a note with no citation key.

Capture runs on the returned keys at once, so adding and capture are one conversation.

Record creation by the person (browser connector, drag-in, add by identifier) stays valid and reaches capture the same way.

No citation-key pinning. Under item-key identity a later key change is a rename, not a lost address.

Open, to be settled in the plan with one attended write test on a scratch collection: where the new item lands. Under Path A the caller names the collection. Under Path B the target is whatever is selected in the Zotero window, and `/connector/getSelectedCollection` reports a numeric collection id that the local API keys by string, so reading the target back is not a one-line check.

## 4. Compile

Chosen outputs (2026-09-04): a source page and cross-source concept pages, both in the adopted wiki's own shape. Claims with locators are dropped as a vault syntax; the requirement that a quoted passage be traceable to a page and byte-checkable on demand is carried by Zotero annotations captured at ingest. Charting belongs to the scoping review and is not a compile output, so the earlier requirement D2 (a caller-supplied per-source template) leaves this set and moves to the scoping review spec.

### 4.1 Requirement set the adoption was screened against

Floors: D1 a per-source page; D3 concept pages with an index, an append-only log, and contradictions kept rather than resolved; D4 the capture of the source file is separable from compiling it, so the tool accepts a raw file another process wrote; D6 installable as a Claude Code plugin or skill as-is; D10 a licence that permits use and modification. Not floors: D5 idempotent re-compile; D7 works on an existing layout or states its own; D8 references a source by a caller-supplied stable id; D9 documents its human gate.

The evidence is in `docs/research/2026-09-04-import-sourcing.md`: 43 candidates read after a triage of 295 discoveries, with every drop counted by reason. The candidate descriptions below are drawn from that note's records. Where its adversarial verify pass had not confirmed a row when this section was written, the note marks it, not this document.

### 4.2 Candidates that meet every floor as-is

Two Claude Code plugins meet D1, D3, D4, D6, and D10 without modification. Both are MIT, both were pushed in the last ten days of August 2026, both are Python underneath.

**AgriciDaniel/claude-obsidian** (v2.1.1, pin `ad67087`, 14,631 stars, 42 commits in 90 days). Integration is a transaction: read-only workers propose a bundle of page writes with expected content hashes, the person approves the plan by its hash, and an engine applies it atomically or not at all. Source pages carry key claims; a claims ledger marks contested claims and refuses contradictory evidence without an adjudication note. Sources enter through a configured inbox directory, and paths outside the vault are refused. Layout: a `wiki/` directory beside an `.obsidian/` directory. The ledger key is a content hash, so the citation key rides in the source locator and the page properties, not in the ledger id. Components at the pin: 15 skills, 3 agents, a SessionStart and a Stop hook, and the `claude_obsidian/` Python engine the skills call. `plugin.json` declares no components, so component discovery is by directory.

**nvk/llm-wiki** (v0.24.4, pin `7c94c9b`, 1,034 stars). Ingest and compile are separate commands: ingest writes an immutable raw page with a fixed six-key frontmatter, compile turns uncompiled raw pages into concept, topic, and reference pages, with a nudge at five uncompiled sources. A raw page written by another process is accepted if it carries those keys, which is the capture-then-compile split of §1 exactly. Layout: `raw/<type>/` and `wiki/`, imposed by a lint that relocates files by their frontmatter. Components: 1 skill (403 lines plus 22 reference files), 27 commands, no hooks.

Candidates that fell on a floor, for the record: ussumant/llm-wiki-compiler (Claude and Codex plugin, no per-source page); kfchou/wiki-skills (contradictions must be resolved before commit, the opposite of D3); pumblus/okf-harness (OKF-native, Apache-2.0, no append-only log by design); the hermes-agent llm-wiki skill (a Hermes skill, not a Claude Code one; its schema template is the strongest pattern donor); atomicstrata/llm-wiki-compiler and SwarmVault (Node CLIs, not plugins); llmwikis.org (prompts and a starter bundle, scripts without a licence).

### 4.3 Proposed adoption: claude-obsidian, generic mode, our names under `wiki/`

**Chosen 2026-09-05.** The compile step adopts AgriciDaniel/claude-obsidian at pin `ad67087`, installed whole, configured through its own mode system, with no edit to the tool. Every accommodation is on this vault's side, which is where nothing binds.

The route the earlier draft proposed, a curated marketplace entry taking four skills and leaving eleven, is **unreachable** and is recorded here so it is not proposed again. Measured against the installed Claude Code 2.1.260: the plugin ships a `plugin.json` at its repo root, so a marketplace entry that also lists components is rejected outright, with the loader emitting "has conflicting manifests: both plugin.json and marketplace entry specify components. Set strict: true in marketplace entry or remove component specs from one location." At `strict: true` the list supplements directory discovery rather than replacing it, which the official reference states as a rule ("Custom paths supplement default directories - they don't replace them"), so all fifteen skills, three agents and both hooks load anyway. A `git-subdir` source pointed at `skills/` would curate, but it severs the `claude_obsidian/` Python engine, and all fifteen skills hard-gate on `$PRODUCT_ROOT/scripts/claude-obsidian.py`, so none of them runs without it. So the plugin arrives whole or not at all, and §4.2's component inventory is what a session carries.

Two ways to subset do exist, both measured, and both are held as fallbacks rather than proposed. **Vendoring** copies the wanted skills into this plugin under a provenance header, the treatment the `agent-plugins` repository already runs for one skill; it drops the hooks by geometry and needs no fork, but the engine gate means the Python package comes too, about fifty files and a megabyte, so the precedent's byte-diff check becomes a tree diff. **An in-tree entry** works because the loader's subtraction branch fires when a marketplace entry's `source` is the string `"./"`, meaning the plugin is the marketplace repository's own root, which is this repository's shape; a probe built to this vault's exact geometry confirmed the listed skills load and an unlisted one is dropped, at the price that the entry's list then governs this vault's own skills too. Neither is proposed now, because the surplus is a context cost rather than a correctness one.

If the surplus does prove intolerable, there is a cheaper intermediate before either: `permissions.deny` on a skill namespace mutes execution, though it saves no context, since the skill still loads.

**Modes.** The tool ships four filing modes, and their folder names are configuration: `generic` (`sources_folder`, `entities_folder`, `concepts_folder`, `sessions_folder`), `lyt`, `para`, and `zettelkasten`. All four validate through `mode_config.validate_wiki_folder`, which requires the first path segment to be `wiki`, so no mode escapes that parent. `generic` is the structural match: its sources-to-concepts split is this vault's per-source-to-cross-source split. Configured names, confirmed by a round trip through `mode get`:

| Layer                           | Path                               | Written by                         |
| ------------------------------- | ---------------------------------- | ---------------------------------- |
| Captured record, one per source | `literatures/<citation key>.md`    | The capture verb, and nothing else |
| Per-source page                 | `wiki/sources/<title>.md`          | The tool                           |
| Cross-source pages              | `wiki/synthesis/<slug>.md`         | The tool                           |
| Entity and session pages        | `wiki/entities/`, `wiki/sessions/` | The tool                           |

`literatures/` stays outside `wiki/`, where the tool provably cannot write: its lint performs no filesystem writes at all, verified byte-identical before and after with `sha256sum`, and its ingest operation is scope-refused from any path outside `wiki/` and `.raw/`. The vault gives up the `wiki/` parent segment and keeps every name inside it. That is the whole accommodation on the layout axis.

`para` mode names the same four things the vault's `projects/` layer does. Whether one tool can carry both layers is left to the workflow-component audit; this spec configures `generic` only.

**What the tool cannot read, and why that is fine.** Of its seven capture adapters exactly one is implemented, and it accepts markdown, text, JSON, CSV, YAML and HTML. PDF, EPUB and images are `metadata-only`, which its own manifest defines as identifying and describing the input without extracting its contents; reading them needs an external runner the repository does not ship. Two consequences, both binding on the seam in §4.4: the compile input this vault writes is **markdown text, never a path to an attachment**, and the tool's own `capture` command is not used at all, because its one working input is the file capture already produces. Zotero's index does the extraction, which is work the tool would otherwise not be able to do.

**What the vault changes, all of it in non-binding territory.** Three conflicts were measured, and each is resolved on our side rather than in the tool:

1. `wiki/index.md` carries six required frontmatter fields; the vault's `okf-structure` check requires a nested `index.md` to carry none. The vault stops applying that rule under `wiki/`. It came from an OKF reading in a non-binding ADR, and the alternative is forking the tool's lint.
2. The tool's `capture` treats a folder named `inbox` as its own drop zone and, run bare, planned to copy this vault's fleeting notes into its raw store. Since `capture` is unused (above), this is avoided by not running it; the plan also records the `--inbox` flag and the `.vault-meta/capture/config.json` key as the belt and braces.
3. `.raw/captured/*.md` copies carry no frontmatter and would fail `okf-frontmatter` wherever the vault's walker reaches them, which it does, since verify rglobs everything but `.git`. The vault gitignores `.raw/` and `.vault-meta/` and excludes them from the walk. Note the tool's `adopt` will not install its own gitignore rules over an existing file without `--force`, so this is the vault's edit to make.

**Tracers, before any compile task is written.** Each is one command on a scratch vault: that the plugin installs whole and the fifteen skills and two hooks are tolerable in a real session; that `mode set` writes the two folder names and `mode get` reads them back; that a compile run over three captured sources writes only under `wiki/` and leaves `literatures/` byte-identical; and that a page the tool writes passes `verify --surface commit` once the two exclusions above are in place.

**If a tracer fails.** The fallback is not nvk/llm-wiki, which the layout probe found worse on every axis: its interior layout is module constants with no configuration path, and one `--fix` pass moved this vault's root `index.md` into a quarantine folder and took the `okf-frontmatter` failure count from zero to thirteen. (Two corrections to §4.2's inventory of it, both measured: it ships twenty-eight commands, not twenty-seven, and its default branch is `master`, so a marketplace entry naming `main` would mislead even though the sha is what resolves.) The fallback is to defer compile to its own spec and ship capture, the linter and setup alone, which is a complete iteration on its own.

### 4.4 The seam between capture and compile

- Capture is the only writer of the literature note and the text cache. It also writes the compile input: a **markdown** file in the tool's configured inbox directory, carrying the attachment's extracted text under a metadata header. Markdown is not a convenience here, it is the tool's only implemented input format (§4.3).
- Where Zotero has no indexed text, or only a partial index, capture writes no compile input and files a finding. Nothing is compiled from an abstract alone, and nothing is compiled from a page range that Zotero truncated.
- The compile input's filename must not equal `<citation key>.md`, and its frontmatter must carry no `title` or `aliases`. Both were measured to make every wikilink to that source ambiguous across two or three files, from the tool's index and from every page it writes, which would bury the real lint signal at one finding per source.
- The tool's `.raw/captured/` store holds a second, content-addressed copy of that text. It is gitignored with the cache and excluded from the vault's walk (§4.3).
- `wiki/synthesis/` is the cross-source layer. That is the same role `synthesis/` was always intended to serve, so the layer keeps its meaning and its name and moves one level down.
- **The per-source page lives in `wiki/sources/`, not in the literature note.** The vault would have preferred one file per source, but the tool's engine has only whole-file create and replace, so it cannot write part of a note another writer owns. Splicing the page in outside the tool would cost the tool its wikilinks, its index entry and its ledger locator, and its next lint would report both as broken. Two files per source, in two trees, is the price of not editing the tool. This is also why the note's free region retires rather than waiting for a splice that is not coming (§6).
- Compile runs when the person asks, after capture, for one source or a batch. The tool's own gate is the human gate: inspect the proposed bundle, approve it by its plan hash, apply it atomically or not at all. No second gate is added.
- The lifecycle linter records the compile input's sha256 in the note. A refreshed note whose input hash changed is reported as needing recompile, which the tool then performs.
- Reading across the boundary already works: a page under `wiki/` carrying `[[<citation key>]]` resolves into `literatures/` with no dead link. Writing across it does not, in either direction, which is what keeps the two writers separate.

## 5. Setup and lint integrity (proposed)

Doctor is setup's linter. It probes Zotero version, Better BibTeX version, local API reachability, server id, translator-format health, fulltext index presence, and the path shim, and reports what it found.

**Provisioning the enrichment plugins is part of setup, and it carries two obligations the sourcing catalogue exposed.** First, each plugin must be verified to load on the running Zotero: they declare version ranges in their manifests, and of the ones catalogued, at least two cap below 10, while `zotero-doi-fix` declares 7.0 to 10 and loads. **Corrected and narrowed 2026-09-07, because the obvious way to run this check does not work.** Reading `strict_max_version` out of the packed `manifest.json` of all 23 installed add-ons finds four that cap below Zotero 10 — `KeepZotero` at `7.0.*`, `Scite` at `8.0.*`, `Zotero TLDR` at `7.0.*`, and `DOI Manager` at `9.0.*` — but Zotero has disabled only the last of them. For the other three `extensions.json` records `targetApplications` of `6.999` to `*`, and all three are installed with `appDisabled` false; two are running. A manifest-derived check would therefore raise **three false alarms out of four findings** on this machine. The field that answers the question is `appDisabled` on the running Zotero, read together with `active`, which is what the decomposition's §6.1 already specifies; this paragraph's earlier version named the wrong plugins and implied the wrong source. A plugin the vault relies on and Zotero refuses to load is a check that silently never runs. Second, the ones that can run automatically must be switched on: the DOI manager hooks Zotero's item-add notifier behind `extensions.shortdoi.autoretrieve` and also sweeps the whole library monthly, and the PMCID fetcher fires on the same notifier behind `extensions.zotero.pmcid.auto`. Without those preferences the work only happens when a person clicks, and an item the agent adds (§3) would never be enriched at all. Installing the plugin is a human wizard step; doctor reports which are present, whether each loads on this version, and whether its automatic mode is on.

The server id has exactly one authoritative home: the running Zotero, which returns it on every response. Doctor reports it and the note's tuple records the value that was live at capture, but nothing caches it as a third copy to be reconciled. The linter never compares two stored copies; it sends the recorded id as a request header and lets Zotero answer 412 (§2.4). That probe costs nothing and needs no key: the id check precedes authentication (§3), so doctor can confirm it is talking to the expected database over the *write* path without holding a write key and without mutating anything.

The standing facts file is deleted rather than repaired. `docs/environment.md` recorded Zotero 9.0.6 and Better BibTeX 9.0.57 while 10.0.1 and 9.0.63 were running (measured 2026-09-04), and it had claimed to be live-verified throughout. Of its eighteen rows, thirteen were either answerable by a probe, measured on a stack that has moved, or about the auto-export this spec retires. The rest were not environment facts at all: two were rationale, one was an interpreter policy that belongs in `pyproject.toml`, and two were Better BibTeX preference readings that no probe can reach. Those five moved to where they are used, in §2.6 and in §9's table. Live values come from `probe`; nothing keeps a second copy to go stale. This removes the failure class rather than adding a check that watches for it, which is the same reasoning §6 applies to the DOI and metadata checks.

Retired from setup: the whole-library auto-export step and its staleness probe. Kept: the human-only wizard steps for Zotero plugin installs.

Every mechanical step in this spec names its linter: setup has doctor; capture, the CSL file, and the text cache have the lifecycle linter through recorded hashes and versions; adding has capture, which runs immediately on the returned keys and reports what it could not read.

## 6. What changes in the existing machinery

Retired by this spec (**confirmed 2026-09-07**):

- the Better BibTeX auto-export contract: observation, byte-compare, the `staleness` verb, the `autoexport` doctor probe, and the `bibliography.py` machinery around them.

  **This is the one retirement where the spec and shipped code disagree, so the cost is stated rather than implied.** Measured 2026-09-07, the contract is live and load-bearing: `scaffold.py:383` has doctor call `observe_autoexport`; `autoexport` sits in **both** `DOCTOR_HARD_UNMATCHED` and `DOCTOR_HARD_UNREACHABLE` (`__main__.py:45-46`), so a persistent mismatch is a hard failure today; `staleness` is a shipped CLI verb; and `bibliography.py` is 763 lines. Retiring means deleting a doctor probe that currently hard-fails, a verb, and most of a module.

  **What makes it right is that the artifact is now obtainable on demand.** §2.3 step 4 regenerates the whole CSL file from one 0.46-second read, so the observe-and-wait machinery guards something we can simply produce. Better BibTeX's documentation also confirms the retirement's premise exactly: `//` is *"a library-name prefix on a collection path"*, not library scope, so `autoexport.add("//")` was never going to register a whole-library export and no RPC route exists. The human-repair text at `bibliography.py:598` — *"a person must create or fix the whole-library Better CSL JSON auto-export in BBT Preferences"* — retires with it, which removes the design's last instruction-to-a-human rung.

- `research_vault/zotero.py`'s hardcoded `base: str = "http://localhost:23119"`. Two Zotero instances now run on this machine at once, distinguished only by port (§9), so the base URL is configuration, not a constant. The default may stay; being unable to name another instance is what retires.

- the note-level screening status, `lint_screening_state`, and the `superseded-note` reason code;

- claim lines as an ingest output, and anchors derived from annotations.

Replaced rather than superseded: the ADR register is non-binding while it is re-derived, so this spec does not negotiate with ADR 0004 or 0005. It states the two positions directly, and whether either becomes a record again is the register's business: identity is the Zotero item key qualified by the server id, and the citation key is the name; capture is the sole writer of the vault's bibliographic record. The text cache is a new derived artifact outside the git boundary, the same class of thing as Zotero's own attachment store. The glossary is rewritten per §1.1, as a fresh derivation rather than an amendment. The supersession of foundation spec §4 is stated once, in the header.

Kept, each with the reason it survives. Existing code is not a reason (see the header), so anything that could not give one went back on the table, and three did.

| Kept                                     | Why it is not adopted or dropped                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Four-state results                       | A reporting discipline, not a component: its whole content is that an outage must never read as a pass and a skipped check must never read as clean. Nothing is available to adopt because nothing external knows what this vault means by a check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Deprecate-never-delete for vault records | Same class. The record's value depends on negative knowledge staying inspectable, which is a rule about writing, not a mechanism.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| The path shim                            | An environment necessity on this machine, not a choice: Zotero returns Windows paths and the vault runs in WSL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Findings and the review queue            | Screened against the adopted tool, and it does not cover the ground. The tool's lint engine writes no files at all by design, so a durable non-blocking finding has nowhere to live in it; its only reaction is to refuse before writing state, which is the opposite of the disposition §2.4 chose. Its source ledger addresses vault-relative paths and HTTPS URLs, so a Zotero item key, version, trash membership or server id cannot be named in it, and six of the seven lifecycle reason codes are inexpressible. It has no acknowledgment concept carrying an actor, a date, a reason and a content hash. Two surfaces would mean neither gets drained, so the vault keeps one and does not add checks the tool already runs under `wiki/`. |
| verify's update-notice check             | Screened, and it stays for a reason worth stating exactly. Zotero detects retractions natively, in partnership with Retraction Watch, and warns at cite time. It exposes that verdict nowhere a client can read: no endpoint, no field in item JSON or `meta`, the flag living in a local SQLite table. So the work is duplicated for the person and unavailable to the vault. Ours stays until upstream exposes it, and asking them to is worth an issue.                                                                                                                                                                                                                                                                                          |

Retired here, on screening rather than on scope:

- **The managed region and the free region below it.** No writer survives. The per-source summary moved to the tool's tree (§4.4), claim lines retire above, and a hook already deny-lists agents from `literatures/` entirely, so no skill could reach the free region even today. No literature note on this machine has ever carried prose there. Against that sat roughly 120 lines of boundary machinery across `notes.py`, `lints.py` and `claims.py`, plus 49 test references across 13 files, whose only purpose was keeping two owners apart in one file. Per-source human prose gets a better home than an unwritten region: a Zotero child note, which capture already reads and now renders (§2.3 step 5). `managed-sha256` narrows to a hash over the body below the frontmatter.
- **verify's `doi` and `metadata` checks.** Screened against the enrichment catalogue in the sourcing note and adopted away. Installable Zotero plugins verify and clean DOIs, retrieve missing ones from Crossref, DBLP, Semantic Scholar and arXiv, resolve PMID and PMCID, and refresh metadata in bulk. They fix the record in the library, so capture's snapshot inherits the correction and the check leaves the vault. The signal survives the move: these plugins report a failure by tagging the item, and §2.2's snapshot carries `tags` for that reason, so an unverifiable DOI is still visible here without the vault making the call itself. This is the design's own premise applied to itself: Zotero is the source of truth, and a check that duplicates what a Zotero plugin does in Zotero is work in the wrong place. Which plugins the vault asks for is a setup step (§5), not code.

Recategorised: `find-sources` is deferred to the search spec, not kept on merit, and the `not-admitted` reason code stays in service because that skill still files it.

Reason codes joining the registry: `re-keyed`, `merged`, `trashed`, `deleted`, `database-changed`. `drift` and `outage` already exist. `superseded-note` leaves with the note status. Three producers named elsewhere in this spec still owe a code and the plan assigns them: the propagation residue check (§2.5), capture's no-text and partial-index case (§4.4), and the recompile-needed report (§4.4).

Frozen, dispositioned in the workflow-component audit: the quote check, factcheck, stance links, the publish gate, project-flow, and archive-source.

**The offline commit contract is amended, and that is a real cost.** `research_vault/templates/git/pre-commit` opens by calling itself offline and runs verify with `--offline`. Invariant 5 puts the lifecycle linter at pre-commit, and that linter needs a running Zotero, so either the hook becomes online for this one check or the linter runs at capture and verify only. This spec proposes the former, because a commit is the last moment before a stale projection becomes history, and prices it here rather than leaving it implicit: the hook gains a check that reports UNREACHABLE and does not block when Zotero is closed, which §2.4 already specifies.

**The synthesis layer keeps its folder and gains a writer.** `synthesis/` is the compiled concept-page layer (§4.4), so nothing about the vault's structure contract changes: the folder stays in `scaffold.VAULT_DIRS`, `structure.check_tree` keeps attesting it, `structure` keeps mapping it to the `synthesis` note type, the Bases view and the templates keep working. What changes is who writes it. Today `synthesis-conventions` tells an agent to compose pages by hand carrying `[[citation key#^claim-id]]` claim links; under this spec the adopted compile step maintains them and claim links are no longer the arrangement's currency. That skill is therefore rewritten, not frozen, and its 2+-source threshold and index-registration rules are checked against whatever the adopted tool enforces so the vault does not carry two conflicting sets of rules for one folder.

Stance links remain frozen pending the workflow-component audit. They live on synthesis pages, so the audit decides whether the compiled pages carry them or whether disputes are recorded the adopted tool's way.

The same reservation covers every frozen check that reads what this spec stops producing. Retiring claim lines removes the only producer the quote check, the evidence-layer check, and factcheck consume, and it leaves `lint_claim_immutability` guarding a syntax nothing writes. Those four are frozen rather than retired, and none of them is removed until the audit says so, so the retirement of claim lines is a retirement of the ingest-side producer only, not of the checks downstream of it.

Rewritten skills: import-source and setup-vault. Others untouched until their own spec.

## 7. Testing (proposed)

What already exists (source read 2026-09-05): `tests/conftest.py` builds the `tmp_vault`, `fixture_vault`, and `net_vault` fixtures and gates the live suite in `pytest_collection_modifyitems`, which skips the `live` marker without `RV_LIVE=1` and the `live_net` marker without `RV_LIVE_NET=1`. Seven marked live tests already run under that gate, across `tests/test_cli_live.py`, `tests/test_zotero.py`, `tests/test_scaffold_live.py`, `tests/test_archive.py`, and `tests/test_verify_cli.py`. Every network response in the 24,571-line suite is hand-written and injected with `monkeypatch`: `FakeTransport` in `tests/test_zotero.py` cans the Better BibTeX JSON-RPC payloads, `_fake_network` in `tests/test_archive.py` cans the HTTP ones. Nothing is recorded from a live service, and the repository holds no recorded response JSON; its `*.manifest.json` files are form-owner manifests.

- Offline suite on hand-written fixtures, in the style the suite already uses. The transition payloads are small: `?since=0&format=versions` and `items/trash?format=versions` are flat `{"KEY": version}` maps, and `/items/<key>/fulltext` is `{content, indexedPages, totalPages}` or `{content, indexedChars, totalChars}` (§9). Five of the six transitions can be taken from a live read: drifted, re-keyed, merged via `dc:replaces`, trashed, and database changed. Database changed no longer needs a second Zotero, because §2.4 detects it as a `412 Precondition Failed`, which one read-only GET carrying a bogus `Zotero-Server-ID` header elicits from the running instance (measured 2026-09-05, §9). **Deleted** is the one that cannot be read live, because a read-only session cannot remove an item from both the library and the trash; its fixture is hand-edited from a recorded baseline, one key dropped from the versions map and from the trash map — **or recorded properly from the test instance during the attended sitting below**, which is preferable, since a hand-edited fixture asserts what the transition looks like rather than observing it.

- What a recorder would add is only the bulky item JSON, and it is not needed to reach the six fixtures. If the plan still wants record-and-replay, `pytest-recording` or `vcrpy` in the dev extra does it with redaction hooks; neither is a dependency today. A recorder verb inside `research_vault` is the more expensive option, because it puts a production surface under the coverage, mutation, and form gates for good.

- **A second Zotero now exists to test against (set up 2026-09-07), and it removes one of the two obstacles above, not both.** The WSL instance runs from profile `~/.zotero/zotero/rfnse7tz.default` against `~/Zotero/`, unsynced from the author's account, bound to port 23129 by `extensions.zotero.httpServer.port` while the production instance keeps 23119. Both answer at once and report different server ids, so the *database changed* transition no longer needs a bogus header to simulate — it can be produced by pointing a request at the other port, which is the real condition rather than a stand-in for it. What the test instance does **not** remove is the attended step. Two things a session cannot do alone: `POST /api/local/authorize` raises a dialog on the author's screen and stays out of bounds, and the test profile carried **zero** add-ons at first. **Better BibTeX 9.0.63 was installed there on 2026-09-07**, matching the production version, and its citekey pattern set to match the library's; the instance now reports 1,455 keyed bibliographic items, so the capture leg has its binding and only the authorize dialog remains attended.

- **The test profile stays minimal, and grows only on evidence.** Production runs 23 add-ons; the test profile runs one, Better BibTeX, because that is the only one the capture leg binds to. The rest are not mirrored **by decision, not by omission**: the enrichment plugins write to the library on item-add and on scheduled sweeps — the DOI manager behind `extensions.shortdoi.autoretrieve`, the PMCID fetcher behind `extensions.zotero.pmcid.auto`, the attachment scanner by tagging — and every measurement this section depends on is a before-and-after read of item versions and full-text state. A plugin editing items in the background does not make the substrate more realistic; it makes the readings unattributable. **Lanes 2 and 3 decide what else goes in**: lane 2 screens all 23 installed plugins plus the author's named not-installed candidates, and lane 3 produces the provisioning artifacts. A plugin earns a place in the test profile when one of those lanes gives a reason to exercise it, and not before.

- **The sitting ran on 2026-09-07 and every leg below is now recorded rather than planned.** Authorize returned a remembered key; a human edit in the interface moved item `ALKT2NF7` from version 0 to 1708, discharging §9's caveat; a re-index of attachment `568BWIT7` — confirmed to have happened, because `.zotero-ft-cache` was rewritten while the PDF was untouched — moved no version at all, confirming §2.6 by observation; a Path A create returned an item already carrying a Better BibTeX citation key; and a trash-then-permanent-delete produced the **deleted** fixture. Recorded snapshots and the log live outside the repository, at `~/zotero-test-sitting/findings/`, because they are 130 KB of machine-specific maps whose value is the conclusions drawn here.

  **What the deleted fixture actually shows, and one thing it does not.** A trashed item is absent from `/items?format=versions` while `GET /items/<key>` still answers 200; permanent deletion then removes it from `items/trash?format=versions`. So *trashed* and *deleted* are indistinguishable in the items map alone and the trash map is what separates them — which is why the linter reads both. **The gap is real and larger than an unrecorded transition.** No snapshot holds the scratch item while it was live, because it was created after the baseline and trashed before the next capture. So the recorded pair shows a key *appearing* in the trash map having never been in the items map — not the shape the linter meets, which is a key *leaving* the items map as it *enters* the trash. The signature above therefore comes from a live probe rather than from the recorded maps, and **the recorded pair cannot be replayed to exercise the trashed transition**; only *deleted* replays correctly. One more snapshot between the create and the trashing fixes it, and the wizard already holds the item key needed to take it.

- **The attended sitting, as one list rather than four interruptions.** Back up `~/Zotero/zotero.sqlite` with the instance stopped; authorize once and choose **Always Allow**, without which the key is single-use (§9, key lifetime) and the first write consumes it while the remaining legs stall on 401; edit one item's metadata in the interface and re-read its version, confirming through the human path what §9 has so far measured only through a plugin write; re-index one attachment and re-read `fulltext?since=0` for that key, which confirms §2.6's finding from the other direction; and trash then permanently delete one item, which produces the **deleted** transition this section says a read-only session cannot reach. That is the whole substrate in one sitting, and every fixture below can be recorded from it.

- Live legs against Zotero 10 on a scratch collection, under the existing `live` marker: capture round-trip, linter classification after a live edit, one connector import of an RIS string. The first write leg runs once with the author present.

- Gates unchanged: coverage, mutation baseline, form owners.

## 8. Open points

1. Connector save target (§3).
2. Preprint-to-published supersession: a Zotero relation, a vault fact, or out of scope for this iteration.
3. Whether the OCR path (scanned PDFs with no text layer) is in this iteration. The premise that the endpoint flags them is false: all 3 scanned PDFs in this library return 200 with `indexedPages` equal to `totalPages` and 62 to 126 characters of masthead (§9), so a status-code test does not find them and a content test has to name a floor.
4. Verb and file names, after the glossary is fixed, including the CSL file's own name.
5. The `index.md` collision: the vault's OKF bundle root reserves it, and the adopted tool writes its own under `wiki/`. Whether one file serves both, or the bundle root moves, is settled in the plan against the tool's actual behaviour.
6. Where the per-source page finally lives. §4.4 settles it at `wiki/sources/` for this iteration. The earlier alternative, splicing it into the literature note's free region, is closed rather than deferred: §6 retires that region, so a later revisit would have to reintroduce it rather than reuse it, and it would have to answer why prose belongs there instead of in a Zotero child note, which capture now renders.
7. The exact shape of the compile input file: which metadata fields head it, and how the literature note path is carried so the tool's source page can be traced back to the citation key.
8. Whether the compiled layer's pages carry a `type` the vault's own structure check accepts: the adopted tool writes its own page shape and its lint requires a `type` on every wiki page, while `research_vault/structure.py` requires a non-empty `type` on every non-fleeting note and derives no expected value for `wiki/`, so the open part is whether the vault takes the tool's vocabulary as it stands or adds `wiki/` to `_FOLDER_TYPES` and then demands one derived value.
9. How a compiled page is prevented from citing a source the vault never captured: no linter in this spec ties a `[[citation key]]` or `[@citation key]` under `wiki/` back to a `literatures/<citation key>.md` that capture wrote, and the adopted tool's own dangling-link check resolves against every file in the vault rather than against the captured set.

### 8.3 Open points on the filename rule (2026-09-07)

§2.1 chooses `literatures/<citation key>.md`, so the filename is the source's **name** while its identity is the item key. That is safe only because §2.5 propagates a re-key, and §2.5 is `proposed` — measured 2026-09-07, `research_vault/` contains zero occurrences of `propagat`, `re-key`, `rekey` or `rename log`. Until it is built, a re-key forks the vault the same way `obsidian-history-vault` forks notes under Zotero Integration, which §2.8 records as a hazard this design avoids. It avoids it on paper.

21. **The rename log has no site.** §2.5 requires the old→new mapping to be *"appended to a rename log so a later reader can follow any key backwards"*, and never says where it lives. A file under `system/`, a frontmatter field, or the review queue are all consistent with the text. Its own linter depends on reading it, so the propagation pass cannot be built without this answered.

22. ~~Seven items have no citation key.~~ **Withdrawn 2026-09-07 — the problem does not exist.** Re-measured against the right population: **1,452 top-level bibliographic items, zero without a citation key.** The keyless six are five standalone attachments and one standalone note, which Better BibTeX does not key because they are not bibliographic items, and which §0 excludes from ingest because they are not sources. §2.1's filename rule has no gap, and this row is removed rather than carried so that no later reader builds a handler for a case capture cannot reach.

    *Recorded for the next reader who reconsiders the filename*: the immutable-filename case rests on the observation that a person reads titles rather than filenames, which holds inside Obsidian. It fails outside it. `[[citation key]]` resolves by basename for every reader; an item-key filename resolves only through Obsidian's alias index, which no other consumer has — including this repository, whose `notes.note_path` is a string join used at **eight call sites**. Choosing the item key would mean maintaining a citekey→item-key index, a second home for a fact §2.2 keeps in one place.

### 8.2 Carried in from foundation spec §5 on its deletion (2026-09-07)

Eighty-eight elements were checked against successors, code, `CONTEXT.md`, skills and the decomposition. Fifty-two are carried, six retired, seven frozen, three already open items. **Twenty had no home.** The seven that are rules rather than rationale are carried here; the rationale is recorded in the commit and left to git.

14. **The acknowledgment scope has no hash.** §5 gave `fixity-sha256` two roles, and named the second explicitly: fixity anchor *and* **ack-scope trigger** — the content hash an acknowledgment is scoped to, so that when the target changes the finding re-fires rather than staying acked. `CONTEXT.md` still defines an acknowledgment as *"scoped to the target's content hash"*, and §6's Kept row defends the concept against the adopted tool on exactly that ground — *"no acknowledgment concept carrying an actor, a date, a reason and a content hash"* — without saying which hash. **Open**: which hash scopes an ack once `fixity-sha256` splits, and the decomposition's §15.21 records the same gap from the doctor side.

15. **`[retraction-ack:: <code>]` was defined once, for three consumers.** The syntax survives in `skills/evidence-conventions`, but §5's rule — that the update-notice gate, `publish` and `evidence-conventions` **all key on that single syntax** — does not. The single-definition-site rule is what keeps three consumers from drifting apart; nothing now states it. **Open**, and cheap to close by naming the definition site.

16. **The reason-code vocabulary spans four surfaces, and only two are covered.** §5 required one controlled vocabulary shared by import-source holds, deprecations, acknowledgments and not-admitted records, *"so reasons are countable and Bases-queryable, never free text alone"*. Holds and acknowledgments are covered; deprecations and not-admitted records are not, and no successor states the span. **Open.**

17. **A human acknowledgment does not clear `[failed-verification::]`, though §5 says it should.** Measured: `_mutate_marker` has exactly two callers — `verify.py:834` stamps on UNMATCHED, `:836` clears on MATCHED. The ack path (`cmd_ack` → `inbox.append_ack`) never reaches it. This is an **unimplemented rule, not a lost one**: deleting §5 silently would have hidden a live gap between a written contract and the code.

18. **Web-Annotation context capture is unowned by the document that specifies capture.** §5 required quote claims to capture `exact` plus 32-character `prefix`/`suffix` at extraction time, on the reasoning that it is *"nearly free at capture, unreconstructable later"*. This document has zero occurrences of `selector`, `prefix`, `suffix` or `TextQuoteSelector`. The cost asymmetry is the whole argument and it is real — if capture does not take the context, no later pass can. **Open**, and it interacts with decision 28: annotations defer to lane 4, so the anchor whose durability §5 guaranteed is deferred with them.

19. **The re-anchoring cascade lost its trigger.** §5 deferred it *"gated on a consistency check, only if the slice shows prose-parsing bottlenecks."* The surviving register entry carries the deferral without the condition, so nothing says when to revisit it. **Open** — a deferral with no trigger is indistinguishable from an abandonment.

20. **A claim in §5 that is simply false, corrected rather than carried.** *"Exact inline-field syntax is validated against Dataview/Bases parsers at build time."* No such validation exists: `research_vault/claims.py:14` is our own regex, checked against nothing, and `frontmatter.py`'s "Bases-compatible" comment is an assertion rather than a test. Recorded so the belief does not outlive the section that stated it.

### 8.1 Carried in from foundation spec §4 on its deletion (2026-09-07)

Foundation spec §4 was deleted as a controlled demolition, this document being its named successor. Four of its five bullets are carried above or retired in §6. These four were not, and are recorded here rather than lost with the section.

10. **The BibLaTeX projection.** §4 ruled that when LaTeX drafting starts, the BibLaTeX file is a research-vault-regenerated projection over tier 2 — `bibliography.json` filtered by `literatures/` membership, converted CSL→BibLaTeX — of the `log.md` artifact class, machine-projected and regenerated whole. Better BibTeX stays sole writer of the one universe file and no second auto-export exists. **Out of scope here**: it belongs to the `long-form` step, which the decomposition's §15.11 records as having no lane and no issue. Recorded so the ruling survives the step's homelessness.

11. **ZotLit-convention notes.** §4 required capture to emit them, on a stated hedge: *"so ZotLit remains adoptable later as an interactive UI, never a dependency."* This document drops the hedge without arguing against it, and the 2026-09-07 screen (§2.8) makes that pointed — ZotLit clears C1, C2 and C4, keys its notes on `zotero-key`, which is this design's identity, and co-owns through `%%zt-managed%%` markers. **Open**: whether §2.2's note shape keeps the hedge or spends it. Spending it is defensible; doing so silently is not.

12. **The presence marker.** §4 allowed exactly one write-back, a MarkDB-Connect `has-vault-note` tag, and banned content write-back. The obligation — making vault presence visible in Zotero — is answered "nothing for now" by the decomposition's §15.16, and §2.8 finds MarkDB-Connect writes one tag and no files, so it composes with this design rather than competing. **Deferred, with an owner.**

13. **Two-way sync: the gate has opened.** §4 deferred it *"behind the Zotero 10 local-writes gate,"* re-ruled 2026-08-23 as a deferral rather than a permanent ban, to be *"revisited when local writes exist and the per-event approval path is proven."* **Both conditions are now met**: Zotero shipped local API writes on 2026-07-27, and §3 specifies Path A's per-application authorization dialog. This document uses that path to *add* items and stays one-way for content. That remains the right scope for this iteration — but the deferral's trigger has fired, so it is an open question now rather than a dormant one, and deleting §4 without recording that would have discarded it silently.

### 8.4 Better BibTeX findings that outlive this iteration (2026-09-07)

Read from BBT's documentation at the **v9.0.63 tag**, matching the measured build, and probed live.

23. **Export configuration is pinnable, even though the version is not.** BBT supports `preferencesOverride` and `postscriptOverride`: a `preferences.json` or `postscript.js` placed **in the export directory** overrides the global preference for that export alone — committable, reviewable, and immune to another project's global setting. The decomposition's **decision 12** says Zotero-side components are declared and observed, never pinned; that is right about the *version* and too pessimistic about export *behaviour*. **Not taken now, because the auto-export retirement removes the export directory it would live in.** Recorded so that if a configured export ever returns, the pin is known to exist rather than rediscovered.

24. **BBT can run git in an auto-export target, and that collides with this repository's commit rule.** The hidden `git` preference (`off` / `config` / `always`) plus `git config zotero.betterbibtex.push true` makes BBT run `pull`, `add`, `commit` and `push` in the target repo itself. `AGENTS.md` requires commits by explicit pathspec and forbids touching another session's uncommitted files; BBT would commit whatever any concurrent session had staged. **Forbidden here**, and doctor checks for it — the retirement removes our own target, but the preference is machine-local and a human could point an unrelated export at this tree.

25. **A deprecated `Extra` syntax exists that our snapshot would not make legible.** BBT still honours `{:pmid: 123456}` alongside `PMID: 123456`, describing it as *"supported but considered deprecated"* and still showing it in its docs. BBT destructures both into CSL; §2.2 copies `Extra` verbatim and parses neither, so an item in the brace form would carry a value in `bibliography.json` and an opaque string in the note. **Measured 2026-09-07: zero of 440 items with a non-empty `Extra` use the brace form**, so this is a watch-item rather than a case to build for.

26. **Two pull-export aliases in BBT's own documentation are wrong on 9.0.63.** `pull.md` lists `csljson`, `yaml`, `yml` and `cslyaml` as valid `format` values; measured, several return HTTP 500. The working spelling is `json`. Recorded because `csljson` is the intuitive choice and the documentation invites it — and because §2.3 step 4's route is undocumented, so its neighbours' documentation is the only guide a later reader has.

27. **BBT's export worker is self-described as experimental and self-disabling.** *"a wild departure from how translation works in Zotero"*, and a single error silently flips the whole instance to foreground exports until a human intervenes. Every pull-export request defaults to the worker path. Not a blocker — but if capture's CSL read becomes slow rather than failing, this is the first thing to check.

## 9. Facts and how each was established (this machine)

Each row records how it was established. Counts are readings on the date given, not constants: the library is live, and a read later on 2026-09-05 returned 2,844 objects at `Last-Modified-Version: 565` against the 2,846 at 540 below.

| Fact                                                              | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Established                                                                         |
| ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Zotero                                                            | 10.0.1 on Windows, local API enabled, reachable from WSL2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | live probe 2026-09-04                                                               |
| Better BibTeX                                                     | 9.0.63; JSON-RPC `api.ready` answers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-04                                                               |
| `Zotero-Server-ID`                                                | present on every local API response; `6LpvURP2E933` on this machine                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-04                                                               |
| `Zotero-Server-ID` scope                                          | stored in the Zotero database, so it follows the user's data rather than the installation and stays the same across restarts and upgrades; a different id means a different database, and therefore a different set of object versions and keys. It is not minted per launch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | documentation (Zotero local API, Server ID)                                         |
| `Zotero-Server-ID` precondition                                   | a mismatched id returns `412 Precondition Failed`, and that check guards `/api/` paths only; `/connector/ping` and `/better-bibtex/json-rpc` answer 200 with a mismatched header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | live probe 2026-09-05                                                               |
| `/items?since=0&format=versions`                                  | 2,846 keys in one response, `Last-Modified-Version: 540`; children and annotations included                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | live probe 2026-09-04 and 2026-09-05                                                |
| `/items/top?since=0&format=versions`                              | 1,459 keys, top level only. The 1,387 children are invisible to it, so the linter reads `/items`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | live probe 2026-09-05                                                               |
| object versions                                                   | 2,724 of the 2,846 objects sit at version 0; only 122 are nonzero, in the range 4 to 540. A `?since=` window above 0 therefore returns almost nothing and can never surface a version-0 object. Per-key comparison of recorded against live over the full map is the mechanism, and its premise — that an edited object moves off 0 — is **verified for a plugin write, 2026-09-07 on the test instance**: an item at version 0 was modified through Better BibTeX and took version 1695, the library's next. A human edit in the Zotero interface is the design's actual case and travels the same save path, but it was not exercised, so §7 keeps that as a confirmation step. The analogous premise is still outright false for full text (see `fulltextItems.version` below), which is the asymmetry §2.6 turns on | live probe 2026-09-05                                                               |
| child versions                                                    | children carry their own versions and do not roll up: of 318 sampled parent-child pairs, 26 have a child version above its parent's (attachment `FFGWM8B6` at 540 under item `27MDQHEI` at 539). A parent's version is not a signal for its children                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-05                                                               |
| `items/trash?format=versions`                                     | 267 keys                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | live probe 2026-09-04                                                               |
| `/deleted`                                                        | 404, no endpoint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | live probe 2026-09-04                                                               |
| citation keys                                                     | full scan of `/items/top`: 1,452 distinct citation keys across 1,459 top items, with no duplicate. The items carrying none are **not sources**: re-measured 2026-09-07, all 1,452 top-level *bibliographic* items have a citation key, and the keyless remainder is five standalone attachments and one standalone note, which Better BibTeX does not key and §0 excludes from ingest. The earlier phrasing counted the wrong population and implied a gap in §2.1's filename rule that does not exist                                                                                                                                                                                                                                                                                                                  | live probe 2026-09-05                                                               |
| `relations.dc:replaces`                                           | on 126 of 300 sampled top items (merge history)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | live probe 2026-09-04                                                               |
| attachment child JSON                                             | carries `md5`, `mtime`, `contentType`, `filename` for stored files only. Full sweep: 1,311 of 1,312 attachments carry `md5` (1,052 `imported_url`, 259 `imported_file`); the one exception is the library's single `linked_url` attachment, which carries no `md5`, no `mtime`, and no `filename`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | live probe 2026-09-05                                                               |
| `/items/<attachment>/file/view/url`                               | returns a `file:///D:/Zotero/storage/...` URL for a stored file; 400 for the `linked_url` attachment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-04 and 2026-09-05                                                |
| `/fulltext?since=0`                                               | 1,350 attachment entries, 432 of them at version 0, maximum 35,545. Fulltext versions are a different number space from item versions, whose library maximum is 540; the two are never comparable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | live probe 2026-09-05                                                               |
| `/items/<attachment>/fulltext`                                    | sweep of all 1,350 listed keys: 1,303 return 200 with `indexedPages` and `totalPages`, 15 return 200 with `indexedChars` and `totalChars`, 32 return 404. The response body carries no `version` key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-05                                                               |
| fulltext coverage                                                 | a 404 is not the only not-indexed signal. 43 of the 1,303 page-indexed attachments have `indexedPages < totalPages`, because Zotero stops PDF indexing at `extensions.zotero.fulltext.pdfMaxPages` (default 100 in the installed 10.0.1) and serves the truncated text as 200. All 3 scanned PDFs in this library return 200 with `indexedPages` equal to `totalPages` and 62 to 126 characters of masthead, so a status code does not separate a usable index from an unusable one                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-05; the cap by source read                                       |
| annotations                                                       | 1 highlight in the library, carrying `parentItem`, `annotationPageLabel`, `annotationText`. `/children` returns it from neither end: the item's `/children` returns the attachment alone, and the attachment's returns nothing. It is reachable by an `itemType=annotation` query, on `/items` and on `/items/<key>/children` from either the item key or the attachment key, and from Better BibTeX `item.attachments`, which nests annotations per attachment with key, type, page label, and text                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-05                                                               |
| `format=csljson`, `format=bibtex`                                 | HTTP 500 on every item and on `/items/top`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | live probe 2026-09-04                                                               |
| BBT `item.export` Better CSL JSON                                 | works; entry carries `citation-key`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-04                                                               |
| connector endpoints                                               | `/connector/import`, `/connector/saveAttachment`, `/connector/saveStandaloneAttachment`, `/connector/getSelectedCollection`, `/connector/ping` (200)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-04                                                               |
| `POST /api/users/0/items` malformed                               | 400; does not discriminate write support                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | live probe 2026-09-04                                                               |
| `/items/<key>/children`                                           | returns attachments and notes; returns no annotation from the item key or the attachment key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | live probe 2026-09-05                                                               |
| `/items/<key>/children?itemType=annotation`                       | returns the annotation from either the item key or the attachment key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | live probe 2026-09-05                                                               |
| `/items/<key>/children?includeTrashed=1`                          | accepted, HTTP 200                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | live probe 2026-09-05                                                               |
| `/items/top?format=json`                                          | 1,458 items in one response; 1,452 carry `citationKey`, 531 carry `relations.dc:replaces`. This is the linter's third read, the one the versions maps cannot answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-05                                                               |
| `/items?since=0&format=versions`                                  | 2,846 keys, 54 KB, 0.045 s, children and annotations included; `/items/top` returns 1,459 and hides every child. Counts are readings on their date and the library moves                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | live probe 2026-09-04, re-timed 2026-09-05                                          |
| Local API write support                                           | Zotero 10+ accepts POST, PUT, PATCH and DELETE for items, collections and saved searches, plus tag deletion, full-text writes and file uploads. This is what §3's Path A rests on; the malformed-POST row below establishes nothing about it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | documentation, read 2026-09-04                                                      |
| Write authorization                                               | A client asks for a key at runtime with `POST /api/local/authorize` carrying `{"appName": ...}` and a `Zotero-Server-ID` header. Local keys are unrelated to zotero.org keys and cannot be created in advance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | documentation, read 2026-09-04                                                      |
| The consent dialog                                                | Zotero names the application and offers Allow, Always Allow, and Deny. This dialog is §3's reason for preferring Path A                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | documentation, read 2026-09-04                                                      |
| Key lifetime                                                      | The response carries `{key, remember}`. Always Allow makes the key reusable indefinitely; otherwise it is single-use, consumed by the first validated write, and the client re-authorizes on 401. The plan's attended trial cannot be specified without this                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | documentation, read 2026-09-04                                                      |
| Key scope and revocation                                          | Keys live in the profile and are unscoped: one key writes to any library the person can edit. All remembered authorizations are revoked from Settings, Advanced. §3's phrase "per-application" is right; "per-library" would not be                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | documentation, read 2026-09-04                                                      |
| Denial and rate limit                                             | Denial returns 403 with `{"denied": true}`; at most five dialog-producing requests a minute are accepted, then 429 with `Retry-After`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | documentation, read 2026-09-04                                                      |
| `Zotero-Server-ID` on writes                                      | Mandatory on every write including the authorize call; omitting it is 428 Precondition Required. On reads it is optional and a mismatch is 412, which is what §2.4 uses                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | documentation, read 2026-09-04                                                      |
| Version partitioning                                              | Zotero directs clients that store data between runs to partition it by server id, and to treat Web API data as its own partition. This is why the provenance tuple carries the id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | documentation, read 2026-09-04                                                      |
| `item.regenerate_key`                                             | Recomputes keys from current metadata through the same path the Regenerate right-click item uses, and returns an old-to-new mapping per input. It mutates, which is why §2.5 never calls it for detection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | source read 2026-09-04, Better BibTeX `content/json-rpc.ts` at cfdba6ac             |
| `item.export` batch behaviour                                     | Takes a list of citation keys; missing or duplicate keys raise INVALID_PARAMETERS naming them, so one bad key fails the batch. §2.3 step 4 calls it per item for that reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | source read 2026-09-04, Better BibTeX `content/json-rpc.ts` class NSItem            |
| Retraction detection                                              | Zotero flags retracted items natively in partnership with Retraction Watch and warns at cite time, keeping the flag in a local `retractedItems` table. It is exposed through no endpoint and no field: `/retractions` and `/retracted` both 404, and item JSON and `meta` carry nothing. This is why §6 keeps the update-notice check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | live probe and source read 2026-09-05, `chrome/content/zotero/xpcom/retractions.js` |
| Better BibTeX key stability                                       | "Regenerate key when item changes" is unchecked, and pattern changes leave existing keys untouched, so a re-key needs a deliberate act. This bounds how often §2.5's propagation runs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | read from Better BibTeX preferences 2026-08-22; no probe exposes it                 |
| Test instance                                                     | A second Zotero 10.0.1 runs in WSL from profile `~/.zotero/zotero/rfnse7tz.default` against `~/Zotero/`, unsynced from the author's account, local API on. `extensions.zotero.httpServer.port` is not shipped in `prefs.js` and defaults to 23119 (`/usr/lib/zotero/app/omni.ja`, `defaults/preferences/zotero.js`); it was set to 23129 so both instances bind at once. Server id `Tdoqsn2J4q4h`, against `6LpvURP2E933` on the production instance                                                                                                                                                                                                                                                                                                                                                                    | live probe 2026-09-07                                                               |
| Two instances, one lineage                                        | Both report `rel="alternate"` to the same account. Of 1,458 top-level item keys present in both, **1,457 carry a different version in each**. Of those 1,457 the production instance reports 0 for 1,379 and a nonzero version for the other 78 (maximum 559), while the test instance reports a nonzero version for every one of them, up to 1,681. Same identity, unrelated versions: this is §2.2's qualified-version rule observed rather than argued                                                                                                                                                                                                                                                                                                                                                               | live probe 2026-09-07                                                               |
| Local API versions vs database versions                           | The API's numbers are its own. On the test instance the API reports item versions in 0-1,681 while the database's `items.version` column runs to 14,595 and `libraries.version` is 35,774                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | live probe and SQLite read of a copy, instance stopped, 2026-09-07                  |
| `?since=` semantics                                               | Behaves as documented on both instances: `/items?format=versions&since=N` returns exactly the objects above version N (production 2,844 at `since=0`, 121 at `since=1`, 0 at `since=10000`). Version 0 means untouched since the counter began, not broken                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | live probe 2026-09-07                                                               |
| `fulltextItems.version`                                           | **All 1,142 rows at 0** on the test instance, `synced` 0 for 1,126 and 4 for 16; the column's schema default is 0. That instance indexed all its attachments locally, so local indexing assigns no version and the nonzero values elsewhere are sync leftovers. `fulltext?since=1` accordingly returns 0 entries there while 1,115 exist. This is why §2.6 ignores the field                                                                                                                                                                                                                                                                                                                                                                                                                                            | live probe and SQLite read of a copy, instance stopped, 2026-09-07                  |
| Full-text corpus fetch cost                                       | 19 ms per attachment over the local API, so all 1,367 listed attachments are about 26 s and 97 MB. A full re-read is cheap enough that §2.6 needs no incremental mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | live probe 2026-09-07, 50-attachment sample extrapolated                            |
| Write guard chain                                                 | Measured, and ordered: no `Zotero-Server-ID` returns **428** `Zotero-Server-ID not provided`; the wrong id (the other instance's, or garbage) returns **412** `Zotero-Server-ID does not match this server`; the correct id returns **401** `API key required -- POST /api/local/authorize to obtain one`. A wrong id is refused **before any key is presented**, so §5's probe costs nothing. These three codes do not establish the evaluation order as such, and no part of the design rests on it. Confirmed on PATCH, POST and DELETE                                                                                                                                                                                                                                                                              | live probe 2026-09-07, no-op body and stale precondition so nothing mutated         |
| `/deleted`, second instance                                       | 404 on the test instance as well, so the absence is a property of the local API rather than of one database's state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | live probe 2026-09-07                                                               |
| Test profile add-ons                                              | Empty at first. Better BibTeX 9.0.63 was then installed from the production profile's own XPI and its citekey pattern set to match; the instance reports 1,455 keyed bibliographic items                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | installed and verified 2026-09-07                                                   |
| Test and production profile divergence                            | Deliberately aligned: Better BibTeX 9.0.63 on both, and the test profile's `citekeyFormat` set to the library's `authEtal2.lower.len + year`. Better BibTeX had also written `fillKeyAfter` 1 into the test profile against its own default of 2, which production leaves unset; the override was removed so both run the default. What still differs is that production carries 22 further add-ons and the test profile carries none                                                                                                                                                                                                                                                                                                                                                                                   | `prefs.js` read and aligned 2026-09-07                                              |
| Citation keys are stored, not derived                             | A copy of this library in a fresh profile, with an empty Better BibTeX key store and a **different** citekey pattern (BBT's default against the library's `authEtal2.lower.len + year`), returned all 1,452 shared keys unchanged, disambiguation suffixes included. Setting the pattern to the library's changed none of them either. Two patterns, both directions, zero re-keys                                                                                                                                                                                                                                                                                                                                                                                                                                      | live probe 2026-09-07                                                               |
| A version-0 object moves off 0                                    | Measured twice on the test instance. Through Better BibTeX's RPC: an item at version 0 took version **1695**. Through **a human edit in the Zotero interface** (2026-09-07 sitting): item `ALKT2NF7` moved from version 0 to **1708**. The design's actual path is the second, and it is now exercised rather than inferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | live mutation and attended sitting 2026-09-07                                       |
| A regenerate that returns the same key does not touch the version | `item.regenerate_key` returned the existing key unchanged on two items and left both versions untouched (13, and 1695 on a second call against the item just re-keyed). Whether Better BibTeX recomputed and got the same answer or short-circuited an unchanged item is **not** settled by these calls; the two are indistinguishable from outside. What holds either way is §2.5's reason for never using the call as a detector: it cannot report what *would* change without changing it                                                                                                                                                                                                                                                                                                                            | live mutation on the test instance 2026-09-07                                       |
| Manifest ranges do not predict loadability                        | Of the 23 installed add-ons, four declare `strict_max_version` below Zotero 10 in their packed manifest (KeepZotero 7.0, Scite 8.0, Zotero TLDR 7.0, DOI Manager 9.0), but only DOI Manager is `appDisabled`. The other three are recorded in `extensions.json` with `targetApplications` 6.999 to `*`. A manifest-derived check gives three false alarms out of four; `appDisabled` on the running Zotero is the answering field                                                                                                                                                                                                                                                                                                                                                                                       | XPI manifest and `extensions.json` read 2026-09-07                                  |
| Add-on inventory                                                  | 23 installed: 13 active, 9 user-disabled, 1 `appDisabled` (`zoteroshortdoi@wiernik.org` 1.6.0)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `extensions.json` read 2026-09-07                                                   |
| The auto-export is live                                           | Production preferences carry an enabled Better BibTeX auto-export to a `bibliography.json` under the repository's former name, `type` library, status `done`. §6 retires this                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `prefs.js` read 2026-09-07                                                          |
| Re-index moves no version                                         | Attachment `568BWIT7` re-indexed from the interface during the 2026-09-07 sitting. The re-index provably ran — `.zotero-ft-cache` was rewritten at 12:14:50 while the PDF itself was untouched since June — and the full-text version stayed **0**, the content sha256 and its 33,822 bytes identical. §2.6 confirmed by observing a re-index rather than by inferring from the all-zeros table                                                                                                                                                                                                                                                                                                                                                                                                                         | attended sitting 2026-09-07                                                         |
| Extracted text is not portable                                    | The same attachment yields 34,024 bytes on production and 33,822 on the test instance, with different hashes. Two machines indexing one PDF do not produce identical text, so a recorded `sha256` is machine-local                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | live probe 2026-09-07                                                               |
| Trash empties itself                                              | `extensions.zotero.trashAutoEmptyDays` defaults to 30 and is unset here. The trash held 504 keys at 11:47 and 212 at 12:02, and a database copy from 11:42 still held 487 `deletedItems` rows — so about 280 items left during a restart in this session, on this instance, with no user action. Deletion is not always a deliberate act (§2.5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | live probe and SQLite read of the pre-session backup, 2026-09-07                    |
| A trashed item leaves the items map                               | While trashed, `II7E6CVR` was absent from `/items?format=versions` and present in `items/trash?format=versions`, and `GET /items/<key>` still answered 200. Trashed and deleted are indistinguishable in the items map alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | attended sitting 2026-09-07                                                         |
| The deleted transition, recorded                                  | Trash returned 204 and permanent delete returned 204; the key left the trash map and the item stopped resolving. §7's one unreachable fixture is now observed rather than hand-edited                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | attended sitting 2026-09-07                                                         |
| Path A, end to end                                                | **Always Allow** returned a 32-character key with `remember: true`, persisted to the profile's `localAPIKeys.json` under the caller's `appName`. One key then carried `POST /collections`, `POST /items`, a trashing `PATCH` and a `DELETE` with no re-authorization                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | attended sitting 2026-09-07                                                         |
| A new item is keyed, but not in the response                      | The item created over the local API came back with `citationKey: null`; Better BibTeX had filled `sitting2026` when it was re-read six seconds later, consistent with its `fillKeyAfter` delay of 2 seconds. Capture must re-read rather than trust the create response                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | attended sitting 2026-09-07                                                         |

## 10. Deferred

Substrate absence; web pages, repositories, product documentation; the claims layer and pinpoint citations (long-form spec); charting (scoping review spec); search (PRISMA-S spec); the workflow-component audit. The `import-source` and `find-sources` skill names are current names and are renamed with the glossary.
