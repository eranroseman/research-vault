# Rethink audit: CONTEXT.md (the meaning layer)

Date: 2026-08-21. Method: `/rethink-audit` rungs, prior-art inline (the artifact's operative contract is local: the domain-modeling skill's CONTEXT-FORMAT — is-definitions + `_Avoid_`, opinionated, no implementation detail; background: DDD ubiquitous language, "the model and the language move together"). Verdict: **sound in structure; five-item content gap** — the file aged a few hours faster than its decisions.

## Consumers (requires)

The format contract; `/improve-codebase-architecture` (reads this file for seam vocabulary at the post-slice pass); onboarding agents/humans (the ruled meaning/governance split — this file is the meaning layer, terminology.md the governance layer); Plan D's vault-glossary seed (projects this content into the vault). Requirements: coverage of load-bearing domain terms; format conformance; `_Avoid_` carries collisions and legacy names; no mechanism detail; **true at HEAD**; vault-projectable; single root context.

## Gap found (all authoring-order accidents, none dead constraints)

1. **`Import` had no entry** — a core verb defined only inside Admission's `_Avoid_` parenthetical, though the admission/import boundary IS the trust boundary.
2. **`Bibliography export` had no entry** — the citekey universe appeared only as three words inside the System-folder entry.
3. **`Closing check` had no entry** — the doctrine's central distinction (holds a surface vs warn-tier informs), used undefined by the Acknowledgment entry ("any *closed* check").
4. **OKF appeared cold + altitude drift** — "structured as an OKF bundle" leaned on unglossed jargon and hard-coded the mechanism where ADR 0001's decision is survivability (OKF severable).
5. **Schema leak** — Verified event's `{by, at, check}` field list; the meaning is "dated, attributed record of which check passed."

All five patched 2026-08-21 (same commit as this report).

## Deliberately out — the stop-line

`managed-sha256`, reason codes, selectors, closing-set contents, `fixity-sha256`: mechanism vocabulary whose home is the spec. The `%%hk-managed%%` marker stays in (recognizable identifier of the concept, not its mechanism). The mechanism rule is what keeps this file projectable into the vault without dragging harness internals along.

## Trade-off / flip condition

Every added term nudges the glossary toward the spec's territory. If Plan D's seed wants *user-operational* vocabulary (how to ack, how to screen), that is a second, vault-local glossary — not growth of this one.
