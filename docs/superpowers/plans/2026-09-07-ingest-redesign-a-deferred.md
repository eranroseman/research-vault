# Part A deferred findings

Minor findings raised by the per-task reviews of `2026-09-07-ingest-redesign-a-capture.md` and
deliberately held out of their tasks' fix loops. None blocks a merge; all are cosmetic or
documentation-level.

Rows are appended by the controller in the same commit as the task that recorded them, so the
record is written at the moment the finding is deferred rather than assembled at the end.

**Task 20's whole-branch review dispositions every row.** Each ends **fixed**, naming the commit,
or **declined**, with the reason written into the row. Rows still open after that review become
one repository issue (`gh issue create --label ready-for-agent`) with the open rows as its body.
No issue is filed before then: `docs/agents/issue-tracker.md` asks that the tracker carry findings
no spec or plan will claim, and this plan claims all of these.

## Deferred minors

| #   | File                                                                                                                    | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Task | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `research_vault/bibliography.py.manifest.json`                                                                          | Lists mutate4py entries for ten helpers deleted with the auto-export contract. Not a constraint violation: sidecar deletion is mandated only for a deleted module, and this module survives. The gate rewrites its own sidecar on its next run.                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2    | **declined** (operator ruling, plan `a179b7d`): the `.py.manifest.json` sidecars retire wholesale with mutate4py in Plan W Task 25, which runs on `main` between Part A's merge and Part B; hand-editing a sidecar the tool rewrites is churn.                                                                                                                                                                                                                                                                                                   |
| 2   | `research_vault/lints.py.manifest.json`                                                                                 | Lists `func/_note_status` and `func/lint_screening_state`, both deleted. Same class as row 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 4    | **declined** (operator ruling, plan `a179b7d`): the `.py.manifest.json` sidecars retire wholesale with mutate4py in Plan W Task 25, which runs on `main` between Part A's merge and Part B; hand-editing a sidecar the tool rewrites is churn.                                                                                                                                                                                                                                                                                                   |
| 3   | `research_vault/__main__.py.manifest.json`                                                                              | Lists entries for `cmd_archive_source`, `cmd_import_note` and other deleted commands. Same class as row 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 1, 5 | **declined** (operator ruling, plan `a179b7d`): the `.py.manifest.json` sidecars retire wholesale with mutate4py in Plan W Task 25, which runs on `main` between Part A's merge and Part B; hand-editing a sidecar the tool rewrites is churn.                                                                                                                                                                                                                                                                                                   |
| 4   | `research_vault/verify.py.manifest.json`                                                                                | Lists entries for `_archive_outcomes` and other deleted functions. Same class as row 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 1    | **declined** (operator ruling, plan `a179b7d`): the `.py.manifest.json` sidecars retire wholesale with mutate4py in Plan W Task 25, which runs on `main` between Part A's merge and Part B; hand-editing a sidecar the tool rewrites is churn.                                                                                                                                                                                                                                                                                                   |
| 5   | `research_vault/notes.py:157`                                                                                           | The pass-through-fields comment still names `archive-url` as an example of a field the renderer carries through unchanged. Accurate for historical notes under ADR 0003, but nothing writes the field going forward.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 1    | **fixed** at `8c7eb35` (Task 4 removed `MANAGED_FIELDS`; `grep -rn archive-url research_vault/` is empty) — verified by the whole-branch fix wave.                                                                                                                                                                                                                                                                                                                                                                                               |
| 6   | `research_vault/lints.py` (`_origin`)                                                                                   | The `fallback or RepoPath(rel)` truthy branch is unreachable: every surviving caller passes `""`. **Created by Task 4**, which deleted `lint_screening_state`, the only caller that passed a truthy fallback — not pre-existing. Collapsing the signature was judged scope creep at the time.                                                                                                                                                                                                                                                                                                                                                                                              | 4    | **fixed** at `e37296b`: `_origin(vault_root, note_file, claim)`, the `fallback` parameter collapsed; the one caller updated.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 7   | `tests/test_canonical_form.py:11-13`                                                                                    | The docstring still says "The tenth, `vault/index.md`" after the deletion of `literature.md` left nine templates. The count was corrected in the first sentence and not the second.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 5    | **fixed** at `e37296b`: the docstring says "The one held out" — no count to go stale (the row's own "nine" had gone stale too).                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 8   | `tests/test_notes.py:115`                                                                                               | `test_generated_metadata_is_substantive_canonical_content` builds `generated` as a bare string rather than the `{by:, at:}` mapping `notes._valid_generated` defines. Not a no-op, but a mapping-aware exemption would slip past it.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 5    | **fixed** at `e37296b`: `generated` built as the `{by, at}` mapping via `must_replace`; `_valid_generated` accepts it and moving `at` changes the content.                                                                                                                                                                                                                                                                                                                                                                                       |
| 9   | `.github/workflows/quality.yml:52-56`                                                                                   | The CRAP-ceiling comment still describes `_bump_generated` in `archive.py`, a module Task 1 deleted. Pre-dates Task 3, which was told to make only the change its brief named. **Part B Task 4 removes the whole block**, so this needs attention only if Part B slips.                                                                                                                                                                                                                                                                                                                                                                                                                    | 3    | **declined**: Part B Task 4 removes the whole `continue-on-error` block and its comment.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 10  | `research_vault/verify.py:558`                                                                                          | `_mutate_marker` skips `wiki/` via `path.relative_to(vault_root).parts[0] == "wiki"`. Correct today, and the brief's printed `startswith("wiki/")` would never have fired — but it silently assumes every caller passes an absolute `vault_root`. `decode_repo_path(relative).startswith(b"wiki/")` would not.                                                                                                                                                                                                                                                                                                                                                                             | 6    | **declined**: latent — `_plan_state` always passes an absolute candidate root; grouped with row 55 in the follow-up issue.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 11  | `tests/` (coverage gap)                                                                                                 | No test exercises `verify` or `structure.check_reserved` against the shape `scaffold_vault` actually produces: `tmp_vault` hand-creates `wiki/concepts`, while a freshly scaffolded vault has no `wiki/` until first compile. The dangling `[wiki/](wiki/)` link and "Read `wiki/index.md`" in that interval are plan-mandated and crash-free; the untested shape is the finding.                                                                                                                                                                                                                                                                                                          | 6    | **open → issue #129**: a test of `verify`/`structure.check_reserved` against the shape `scaffold_vault` produces.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 12  | `tests/test_inbox.py:1210`                                                                                              | `f"citation_key/x{separator}/2026-08-16"` is snake_case where the finding-id grammar is kebab (`citation-key/`, pinned at `tests/test_publish.py:917`). The value is deliberately hostile junk fed through `contextlib.suppress`, so nothing depends on it — but it is the one place a spelling class leaked, and it reads as an example of a shape the codebase never produces.                                                                                                                                                                                                                                                                                                           | 7    | **declined**: hostile junk by design; not a shape the code produces.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 13  | `research_vault/__main__.py:539`                                                                                        | `add_argument("citation_key")` puts snake_case in the CLI usage line. The `dest` must stay `citation_key` — argparse translates `-` to `_` only for optionals, so a kebab `dest` would make `args.citation_key` raise `AttributeError` — but `metavar="CITATION_KEY"` would make `--help` agree with the placeholder the skills document. No test reads usage output.                                                                                                                                                                                                                                                                                                                      | 7    | **fixed** at `273e246`: `metavar="CITATION_KEY"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 14  | `research_vault/notes.py:92-94`                                                                                         | `f"{new}:"` is a live `re.sub` replacement template, so a `new` containing a backslash or `\g` would be read as an escape rather than inserted literally, while `old` is properly guarded by `re.escape`. Brief-verbatim and all callers pass literals, so this is asymmetry rather than a live bug; `lambda _: f"{new}:"` closes it.                                                                                                                                                                                                                                                                                                                                                      | 7    | **fixed** at `e37296b`: `lambda _: f"{new}:"`; a backslash or `\g` in `new` is inserted literally (pinned).                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 15  | `mutation-baseline.txt` (`_citation_key_hash`)                                                                          | The row `isinstance(first, str) and first -> isinstance(first, str) or first` does not match current source at either occurrence (`verify.py:222`, `:234`), which read `isinstance(first, str) and re.fullmatch(...)`. **Pre-dates Task 7** — the identical mismatch existed at `deb7056^` under the old name, verified against the committed tree. Harmless to CI, since `new_survivors = found - baseline` means a row that cannot match source can never manufacture a failure — but it silently suppresses a real survivor for that string, so it is a latent coverage hole rather than cosmetic.                                                                                      | 7    | **homed in Plan W Task 25** (operator ruling, plan `a179b7d`): the baseline is rewritten over every module there, between Part A's merge and Part B; no Part A `--update-baseline` commit.                                                                                                                                                                                                                                                                                                                                                       |
| 16  | `tests/test_zotero.py:76`                                                                                               | `test_search_carries_citation_key` keeps its name while now asserting `["citekey"]`, the Better BibTeX wire key. The docstring resolves the tension in prose; the function name does not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 7    | **fixed** at `a1210c6` (Task 12's client rewrite deleted `search` and the test) — verified by the whole-branch fix wave.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 17  | `docs/terminology.md` §4.2                                                                                              | The section drops `fixity-sha256` while `research_vault/verify.py:219,231` still reads it and `lints.py:559` lists it among the machine-owned fields, so a spelling in live use has no governed row. §4.2 has no parity test, so nothing catches it. Plan-mandated and transient — decision 21 deletes the branch at Task 18.                                                                                                                                                                                                                                                                                                                                                              | 8    | **fixed** by Task 18 (`e22f1b2`, decision 21): `fixity-sha256` is read nowhere in `research_vault/`; `_MACHINE_OWNED_FRONTMATTER_KEYS` is `notes.CAPTURE_FIELDS`; §4.2 needs no row.                                                                                                                                                                                                                                                                                                                                                             |
| 18  | `docs/adr/0003-deprecate-never-delete.md:7`                                                                             | Still reads "Synthesis prose is freely rewritable" while `CONTEXT.md:61` now marks `synthesis layer, synthesis note` as `_Avoid_`. A retired term surviving in an authority-list document — the same class of defect that motivated this ADR's amendment. Left because the brief's Step 3 says "Nothing else in the file moves"; the implementer flagged it rather than silently leaving it.                                                                                                                                                                                                                                                                                               | 8    | **PROMOTED to Task 19** (plan `1a01905`): the replacement sentence is printed in Task 19's Files block and `docs/adr/0003-deprecate-never-delete.md` is on its commit pathspec. Task 20 does not disposition this row; Task 19's review does.                                                                                                                                                                                                                                                                                                    |
| 19  | `research_vault/zotero.py:141`                                                                                          | A 400 from the local API is a definite refusal (spec §9: a malformed `POST /items` answers 400) but falls through `_local` to `UNREACHABLE`, so `add` would report an outage for a bad payload. The same four-state class as the two defects fix round 1 closed, one route over.                                                                                                                                                                                                                                                                                                                                                                                                           | 9    | **PROMOTED to Task 17** (plan `18e464d`): `_local` maps 400 to `ZoteroError(UNMATCHED, "local API 400 for <path>: <body>")`, with a printed test that `add` reports `mismatch — local API 400 …` rather than an outage. Task 20 does not disposition this row; Task 17's review does.                                                                                                                                                                                                                                                            |
| 20  | `research_vault/zotero.py:64-83`                                                                                        | `base_for` tolerates a malformed `.research-vault/machine.json` so `doctor` can still report it, but the tolerance is global: every verb now falls back silently to `DEFAULT_BASE` when the file that says "use 23129" is unparseable. A typo could route `capture`/`add` at production instead of the test instance. Narrow it to `doctor`, or have `main()` report it for every other verb.                                                                                                                                                                                                                                                                                              | 9    | **PROMOTED to Task 13** (plan `18e464d`): `base_for(..., *, strict=True)` raises `ZoteroError(UNMATCHED, "machine.json unreadable: …")`, `main()` passes `strict=args.command != "doctor"` and exits 2, and doctor keeps the tolerant read because its `machine-config` probe is the reporter. Task 20 does not disposition this row; Task 13's review does.                                                                                                                                                                                     |
| 21  | `research_vault/zotero.py` (header reads)                                                                               | `_http` does `dict(response.headers)` and every reader (`server_info`, `_version_header`, `authorize`'s `Retry-After`) looks up exact-case. HTTP headers are case-insensitive per RFC 9110. Risk is empirically nil today — Zotero sends the observed case and the live legs pass — but a case-altering intermediary would silently degrade rather than fail: `_version_header` would return `None`, reading as "no version" rather than as a distinct failure.                                                                                                                                                                                                                            | 9    | **open → issue #129**: case-insensitive header lookup (keep the `HTTPMessage` or lower-case the keys); risk empirically nil on both instances.                                                                                                                                                                                                                                                                                                                                                                                                   |
| 22  | `research_vault/zotero.py` (`trash_versions`)                                                                           | Rests on the spec's own measurement with no `docs/research/` record behind it, unlike every other route the client reads. A spec measurement with no corpus entry is the shape that let Task 7's wrong Better BibTeX wire key survive three parties. Backfill a record for parity.                                                                                                                                                                                                                                                                                                                                                                                                         | 9    | **declined**: claimed by Part B's write leg, whose Files block appends the `items/trash?format=versions` record.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 23  | `research_vault/zotero.py:309-311`                                                                                      | The post-decode `payload.get("denied")` check is unreachable for status 403 now that fix round 1 handles 403 entirely above it, and Zotero denials are only ever 403. Dead but inert.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 9    | **declined**: a `200 {"denied": true}` is not ruled out by the records; the check is defensive, not dead.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 24  | `research_vault/__main__.py:85`, `tests/test_cli_live.py:31`, `tests/test_zotero.py:121,131`, `tests/fakes.py:15,65,77` | Test and CLI hygiene from Task 9, grouped: a dead `getattr(args, "base", DEFAULT_BASE)` default now that `main()` always sets it; a dead-port test using `127.0.0.1:1` against `docs/testing.md:40-44`'s "bind an ephemeral port" (~5s per run under WSL2); two tests missing the `assert fake.calls == []` sibling that `:160` uses to prove a precondition fired before any request; the fake raising `UNREACHABLE` for an unregistered RPC method where real Better BibTeX answers an error envelope the client types `UNMATCHED`; and fixture defaults that are the real production server id and zotero.org user id, which are brief-printed pinned values but read like a contract.  | 9    | **open → issue #129** (grouped CLI/test hygiene; the dead-port test is live-only).                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 25  | `research_vault/fulltext.py` (`verdict`)                                                                                | Four edge-label quirks, all brief-verbatim: a dict carrying neither the page pair nor the char pair is labelled "malformed — indexedChars pair missing" though neither was present; `indexed > total` is untested and silently treated as complete rather than flagged; the "empty" label is uniform however far under the 500-character floor the content falls; and `_render`'s `int(response[name])` would raise uncaught on a non-numeric page or char value. None changes a verdict's boolean outcome.                                                                                                                                                                                | 10   | **open → issue #129**, grouped with the malformed-response class (review I-5); none changes a verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 26  | `tests/test_templates.py:111-112`                                                                                       | The block comment above the AGENTS.md byte-pin still enumerates four machine surfaces after fix round 1 expanded the pinned paragraph to five. The comment's actual claim — that no enforcement mechanism holds uniformly across them — remains true, since `inbox/review-queue.md` is still not guard-covered the way `fulltext/`, `literatures/` and `log/` are; only the enumeration undercounts. Introduced as a side effect of the fix, just outside its diff hunk.                                                                                                                                                                                                                   | 10   | **fixed** at `e37296b`: the comment enumerates the six surfaces the pinned paragraph names.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 27  | `research_vault/notes.py` (`LedgerUnreadableError`)                                                                     | Derives from `Exception` where `notes.InvalidCitationKeyError(ValueError)` sets a precedent for a specific builtin base, and `RuntimeError` is the conventional choice for an environmental failure. No live difference: no `except RuntimeError` clause exists anywhere in `research_vault/` or `tests/`, and the load-bearing property — not `OSError`/`ValueError`, so a broad `except` cannot re-collapse it — holds under both. Plan and code agree on `Exception`; recorded rather than churned.                                                                                                                                                                                     | 11   | **declined**: no live difference; plan and code agree on the base class.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 28  | `research_vault/notes.py` (the raise message)                                                                           | Interpolates the relative `LEDGER_PATH` constant rather than the resolved ledger path, so a log line alone does not show which vault root was in play. Capture normally runs one vault per invocation, so the cost is small; the plan's printed form uses the constant and two tests pin it.                                                                                                                                                                                                                                                                                                                                                                                               | 11   | **declined**: constant message pinned by two tests; one vault per run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 29  | `research_vault/notes.py:376-377`                                                                                       | Inside a well-formed `sources` dict, a record that is not a dict, or whose `origin` is not a dict, is skipped silently by `isinstance` guards. Brief-printed and by design, so not a defect — but it is the remaining quiet-skip surface in `compiled_pages` after rounds 1 and 3 closed the four ledger-level ones, and the place to look first if a page that should embed ever does not.                                                                                                                                                                                                                                                                                                | 11   | **declined**: by design; the docstring names it as the quiet-skip surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 30  | `research_vault/lifecycle.py:73-74`                                                                                     | An item key present in BOTH the versions map and the trash map reads `current`: its own trash membership is only consulted when it is absent from versions, while spec §3.4 step 4 says "any key in the trash set". Safe today because the default `versions` route excludes trashed items (research record `:996-997`), so the two are mutually exclusive on the measured shape — but the reading depends on that default rather than on the spec's rule.                                                                                                                                                                                                                                 | 12   | **declined**: the spec's rule holds on the measured shape (the default versions route excludes trashed items).                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 31  | `research_vault/lifecycle.py:49-53`                                                                                     | `read_live` raises `AttributeError` on a top-items payload carrying `data: null`, which `lint_lifecycle`'s `except ZoteroError` does not catch — so a malformed payload takes the whole verify run down instead of filing an outage. An `isinstance(data, Mapping)` guard, or raising `ZoteroError("malformed top items…")`, files it as the outage it is.                                                                                                                                                                                                                                                                                                                                 | 12   | **fixed** at `a6b4b86` (review I-5): `read_live` guards `data`/`relations` with `isinstance(…, Mapping)`; a `null` *row* remains unguarded — in the follow-up issue.                                                                                                                                                                                                                                                                                                                                                                             |
| 32  | `skills/evidence-conventions/SKILL.md:83`                                                                               | The `not-admitted` row names two conditions; there are now three. The vault-level API-off 403 reaches it through `lifecycle.blocked`, and so does a 404 on a versions route via `error.result`. Plan-mandated code choice with an undocumented condition.                                                                                                                                                                                                                                                                                                                                                                                                                                  | 12   | **PROMOTED to Task 15** (plan `ddf497d`): the `not-admitted` row names the third condition, a typed refusal arriving via `lifecycle.blocked`. Task 15's review owns it.                                                                                                                                                                                                                                                                                                                                                                          |
| 33  | `research_vault/lifecycle.py:174`                                                                                       | `lint_lifecycle` mutates the caller's `client.server_id`. Harmless for Task 13, which sets it to the same value first; worth a docstring line because Task 17's `add` needs the live id for `authorize`, and a helper that quietly rewrites a caller's client is the kind of thing found by debugging rather than by reading.                                                                                                                                                                                                                                                                                                                                                              | 12   | **fixed** at `a6b4b86`: `lint_lifecycle`'s docstring says it sets `client.server_id` and that capture reads it back.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 34  | `research_vault/lifecycle.py:100-129` (`_drift_detail`)                                                                 | Two value-level branches remain untested after fix round 1 reached 100% statement coverage: a cached file present with a differing sha256 (the right operand of `:124`'s `or` never evaluates True, and its output string is identical to the already-tested absent-file case, so the gap is redundant rather than a hole), and a child without a string `key` at `:109-111` (a pre-existing mypy-forced defensive guard against a value the Zotero API contract never sends). Both named by the implementer rather than implied covered.                                                                                                                                                  | 12   | **declined**: redundant gap; the value-level branches are covered by the statement-level pins.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 35  | `docs/testing.md`                                                                                                       | Does not mention the `_no_zotero_socket` guard. The guard's own error text names "the offline suite", which points a reader at `conftest.py`, so this is one sentence under "The suite" rather than a gap that misleads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 12   | **fixed** at `e37296b`: one sentence under "The suite" in `docs/testing.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 36  | `research_vault/capture.py:313` (`resolve_keys`)                                                                        | Resolves citation keys through `/items/top`, which excludes trashed items, **before** the `standing` refuse check — and `refresh_all` enumerates citation keys rather than item keys. Reproduced by the reviewer: a trashed item requested by citation key, and any `--all` refresh after a re-key, report `not-admitted — … is not in the library` and file a hold **contradicting the linter's `trashed`/`re-keyed` on the same run**. Spec §2 says a key change is a rename, not a lost address. Recommended: resolve requested keys through `item_key_of` first and enumerate item keys for `--all`. Plan-mandated; a candidate for Task 14 or 17 rather than the whole-branch review. | 13   | **PROMOTED to Task 15** (plan `e56a4ec`, superseding `ddf497d`): `resolve_keys` gains a `known` argument so a requested key that is a captured note's citation key resolves through its tuple, and the linter's verdict reaches it because `standing` is already keyed by item key (`capture.py:306`). `requested` stays as built — citation keys — so no row's target changes; an earlier draft of this promotion said `refresh_all` would enumerate item keys, which was wrong and was corrected against the branch. Task 15's review owns it. |
| 37  | `research_vault/capture.py:391`                                                                                         | `library_name or "My Library"` hardcodes the library name whenever no item was read this run, **against decision 13's own "never hardcoded"**. `top_items` envelopes carry `library.name` and `resolve_keys` already reads them, so the value is available. Plan-mandated.                                                                                                                                                                                                                                                                                                                                                                                                                 | 13   | **PROMOTED to Task 15** (plan `e56a4ec`): `_regenerate_csl` takes an optional `library_name` and goes straight to `item.export` over the captured keys when no envelope was read this run, so the `My Library` hardcode goes. Task 15's review owns it.                                                                                                                                                                                                                                                                                          |
| 38  | `research_vault/capture.py:359-364`                                                                                     | The mid-loop 412 return skips `stamp.stamp_types` and `okf.regenerate_log` for notes already written in that run, so `log.md` is stale until the next clean run. Plan-mandated; the notes themselves are correct, only the derived log lags.                                                                                                                                                                                                                                                                                                                                                                                                                                               | 13   | **PROMOTED to Task 15** (plan `ddf497d`): a mid-run 412 breaks instead of returning, so `stamp_types` and `regenerate_log` run for the notes already written before the vault row closes the list. Task 15's review owns it.                                                                                                                                                                                                                                                                                                                     |
| 39  | `tests/test_capture.py:288`                                                                                             | Commits the real test-instance server id `Tdoqsn2J4q4h` as a literal, against the Global Constraint that no server id is a committed constant. Printed by the brief, and `tests/fakes.py:15` already carries the production id from Task 9 — so the constraint has two standing exceptions, both introduced by printed code.                                                                                                                                                                                                                                                                                                                                                               | 13   | **RESOLVED by the plan** (`ddf497d`): the machine-local constraint now records that a fixture's canned server id is a test value rather than a machine-local fact, naming both `tests/fakes.py`'s id and Task 13's, so the rule is not read as broken by them. The socket guard is what keeps a fixture id from ever matching a live answer. No action.                                                                                                                                                                                          |
| 40  | `research_vault/capture.py:79` (`read_item`)                                                                            | Indexes `c["key"]` where `_attachment_tuple` uses `.get`, so a child without `key` is a `KeyError`; and `fulltext.path_for`'s bare `ValueError` for a non-`ITEM_KEY` attachment key is not in the per-item catch tuple. Both are the malformed-response class, both in the brief's printed shape.                                                                                                                                                                                                                                                                                                                                                                                          | 13   | **fixed** at `a6b4b86` (review I-5): `read_item` filters a child without a `str` key.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 41  | `research_vault/capture.py` (assorted)                                                                                  | Hygiene grouped: `capture()` has no docstring (the module docstring carries the contract); `test_database_changed_aborts_before_any_write` asserts the outcome list rather than that nothing was written; and a `linked_file`-only attachment carrying an index is reported SKIPPED "no attachment to read", consistent with the brief's "no stored attachment" but worth knowing since Zotero does index linked files.                                                                                                                                                                                                                                                                    | 13   | split — `capture()` docstring **fixed** in `a6b4b86..3fd6087`; "asserts nothing was written" **open → issue #129**; the `linked_file` index **declined** (consistent with the brief; Part B's lane).                                                                                                                                                                                                                                                                                                                                             |
| 42  | `research_vault/scaffold.py` (`_path_shim_probe`, `_bbt_probe`, `_compile_tool_probe`)                                  | Reason-string hygiene grouped, from Task 16's quality review: the `path-shim` UNMATCHED reason is the bare resolved path (say `<path> does not exist`; brief-printed `str(local)`); the C1 branch's `str(error)` for a malformed `machine.json` names neither the file nor the fault class; a `compile-tool` record without `gitCommitSha` prints `… at , pin is ad67087`.                                                                                                                                                                                                                                                                                                                 | 16   | **fixed** at `e37296b`: `<path> does not exist`; the C1 branch names `machine.json path_map: <FaultClass>: <error>`; a record without `gitCommitSha` is UNMATCHED with a reason that says so.                                                                                                                                                                                                                                                                                                                                                    |
| 43  | `research_vault/scaffold.py` (`_installed_plugins`)                                                                     | The body is never asserted: `tests/test_doctor.py`'s autouse `_hermetic_host` replaces it in every test, and `tests/test_okf.py`'s one unpatched `doctor()` call asserts names only. One test under a patched `Path.home()` covering a missing file, malformed JSON and a non-dict `plugins` would pin the shape checks.                                                                                                                                                                                                                                                                                                                                                                   | 16   | **open → issue #129** (doctor coverage: `_installed_plugins` under a patched `Path.home()`).                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 44  | `tests/test_doctor.py` (coverage)                                                                                       | Untested branches after two fix rounds: a required add-on that is `inactive` (only `missing` and `appDisabled` are covered); `write-guard` UNREACHABLE (a transport failure on the guard POST); a present-but-non-numeric `Total-Results` (`"N/A"`, excluded by `.isdigit()`; only the absent-header case is pinned); and `attachment listing malformed: expected a list` (`scaffold.py:542-547`).                                                                                                                                                                                                                                                                                         | 16   | **open → issue #129** (doctor coverage: required add-on `inactive`, `write-guard` UNREACHABLE, non-numeric `Total-Results`, malformed listing).                                                                                                                                                                                                                                                                                                                                                                                                  |
| 45  | `research_vault/scaffold.py` (`_path_shim_probe`, decision 10)                                                          | A Linux-hosted Zotero answers a POSIX `file:///home/…` URL from `/file/view/url`; the probe unwinds it on decision 10's Windows premise and prints a relative `home/eranr/…` path in the reason. Measured on the test instance 2026-09-13. UNMATCHED is the right result (the file is not reachable from this WSL); the reason string is mangled. Out of decision 10's premise (Windows Zotero behind WSL), per the plan's author.                                                                                                                                                                                                                                                         | 16   | **open → issue #129**: a POSIX `file:///` URL is a design change to decision 10.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 46  | `research_vault/scaffold.py` (size)                                                                                     | 575+ lines after Task 16, mixing vault scaffolding with thirteen host/Zotero probes; a `doctor.py` would be the natural seam. Files-block-mandated placement; a whole-branch-review question, not a task's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 16   | **open → issue #129** (with row 51): `doctor.py`, `keystore.py` and a `markers.py` cut after the gate baseline lands; the move relocates `test_config_validity._probe_ids`' path, `test_doctor._hermetic_host`'s patch target and `mutation-baseline.txt` rows.                                                                                                                                                                                                                                                                                  |
| 47  | `research_vault/capture.py` (`_store_key`, `_load_key`)                                                                 | Key-store hygiene grouped, from Task 17's review: `write_text` then `chmod(0o600)` leaves the key file at umask-default mode for the instant between them (`path.touch(mode=0o600, exist_ok=True)` first closes it); an unparseable existing store is silently replaced with `{}`, discarding every other server id's key (a stderr line or a rename-aside would say so); after a 401 on a stored key, a `remember: false` re-grant leaves the stale key in the store, so each later run repeats one 401 and one dialog. All plan-printed shapes.                                                                                                                                          | 17   | split — `path.touch(mode=0o600, exist_ok=True)` before the write **fixed** at `a6b4b86`; the unparseable store replaced silently and the stale key after `remember: false` **open → issue #129**.                                                                                                                                                                                                                                                                                                                                                |
| 48  | `research_vault/capture.py:539` (`add`, `--collection`)                                                                 | `--collection` replaces an item's own `collections` list rather than merging into it. Plan-printed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 17   | **declined**: `add` creates the item; `collections` in the item JSON and `--collection` are one input twice, and the flag is documented as *the* collection — merging would hide a contradictory pair.                                                                                                                                                                                                                                                                                                                                           |
| 49  | `research_vault/capture.py` / `research_vault/lifecycle.py:161` (reason asymmetry)                                      | The same local API 400 reads `mismatch —` from `add` and `not-admitted —` from a capture read through `lifecycle.blocked`. Plan-printed; one refusal, two spellings by caller.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 17   | **fixed** at `a6b4b86` (review I-2): both `add()` error branches go through `lifecycle.blocked`; the pins moved to `not-admitted`.                                                                                                                                                                                                                                                                                                                                                                                                               |
| 50  | `tests/test_zotero.py`, `tests/test_add.py` (coverage)                                                                  | No case pins `base_for(strict=False)` with a wrong-typed `zotero_base` still answering `DEFAULT_BASE` (doctor's path; the code does it at `zotero.py:97`); the `remember: False` leg never asserts the key store is absent afterwards.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 17   | **fixed** at `273e246`: the `remember: False` store-absent assertion rides the `cmd_add` CLI tests; one `base_for(strict=False)` wrong-type test.                                                                                                                                                                                                                                                                                                                                                                                                |
| 51  | `research_vault/capture.py` (size)                                                                                      | Grew 169 lines in Task 17 and now owns the credential store (`KEY_STORE`, `_load_key`, `_store_key`) beside capture and add; a `keystore.py` is the deep-module cut if the whole-branch review wants one. Brief-placed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 17   | **open → issue #129** (with row 46).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 52  | `research_vault/lifecycle.py:137-138` (`_provenances`)                                                                  | Swallows `OSError`/`UnicodeError` with a silent `continue`, so a literature note nobody could read leaves the captured set without a row (the read-notes split elsewhere types it UNREACHABLE). Pre-existing from Task 12; surfaced by Task 17's round-2 re-review and already noted in a comment at `capture.py:458-461`.                                                                                                                                                                                                                                                                                                                                                                 | 17   | **declined**: `captured._read_notes` reports the unreadable note as a row and `capture --all`'s `_every_note` files its own; `_provenances` is the linter's input, not the reporter.                                                                                                                                                                                                                                                                                                                                                             |
| 53  | `research_vault/__main__.py` (`cmd_ack`)                                                                                | Re-runs `inbox.load` after `append_ack` already loaded the file, and stdout is now two lines when a marker clears (the `cleared …` line, then the id) — nothing parses it today (checked `hooks/`, `scripts/`, `skills/*/SKILL.md`), but `id=$(… ack …)` would take the wrong line. Plan-printed shape.                                                                                                                                                                                                                                                                                                                                                                                    | 18   | **fixed** at `273e246`: the `cleared …` line goes to stderr; stdout is exactly the ack id.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 54  | `research_vault/verify.py` (`clear_marker_for`)                                                                         | Grouped: a `path-bytes:` target clears every terminal marker for the check in the file, not one line (the inbox row carries no line number) — documented, wider than `_mutate_marker`'s single-line clear; the `#^` branch discards the citation key, so an anchor id shared by two notes citing different keys clears both; `_claim_notes` globs `literatures/` non-recursively where `_plan_state` rglobs it, so a nested literature note is stamped and unreachable by the clear; the function duplicates ~15 lines of `_mutate_marker`'s read/split/gate/sub/write loop (the `break`/numbered-line logic makes extraction non-trivial).                                                | 18   | split — the nested-literature-note glob **fixed** at `e37296b` as one rule at every reader (`literatures/` is flat everywhere; `lints._literature_files` rejects a `/` after the prefix; `_plan_state` globs flat); the `#^` branch discarding the key and the file-wide `path-bytes:` clear **declined** (documented; the inbox row carries no line); the 15-line duplication with `_mutate_marker` **open → issue #129**.                                                                                                                      |
| 55  | `research_vault/verify.py` (marker writer/clear asymmetry)                                                              | `ANCHOR_RE` needs no whitespace before `^`, so a tab-terminated claim yields `\t[marker] ^id`, which neither `_terminal_marker_pattern` nor the scope strip `_without_own_marks` handles. Pre-existing; `_mutate_marker`'s own `wiki/` guard at `:599` also does `relative_to(Path(vault_root))` unabsolutised — latent only, since `_plan_state` always receives an absolute candidate root.                                                                                                                                                                                                                                                                                              | 18   | **open → issue #129** (pre-existing writer/clear asymmetry, with row 10).                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 56  | `tests/test_verify_cli.py`, `tests/test_config_validity.py` (coverage and scan reach)                                   | `test_correction_ack_does_not_suppress_same_hash_blocking_retraction` narrowed to update-notice entries (the lifecycle leg's outage under the socket guard) and lost its "nothing else open" pin — a per-test monkeypatch of the lifecycle leg would keep it; the printed `test_ack_clears_the_failed_verification_marker` takes `monkeypatch` unused; both guards remain spelling-bound by design (the one-clock regex pins the call shape, the fixture scan the literal's shape).                                                                                                                                                                                                        | 18   | **open → issue #129** (coverage and scan reach).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 57  | `mutation-baseline.txt:100-101`, `research_vault/verify.py.manifest.json`                                               | Two dead `_citation_key_hash` rows (the mutated lines are gone, the function kept, so the prune — scoped to deleted functions — leaves them); the manifest's `tested_at` 2026-08-23 is stale for the whole module. Both cleared by Task 20's gate run (`--update-baseline` semantics), never by hand.                                                                                                                                                                                                                                                                                                                                                                                      | 18   | **homed in Plan W Task 25** (operator ruling, plan `a179b7d`), with row 15; the `tested_at` sidecar retires with the tool. Correction stands: gate mode never writes the baseline.                                                                                                                                                                                                                                                                                                                                                               |
| 58  | `tests/test_verify_cli.py` (sibling pin), `research_vault/notes.py` (`canonical_content`)                               | From Task 18's round 3: `…_acknowledgment_is_decided_from_candidate_before_projection` keeps its name while its flipped line now passes regardless of ordering (the "before projection" claim is still carried by the unchanged `candidate_hash` assertions) — a rename candidate; and an empty `verified: []` is excluded from the scope (pre-existing `all([]) is True`) while an empty `failed-verification: []` is kept as hand-written — two owned lists, two empty-list rules, worth one docstring line.                                                                                                                                                                             | 18   | **fixed** at `e37296b`: the sibling pin renamed `test_unwitnessed_note_acknowledgment_survives_projections_own_writes`; `canonical_content`'s docstring names the two empty-list rules.                                                                                                                                                                                                                                                                                                                                                          |
| 59  | `tests/test_skill_contracts.py` (`test_the_code_spells_no_hyphenated_skill_name`, `_code_spelled_identifiers`)          | The self-check iterates live skill directories only, so a skill deleted in the same commit as a product-code literal spelling its name would be masked by the code-spelled exemption — the dangling-route shape the scan exists to catch, in a narrow window. The `_code_spelled_identifiers` docstring's "42 distinct tokens" is a dated rationale (40 after round 2), not asserted.                                                                                                                                                                                                                                                                                                      | 19   | **declined**: narrow window (a skill deleted in the same commit as a literal spelling it), documented in the test.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 60  | `skills/synthesis-conventions/SKILL.md:16,22`, `skills/evidence-conventions/SKILL.md:16`                                | Still say "synthesis page"/"synthesis note" after Task 6 moved the layer under `wiki/`; no Part A Files block claims them. Part B Task 3 rewrites `synthesis-conventions`; the evidence-conventions line rides with it or is a one-word edit there.                                                                                                                                                                                                                                                                                                                                                                                                                                        | 19   | **Claimed by Part B Task 3** (the rewrite); not a Part A row to fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 61  | `docs/superpowers/plans/2026-09-07-ingest-redesign-a-capture.md` (Task 19's printed outcomes table)                     | The printed `capture-source` table carried a raw pipe inside a code span (the `merged`/`trashed`/`deleted` alternation), which mdformat splits into a cell; the tree carries the pipes escaped as `\|` inside the span. The plan's printed text should do the same so the next extraction does not reintroduce the mangling. Measured by Task 19's implementer.                                                                                                                                                                                                                                                                                                                            | 19   | **fixed** by the plan's author at `9d10db3`: the printed table escapes its pipes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## Do not fix

Decided non-actions. Recorded so a later reader who finds them does not mistake them for rot.

| Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Why it stands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The check value `quote:<claim-link>:managed-region` survives in `research_vault/events.py:12,297`, `verify.py:711` and `quotes._extra`, naming a managed region that no longer exists.                                                                                                                                                                                                                                                                                                                                                                                                                | It is a durable value already written into vault `verified` events. Renaming it would invalidate those records, which ADR 0003 forbids. A name that outlived its referent is the correct outcome here, not a defect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `docs/terminology.md` §4.4's preamble example uses the same `quote:…:managed-region` spelling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Same reason: the document describes values that exist in written records.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `verify` files the propagation lint's SKIPPED row on every fresh vault, because `system/propagations/` is empty until the first `propagate` runs (Task 14, implementer's concern, carried through two fix rounds).                                                                                                                                                                                                                                                                                                                                                                                    | It is the intended reading, not noise. ADR 0002 leaves one honest state for zero records: not MATCHED, because vacuous truth over zero applied plans certifies nothing and emptiness is not a pass; not silence, because a check that emits nothing is indistinguishable from one that never ran. SKIPPED is "the check does not apply, determined automatically", which is exactly this case. Verified against the code rather than assumed: the row is filed once because verify's dedup keys off `inbox.open_entries`, where SKIPPED entries stay by design (`inbox.py:754,765`); `inbox.summary` excludes SKIPPED from both the count and the age basis, so it never ages; and `verify.surface_decision` builds blockers only from UNMATCHED checks in the surface's closing set and from UNREACHABLE (`verify.py:1092,1111`), so it can never block a commit or a publish. It closes by the first propagate rather than by an acknowledgment — the same shape as decision 26's lifecycle row on a vault with no captured note. No code change. |
| `cmd_propagate` files an import hold for UNMATCHED outcomes only, where `cmd_capture` in the same file files one for UNMATCHED **and** UNREACHABLE (`__main__.py:75-83` against `:163-173`). An outage during the recapture inside `propagate.apply` therefore exits 3 and prints a line, but leaves no inbox row — after the rename and the surface rewrite have already landed.                                                                                                                                                                                                                     | The asymmetry is principled and the states are indistinguishable by construction. A recapture outage leaves the note at `literatures/<new>.md` still recording `<old>`, which is exactly the state `lifecycle.lint_lifecycle` reports as `re-keyed — <old> → <new>` — the same row that generates the mapping `_mapping_from_linter` feeds to the next `propagate` (`propagate.py`, `_mapping_from_linter`). So the vault reports the unfinished apply through the lint that also drives its recovery, and a re-run finishes it by the self-heal path `_sources` exists for. A hold would add a second record of one fact whose remedy is already named. `cmd_capture` needs its UNREACHABLE hold for the opposite reason: a capture that fails before writing leaves no vault artefact to lint, so without the hold nothing records that the operator tried. Checked against the multi-entry case: an outage on entry 2 of 3 leaves entry 1 complete, entries 2 and 3 reported `re-keyed`, and one re-run finishes both. No code change.           |
| In a vault whose notes record two Zotero server ids, a `propagate` that maps only the live-id note passes its plan, renames and rewrites — and then the recapture refuses. `capture.py:285` sets `client.server_id` from `existing[0][1]`, the **alphabetically first** literature note, which need not be in the mapping; the linter it then runs reads against that id and 412s. Measured by Task 14's re-review, not inferred. Pre-existing: the code took the identical path before round 2 whenever a client was passed.                                                                         | The refusal is correct and the vault is already flagged. A note's provenance tuple names its library (decision 08), so reading it against a different live instance *is* `database-changed` — a typed refusal, not an outage and not a pass, exactly as ADR 0002 requires. And the precondition is itself reported: `lifecycle.lint_lifecycle` sets `client.server_id = sorted(recorded_ids)[0]` and raises `DatabaseChangedError` at vault level (`lifecycle.py:178-181`), so every `verify` on a two-id vault already says so. The partial state left behind is the one `_sources` was built for and a re-run finishes it. The fix is reconciling the vault's ids, not routing the recapture around them — routing around would make a verb paper over a fault the linter exists to report. No code change.                                                                                                                                                                                                                                       |
| `verify` ends in a `UnicodeDecodeError` traceback on a vault holding a non-UTF-8 byte in any of ~15 unguarded `.read_text()` sites across 10 modules — `quotes.py` x2, `lints.py` x3, `structure.py` x3, `bibliography.py`, `paths.py`, `checks.py`, `searchlog.py`, `inbox.py`, `okf.py`, `scaffold.py`. Measured end to end by Task 15's re-review on a scaffolded vault: the crash comes from `quotes.py:130` via `verify.py:639`, which runs at `verify.py:930` — **before** `captured.lint_captured_set` at `:981` is reached at all. Guarding that one site moved the crash to `checks.py:139`. | **FOR THE WHOLE-BRANCH REVIEW (Task 20), not deferred.** Task 15 fixed its own site correctly and its comment is accurate about its own lint, but the original finding's framing — that an unguarded read "kills `verify`" — was true of the code and not of what an operator sees, because an earlier reader always crashes first. The class is the question, not any one site: which reads should produce an `UNREACHABLE outage` row for the file they could not read, and which should abort the run. `propagate.py:436` and `captured.py:118` are the two sites that answer it correctly today. Recorded with the measurement so Task 20 starts from evidence rather than re-deriving it.                                                                                                                                                                                                                                                                                                                                                      |
| A file literally named `Foo.md.md` is stored as page name `Foo.md`, so `[[Foo.md]]` strips to `Foo` and does not resolve to it (Task 15's re-review, finding F).                                                                                                                                                                                                                                                                                                                                                                                                                                      | Declined. The filename is absurd, the failure is a spurious finding rather than a missed one, and every fix costs a special case in the one function two other rules resolve through. Recorded so a later reader who constructs the case knows it was seen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `captured._page_names` walks `fulltext/`, so every `fulltext/<attachment key>.md` contributes its 8-character key as a page name and a wiki page writing `[[D7EJ9FTG]]` resolves silently as a page link (Task 15, concern 8). `structure.EXCLUDED_DIRS` omits `fulltext/` deliberately, per decision 19.                                                                                                                                                                                                                                                                                             | Recorded, not changed. Harmless today — the failure is a link resolving that a reader might expect to be a finding, not a finding suppressed on a real fault — and the resolution rule as written gives no hint that attachment keys are in the page namespace, which is exactly why it is worth writing down rather than leaving to the next reader of `_page_names`. Nothing after Task 15 touches `captured.py`, so this is a decline rather than a deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| The source ledger has two target spellings in one check (Task 15, concern 9). `_structural`'s `SKIPPED` row targets the plain string `wiki/meta/ledgers/source-ledger.json` with `target_kind="identifier"`, while every `UNMATCHED` row on the same file targets `RepoPath(os.fsencode(LEDGER_PATH))`. One file therefore yields two distinct acknowledgment scopes, and only the repo-path rows reach `verify._safe_relative`.                                                                                                                                                                      | Shipped as printed and recorded rather than changed. The consequence is narrow: an acknowledgment of the missing-ledger `SKIPPED` row does not carry over to a later `UNMATCHED` row on the same file, which is arguably right — they are different claims about different states. Worth knowing at the whole-branch review because the split is invisible from either row alone. Nothing after Task 15 touches the file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| A never-compiled vault carries a `captured-set` / `no-identifier — no source ledger` entry in `inbox/review-queue.md` on every `verify`, because `_plan_state` files every non-MATCHED effective row including `SKIPPED` (Task 15, concern 11).                                                                                                                                                                                                                                                                                                                                                       | Designed, and the third instance of one shape: propagation's Task 14 row and lifecycle's decision-26 row behave identically, and both are recorded above as decided. ADR 0002 leaves one honest state for a check with nothing to check, and the row never blocks, never ages and closes by the first compile. Recorded here so the three read as one deliberate pattern rather than three oversights.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| An `update-notice` finding's frontmatter `failed-verification` row is not cleared by `ack` (Task 18's review, minor 9; the implementer's concern 7 second half).                                                                                                                                                                                                                                                                                                                                                                                                                                      | Declined by the plan's author (`3e170f3`): the row is a verification **event** — a record under ADR 0002 and ADR 0003 that `events.record_failure` writes and `record_pass` removes — and an acknowledgment is itself the record of acceptance; the two stand together like an acked inbox row beside its entry. `ack` clears the tool's inline *marker* (open point 09), never a record.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## Process notes

Lessons from running this plan under `superpowers:subagent-driven-development`. They are recorded
here because a committed file is the only home that survives the workspace deletion at Finish;
"not issue-shaped" is a reason not to file them, not a reason not to write them down.

1. **An interim fix must not be relayed to a running implementer as an instruction.** The
   controller passed a plan author's in-progress correction ("drop the rename log") to Task 6's
   implementer before the replacement text was settled, then had to retract it when the answer
   turned out to be "the applied propagation plans" rather than a deletion. It cost nothing only
   because the implementer had not reached that file yet, twice. The fix adopted mid-run: the
   author marks every message **settled** (in the plan on main at a named commit) or **pending**
   (a decision is coming), and a pending item is held, or sent only as "stop before this line",
   never as an instruction. Task 6's implementer independently rejected the bad message after
   finding it contradicted the repository — that verification is now a standing instruction in
   every dispatch.

2. **A verification grep run during a live implementation must read a committed ref, not the
   working tree.** Checking a hazard's call sites with `grep` over the worktree while Task 4's
   implementer was editing `lints.py` returned a line number belonging to neither the before nor
   the after state. `git show HEAD:<path>` is the only stable view while a subagent holds the
   tree.

3. **Diff from the merge base, never from the branch tip, to decide whether a merge is safe.**
   `git diff --stat <branch-tip> main` counts the branch's own commits as reversals and can report
   dozens of files where the real incoming set is three. `git diff --stat $(git merge-base HEAD main) main` is the question actually being asked.

4. **Grep `docs/research/` before deferring a fact to a live probe.** Task 7's brief asserted that
   Better BibTeX's `item.search` returns `citationKey`. It returns `citekey`, and this repository
   already held the answer: `docs/research/2026-09-05-zotero-api-reading.md` record 291 quotes
   `content/json-rpc.ts` L247-251 at v9.0.63. The implementer flagged the uncertainty but
   concluded it could not be settled offline; the plan author asserted the wrong key; the
   controller relayed it. Only the review thought to look. The plan's own constraint that
   "environment facts come from a probe" reads as *go measure it* and quietly discourages
   searching for a measurement someone already took and dated. Search the corpus first.

5. **A coordinating peer's sequencing label is a claim about the plan, not the plan.** The author
   called Task 8 "next" from the moment its glossary edits landed, and the controller adopted that
   twice without checking. Task 8's own brief states its dependency on Task 7 in its text, one
   grep away. This is "deletion lists are claims, not orders" one level up: an assertion about
   what the plan says is checkable against the plan, and should be checked before it is acted on.

6. **"Converge at the next touch of X" is not a schedule unless a later Files block claims X.**
   The `LedgerUnreadableError` message string was left to converge "at the next touch of
   `notes.py` (Task 13 if the implementer is in that file, otherwise Task 20)". Task 13 does not
   touch `notes.py` — its Files block names `zotero.py`, `__main__.py`, `inbox.py`, the skill and
   terminology, and creates `capture.py`. So the deferral silently landed on Task 20, which runs
   after Task 13, whose printed test asserts the plan's spelling. Composed, the code's message read
   `outage — unreadable ledger wiki/…json: …` against a `startswith` looking for
   `outage — wiki/…json unreadable`; run rather than eyeballed, that is `False`. Task 13's test
   would have failed on the day it was written, and its implementer could only have weakened the
   assertion or edited a file outside its Files block. A Files block is the one place file
   ownership is knowable, so a deferral names the task that claims the file. Neither the author who
   scheduled it nor the controller who re-extracted the brief twice checked the assertion against
   the committed string; it surfaced from reading the Files block for an unrelated reason.

7. **A fold-in commit is diffed against the shape the review approved, before it lands.** Folding a
   review's rulings back into a plan's printed code has twice introduced a change no review saw. At
   Task 8 a fold-in carried a date that was false when written and a Status-line form that broke
   `docs/agents/domain.md`'s "ADRs carry no history" rule. At Task 13 a fold-in moved a reviewed
   outcome's target from `vault` to the CSL file — a `raise` turned into a `return` inside a
   function whose signature returns one CSL outcome, transcribed without re-reading the approved
   outcome table. Both were caught, but the second only because an implementer flagged a
   brief-versus-message disagreement it was under no obligation to explain; the review that would
   have caught it had already run. The reviews emit outcome tables and per-case enumerations
   precisely because they are diffable artefacts, and until this point nobody was diffing against
   them. The check costs one pass over something that already exists.

8. **When a fold's printed code predates a review-approved refactor, reasoning about the printed
   construction says nothing about the tree.** Task 14's round-2 fold printed a plain
   `if target.exists():` where the branch had
   `if source.name != target.name and (target.exists() or target.is_symlink()):`. Reading only the
   printed code, the extra condition looks redundant: `source` is `literatures/<old>.md` and
   `target` is `literatures/<new>.md`, so the names differ exactly when the keys do, and the
   controller ledgered a ruling to drop it. The branch builds `source` a different way. Round 1's
   review had replaced the filename lookup with `_sources`, which finds each note by its recorded
   `citationKey` — so after a partial apply the note sits at `literatures/<new>.md` still recording
   `<old>`, and the self-heal re-run has different keys and the *same* filename. The condition the
   controller called redundant was the entire self-heal escape, and dropping it would have
   re-stuck the vault that round 1 unstuck. The plan had never absorbed `_sources`: it named the
   helper zero times. Note 7 says to diff a fold against the shape the review approved; this is
   the case where the plan's printed shape is not that shape at all, and the diff must be against
   the committed tree. The rule that generalises: a claim about what code does is checked against
   the ref that will run it.
