# Validation Slice — Execution Protocol

> Spec §9 transcribed into an execution protocol. Mode: **author-session-driven** — the slice is real research use of the installed plugin by the author with agents in vault sessions; Codex enters only for defects found. Phase 0 runs NOW, parallel to Plan Q (separate repo, zero file overlap). Phases 1–6 wait for the post-Q batch (F-5's citekey guidance and the routing index land there; first import hits both).

## Phase 0 — substrate (parallel-safe, human step included)

1. **Install the plugin**: `/plugin marketplace add eranroseman/knowledge-harness` in a fresh session — the first real install. Record: does it load, do the nine skills appear, do the vault template's bare skill names resolve (**the C-4 empirical test** — record the answer in the audit doc's C-4 card).
2. **Create the vault**: fresh private repo (author names it; spec: separate from the plugin repo), then `setup-vault` — consent flow, scaffold, doctor. Expected: doctor MATCHED on tree/machine-config/zotero/bbt; autoexport UNMATCHED with repair guidance (the human step not yet done).
3. **The one-time human step** (BBT Preferences, ~3 minutes — wizard-form walkthrough, prose ed
