# Requirements: sourcing (the build-or-buy phase)

> **For agentic workers:** this document consumes an **approved** requirement set and produces build-or-buy findings in its own artifact. It is one of two successors `writing-reqs` offers at its review gate; the other is `writing-specs`.
>
> **Status: derived, not elicited.** This set was not produced by the process `writing-reqs` specifies. Nobody was interviewed. Every requirement below is either a decision the author made explicitly while specifying `writing-reqs` on 2026-09-01 and 2026-09-02, or a method this project actually ran and can point at. **Anything that was neither is an open question, not a requirement.** As of 2026-09-02 every requirement but two also carries prior art, read from five corpora; **S2 and S9 carry none and say so.** The author's ruling is to build this skill first and use it on `writing-reqs` rather than the reverse, so this set is not waiting on an interview — it is waiting on its prior art being complete, and it now names where it is not.
>
> **Identifiers are `S`, not `R`,** and are never changed or reused. A red-team pass on the sibling document found INCOSE's own rule numbers sitting in a column of that set's identifiers, meaning two things at once; separate namespaces prevent it here.

## Problem

Deciding whether to build or adopt is a distinct act from deciding what is needed, and it was running inside `writing-reqs` as a third phase. Three things made that wrong. It has other callers — a build-or-buy question arises without a requirements interview at all, and #75's disposition survey was exactly that. It runs on a different clock — requirements settle once, sourcing re-runs whenever a requirement's meaning narrows. And its findings were landing inside an artifact that had already been approved, changing the thing after agreement.

## Solution

A skill that takes an approved requirement set, finds candidates that cover **enough** of it to be worth deciding about, and records what each one covers. It does not decide adopt, adapt or reject — that is `writing-specs`' call, made on the evidence this skill gathers. It never edits the requirement set.

## Assumptions

- **Candidates are readable.** Screening judges the artifact body — a skill file, a template, a licence — so a candidate whose body cannot be obtained cannot be screened. Nobody is obliged to publish one.

## Prior art

Two standards already held, read 2026-09-02, plus five corpora read for the competitive analysis below. **This is not a survey.** No COTS-selection literature was searched before the requirements were written; it arrived with the competitive analysis, which is the wrong order and is why several requirements were rewritten rather than written once.

What each source gave, and to which requirement, is on that requirement's *Source* line. Two findings are worth stating at set level because they shaped more than one requirement:

- **Make-or-buy is a trade-off, not a screen** (12207 6.1.1). It is supported by decision analysis and ends in an estimate, not a verdict — which is why this skill gathers evidence and hands the decision on.
- **Adopting does not excuse the analysis** (24748-2): *"Use of off-the shelf system elements does not replace system feasibility analyses… needed to confirm that interfaces are compatible."* Install-as-is is the top rung of the ladder and the one most likely to be taken as a shortcut.

### Where the split sits, and the test for it

ISO/IEC/IEEE 15288:2023 **removed** the separate category buy-options used to have:

> *"The term 'Candidate Non-Developmental-Items (NDI)' including COTS (commercial-off-the-shelf), reuse of a previous design or acquirer provided items for obtaining system elements **has not been used and these have been included into 'system design alternatives'**."*

Read first as an objection to splitting sourcing out. It is not. What 15288 removes buy-options *from* is the requirements phase, and where it puts them is design — which is what this split did: sourcing no longer runs inside `writing-reqs`, and its verdicts no longer land in a requirement set.

The residual difference is smaller than it looked. 15288 has design generate and evaluate buy-options among its alternatives; here sourcing screens them first and design receives the result. Those converge **provided the design phase gets both artifacts**, which is why S14 exists. Sourcing pre-computes an analysis rather than removing it from design's reach, and S12's rationale requirement is what lets design overturn a verdict rather than inherit it.

**This is testable, but the test is blocked, not scheduled.** The test feeds `writing-reqs`' requirement set and its sourcing findings to `writing-specs` together. **`writing-specs` does not exist** — it is #75's vendored rename of `brainstorming`, and #62 owns its sequencing — so the test waits on that rather than on anything here. S14 is satisfied by producing a handoff, not by something consuming it, so nothing about building or running this skill is blocked. If `writing-specs` can design from the pair, the arrangement holds. If it needs to re-open a screened candidate, the pre-screening cost something and belongs inside design after all.

## Competitive analysis

Five corpora, 2026-09-02: COTS and OSS selection methods, open-source health metrics, procurement's participation gate, supply-chain dependency review, and design-alternative evaluation. Twenty agents, with every non-confirming finding sent to a verifier instructed to refute. **Twelve of fifteen were refuted**, mostly for generalising past their text — so the surviving findings are thin by design rather than by accident.

**The result that mattered was a null: no coverage threshold exists.** All five corpora were asked directly for a rule of the form *"screen in if it meets N of M requirements"*, and all five returned nothing, independently. Procurement's participation gate does not test coverage at all — it asks whether a party can perform. OpenSSF argues against aggregate scores outright. The COTS methods screen on a **named subset**, never a fraction.

That falsified a requirement rather than refining one. The requirement then in place had asked what the threshold should be; the answer is that the question was malformed, and asking it is what produced this project's failed screen. What the corpora supply instead converges from four directions and is now **`writing-specs`' inheritance rather than this skill's**: elimination requires a named critical flaw, the screen runs on a designated subset, and the survivor count is bounded by the evidence budget. Only the last clause stayed here — everything is **recorded rather than disqualifying** — which is S16.

**Limits.** Sixteen findings were initially held rather than tested, under a rule that turned out to be circular; they were mined on 2026-09-02 and five became requirements, of which two survive here and three moved to `writing-specs`. One survivor remains declined — tiering requirements at approval time — because it contradicts the all-musts ruling and would force both this skill and `writing-specs` to transform the scheme before either could use it.

## Requirements

**Retired: S15, S17, S18, S21, S23.** All five were screening rules, and screening belongs to the design phase — 15288:2023 folds candidate off-the-shelf items into *"system design alternatives"*, evaluated inside design, and this skill's own S5 says it gathers evidence rather than choosing. They are recorded as inherited requirements for `writing-specs` in `docs/superpowers/reqs/2026-09-02-sourcing-for-sourcing.md`. Identifiers are never reused, so the gaps are deliberate.

Each requirement names the ruling or the run it comes from. Nothing here is inferred from a standard that was not read.

### Input and ordering

**S1** — Checks the requirement set is screenable, and says so when it is not.
*Fit:* the run checks the set marks its must-have requirements and versions them (S2 voids findings scored against a superseded meaning). A set that marks neither is still **covered** under S16; the run says which fields were present. Neither case refuses the work.
*Floor:* **yes** · *Version:* 2
*Why:* Without it S2 and the coverage report reach for fields that may not exist, and the run stalls with no way to say why. With it, a set from any source is either screened or covered, and the reader can tell which.
*Source:* the author's rulings of 2026-09-02 (sourcing follows approval) and 2026-09-03 (invoking the skill is the approval; the skill checks form, not provenance). Design-alternatives corpus: an approval gate before the study, where *"approved" means baselined, not frozen*.

**S2** — Voids findings from an earlier run once a requirement's meaning has changed, and re-runs against the new meaning.
*Fit:* every finding records which version of which requirement it was scored against, reading the version the set puts on it. A later run comparing those versions against the set's current ones marks each affected finding void rather than stale. **This is between runs, never within one:** a run reads the set once, at the start, and nothing in it re-reads.
*Floor:* no · *Version:* 1
*Source:* 29148 5.2.8, **Version Number** — *"to make sure that the correct version of the requirement is being implemented"* — with 29148 naming traceability as fundamental to *"impact analysis when requirements change."* 12207 6.3.5 supplies the baselines. The author's ruling of 2026-09-02: *"lets finalize reqs and then we redo all the sourcing."*

### Screening

**S19** — Searches from more than one source and stops when new sources return only candidates it has already seen.
*Fit:* the run declares **before searching**: the candidate concept (S24), the kinds of source it will consult, and the stopping rule. Which specific sources it reaches is then a result, not a choice. **A source kind or query form added after the search began is recorded as an addition, with the reason** — that is what makes motivated stopping visible. The finding names every source searched and where saturation was reached. A run that searched one source, or stopped without saying why, fails.
*Floor:* no · *Version:* 2
*Source:* OTSO, read 2026-09-02 — *"One main challenge in the search is the difficulty of deciding when to stop the search: how do you know that you have searched enough and found all the relevant alternatives? A simple strategy for ending the search is to use several sources."* Nothing else in this set bounds the **search**, and with the screen gone this is the only bound there is. Held back from the competitive analysis by a test-not-generator rule and applied 2026-09-02.

**S20** — States the search budget it worked to and where it stopped.
*Fit:* a reader can tell a short candidate list from a verdict on the field. A finding that reports few candidates without saying how far the search went fails.
*Floor:* no · *Version:* 1
*Source:* FAA NAS SEM §4.6.3.4 — *"When numerous possible alternatives are identified, a detailed analysis of each one may not be cost effective; therefore, down-selection of candidates is recommended."* The budget, not candidate merit, is the only warrant for narrowing, so an unreported budget makes the two indistinguishable. **This is the requirement whose absence produced this project's worst sourcing claim:** *"nothing exceeds three of twenty-nine"* was read as a fact about the field when it was an artifact of the method.

**S7** — Screens each candidate in isolation, without knowledge of the others.
*Fit:* no screening run can name a candidate it was not given, rank candidates, or recommend one.
*Floor:* no · *Version:* 1
*Source:* the method this project ran on 2026-09-01, recorded in the sourcing screen. Twelve candidates, one isolated agent each.
*Source, prior art:* both UK instruments make a condition of participation a property of the supplier measured against the contract, *with no reference to any other supplier*. The underlying distinction is what makes this correct here — **capability is absolute, preference is relative** — so isolation is right at screening and wrong at selection.

### What it records

**S3** — Findings live in this skill's own artifact, never in the requirement set.
*Fit:* the requirement set is byte-identical before and after a run, and the run names the path of the artifact it wrote. The first half is the prohibition; the second is the obligation, and a run satisfying only the first has produced nothing.
*Floor:* **yes** · *Version:* 1
*Source:* the author's ruling, 2026-09-02 (Q19). Keeping them in the requirement set would change an artifact after it was approved, which is what approval-before-sourcing exists to prevent.
*Source, prior art:* FAR 10.002(e) mandates that search results be documented as their own output, and FAR's ordering makes co-location with the requirements document impossible in the normal case, since research runs *"Before developing new requirements documents"*. The separateness is a **consequence of the ordering, not a stated prohibition**. The design-alternatives corpus keeps findings in a dedicated report artifact distinct from the requirement baseline it consumed, and the health corpus separates what an artifact's authors assert from what an assessor concluded — the same separation one level up.

**S4** — A finding names the smallest part of a candidate that could be taken without taking the rest.
*Fit:* the unit follows the candidate's kind — a section, rule, gate, table or step in a skill; a module, function or exported interface in a library; a clause in a standard; a field in a template. Reading the body under S8 is what identifies them. A finding may name a whole candidate where the whole *is* that smallest part, or where the whole is what would be taken. **No finding rejects a whole candidate**: a part failing a requirement says nothing about the rest of the body.
*Floor:* no · *Version:* 2
*Why:* Rejecting a body for one failed part throws away the parts that work, and there is no way to get them back later except by hand. This project did exactly that three times.
*Source:* the author's ruling of 2026-09-01 — *"the fact that a skill violates one requirement doesn't mean it has another part that fulfils another requirement better."* PORE and MiHOS both record at the pair *(one requirement, one feature of one product)*. SBOM standards use the component as the unit; CycloneDX anchors evidence within one. The design-alternatives corpus prescribes building a new candidate from a rejected whole's usable parts.

**S16** — Records, per candidate, which requirements it covers, which it does not, and which could not be determined.
*Fit:* a reader can say for any candidate and any requirement which of the three it is, without re-reading the candidate. The run states affirmatively where a candidate's coverage was fully assessed; absent that statement, coverage reads as incomplete rather than as absent.
*Floor:* **yes** · *Version:* 1
*Source:* a coverage report needs the whole map, where a rejection needed only the first failure. The third state is NTIA's SBOM minimum-elements rule, read 2026-09-02, which makes *unknown* the default reading and puts the burden on the recorder to claim completeness rather than letting silence imply it. The earlier screen recorded claimed passes and a failure count, which cannot answer the question `writing-specs` will ask.

**S22** — Records, per candidate, capability the requirement set did not ask for, and whether it is expected to help, be neutral, or cost.
*Fit:* excess is recorded as its own finding, not folded into coverage. A candidate carrying twice the surface area needed is not thereby better.
*Floor:* no · *Version:* 1
*Source:* MiHOS, read 2026-09-02, which splits it — *"OverMatch: A COTS feature exhibits more capability than what is required… Surplus: There is an extra COTS feature which is not required"* — and treats the excess as having a positive, neutral or negative effect. S16 records what a candidate misses and nothing recorded what it carries unasked. For one developer the cost is concrete: surface area to understand, and drift to track.

**S12** — A finding carries its rationale, not only its result.
*Fit:* a reader can say why each finding went the way it did without asking, and a finding whose stated grounds are absent from the artifact it judged is void.
*Floor:* no · *Version:* 1
*Source:* ISO/IEC/IEEE 24748-2:2024 on 15288:2023 6.4.5.3(d)(3) — rationale kept *"retrievable and accountable"*. Read 2026-09-02. Forced by measurement, not preference: the 2026-09-01 screen rejected spec-kit `/specify` on two behaviours that re-reading the same day found in neither current preset.

### Gathering evidence about a candidate

**S8** — Judges the artifact body, never a description, README or roster position.
*Fit:* two steps, and only the second grounds anything. A description, README or registry entry **may narrow the search** — that is what descriptions are for. A **finding** quotes the body it judged and names every source it consulted; one resting on a description fails, which is the failure this requirement was written from. A candidate whose body was never read is recorded as not-examined, never as covering or not covering.
*Floor:* **yes** · *Version:* 1
*Source:* the same run, and the failure it was correcting: an earlier pass read a candidate's exit path off its roster rather than its body, and was wrong about all three candidates it checked. The two-step shape is the author's, 2026-09-03, and it is how skills themselves work — a description routes, a body does. Discovery stays cheap; judging stays honest.
**Prior art, 2026-09-02, by two routes.** The roster clause has a direct anchor: Procurement Act 2023 s.22(4)(a) forbids building a condition of participation on a candidate's presence in a prior-award list — being on someone's list is not evidence of capability. The body-over-description clause is supported by admission rather than by prescription: OpenSSF Scorecard disclaims its own checks as heuristics with false positives and negatives, and the OpenSSF's own comparison concedes it measures what is automatable rather than what matters, with the badge depending on self-reporting.

**S9** — Every claim about a candidate quotes text present in that candidate at the version named.
*Void, defined once and used by S2, S9 and S12:* the finding does not count toward anything, and the run names what must be redone to replace it.
*Floor:* no · *Version:* 2
*Fit:* two steps, as S8 has. A mechanical check that the quoted text appears at the named version **flags** a mismatch and voids nothing. Voiding requires a **read** confirming the text is absent, not merely that the check failed; where the read finds it present, the run records what the check tripped on. Where it finds it absent, the finding is void and the candidate returns to be screened again — **nothing is eliminated on it**, because a failure of the reader is not a flaw in the candidate. Each check records what was tested and what it returned, including *not applicable*, rather than a bare verified.
*Source:* a measured failure in this project, and only partly supported by prior art. The failure: the 2026-09-01 screen rejected spec-kit `/specify` on two behaviours — a hard error on empty input and a three-marker clarification cap — and re-reading both presets the same day found neither. The artifact existed; the cited behaviour did not.
**What the prior art covers, and what it does not.** `sources/` holds four 2026 papers on LLM citation hallucination, read 2026-09-02. All four address **existence** — whether a cited work is real. HALLMARK decomposes validity into *"six sub-tests (values: True/False/N/A)… independently verifiable dimensions: (1) DOI resolves, (2) Title exists in bibliographic databases, (3) Authors match…"*, and Zhao et al. measure the problem at scale in published arXiv papers. **None addresses the failure this requirement is for**, which is a source that exists and does not say what was claimed — support rather than existence. Citing them as support for the rule would be the stretch this project has made three times already.
What does transfer is the **shape**: decompose the check into named dimensions, record what each returned, and carry an explicit not-applicable rather than letting silence mean anything. That is HALLMARK's method and it is credited as a method, not as evidence for the rule. *An earlier S9 required recording a per-candidate date of reading; that was withdrawn on 2026-09-02 after the supply-chain corpus showed NTIA prescribes exactly one document-level timestamp, making the recorded single date correct practice rather than the failure this requirement was built on.*

**S10** — Reads the licence from the candidate's own files, not from a platform's detected label.
*Fit:* the licence recorded under S13 names the file it was read from — a licence file, a README, a manifest, or a header. **No licence file is not no licence**: the run records "none found" only after looking in the candidate's own files, and says which it looked in. This binds the licence finding, not every finding.
*Floor:* no · *Version:* 2
*Source:* the same run — `framing-doc` was found to have no LICENSE file, verified rather than inferred.
*Source, prior art:* SPDX exists in this shape because a platform label is not trusted as the licence of record. It splits the field — the **declared** licence must come from text in the package's own files, and information from a third-party repository is barred from it by name; a detected label may enter only as a **concluded** licence, which is the document creator's own analysis and must be explained.

**S13** — Records the supplier and the originator where they differ, the licence, and the intellectual-property and copyright assertions.
*Fit:* due when a candidate enters the screen, not when it is found — discovery is cheap and this is not. Each is then named per candidate, or recorded as absent; "MIT" alone does not satisfy it. Who distributed a component is routinely not who made it, and both SBOM standards split them for that reason.
*Floor:* no · *Version:* 1
*Source:* ISO/IEC/IEEE 12207:2017 6.1.1 — for COTS and open-source software, acquisition *"can be limited to identifying the supplier, accepting or negotiating the conditions in a pre-defined licence… determining rights to intellectual property and data rights."* Read 2026-09-02. S10 covered only the licence.

**S11** — Names the compatibility check it ran at the seam, not only the requirements a candidate covers.
*Fit:* every included candidate states what was checked at the seam — invocation, inputs, outputs, and the harnesses under `writing-reqs` R34 — and a finding claiming a pass with no seam check fails.
*Floor:* no · *Version:* 1
*Source:* ISO/IEC/IEEE 24748-2:2024 — *"Use of off-the shelf system elements does not replace system feasibility analyses or the risk reduction analyses and effectiveness assessments needed to confirm that interfaces are compatible."* Read 2026-09-02. This project's own screen measured requirement coverage and never checked a seam.

**S5** — Gathers the evidence each treatment on the ladder would need, without choosing one.
*Fit:* for every candidate, the artifact carries what a downstream decision would require to pick among install as-is, fork and keep merging, copy frozen, copy plus a delta, author to the design and credit it, or write from scratch. **A per-candidate opinion is permitted and is evidence**: the run may say which treatment a candidate's evidence points toward, and may say so for every candidate or for none. What it may not do is **close the decision** — name one candidate as the answer, or state an outcome the design phase is expected to accept. Five candidates each carrying an adopt recommendation is a richer input than five bare coverage tables; one candidate named as the choice is a decision this skill does not make.
*Floor:* **yes** · *Version:* 1
*Source:* the author's rulings supply the ladder (2026-08-31, 2026-09-01), move the choice downstream (2026-09-02), and permit a per-candidate opinion as evidence rather than a decision (2026-09-03). OTSO's cost-to-baseline and MiHOS's anticipated fitness both price a treatment without choosing one; FAR 10.002(d) puts the choice with the contracting officer, downstream of the research. **The six-rung ladder itself has no support in any corpus read.**

**S24** — Records what it searched for and in what terms, so any result can be re-run.
*Fit:* the artifact states the concept a candidate had to match, and for each source the exact terms or queries used. A null result from a source whose terms are unrecorded is not a finding about the field and may not be reported as one. Where a source returns nothing, the run says how it established that the source and the query were working.
*Floor:* no · *Version:* 1
*Source:* the by-hand run of 2026-09-02, which produced a null from a GitHub search on three unrecorded queries. The null was only trustworthy because the run happened to test the search tool on a control query first — a step nothing required and nothing recorded. This is S9's principle applied to the search rather than to a candidate: a claim nobody can re-run cannot be checked, and a null is the claim most likely to be wrong and least likely to be questioned.

### Handoff

**S6** — Reports that no candidate qualified, and does not decide what follows.
*Fit:* a run finding nothing emits that as a finding with the coverage it did measure, edits nothing upstream, and names no build decision.
*Floor:* no · *Version:* 1
*Source:* the author's rulings of 2026-09-02 — *"if you can't source anything, then you develop"* — and 2026-09-03, that the decision follows sourcing. FAR 10.002(c) and (d)(2) treat a nil result as input to someone else's decision, not a verdict the searcher draws. The design-alternatives corpus names three responses to a null and gives all three to the stakeholder.

**S14** — Hands both artifacts to `writing-specs`: the approved requirement set it consumed, and its own findings.
*Fit:* the design phase can reach every finding and its rationale without re-running the screen, and can overturn one — a run that hands over findings as settled rather than as evidence fails.
*Floor:* no · *Version:* 1
*Source:* the author's ruling, 2026-09-02 — "feeding it to it together with the reqs will achieve the same results". It is what makes this arrangement converge with ISO/IEC/IEEE 15288:2023, which evaluates buy-options among design alternatives rather than ahead of them; pre-screening is only equivalent if design still sees the alternatives and the reasons.

**S25** — Runs on Claude Code and Codex.
*Fit:* the skill body invokes only mechanisms both harnesses provide, and a fresh install on each produces one complete findings artifact from one run.
*Floor:* no · *Version:* 1
*Why:* A skill that works on one harness fails silently on the other, and the failure is absence rather than error — #56 recorded 8,873 bytes of guidance arriving missing with nothing raised.
*Source:* promoted from a constraint on 2026-09-03, on the author's observation that none of this skill's constraints were constraints. It fails 29148 3.1.7 — the harnesses are pre-existing, but supporting both could have been decided otherwise and is testable — which is the same test that promoted `writing-reqs` R34. The bullet had been copied across from the sibling without re-applying it.

## Constraints

**Test applied**, ISO/IEC/IEEE 29148:2018 3.1.7: a constraint is an *"externally imposed limitation… imposed on the solution by force or compulsion"*. Could it have been decided otherwise? If yes it is a requirement, not a constraint. **All three earlier bullets failed that test** and were removed on 2026-09-03: running on both harnesses is a decision and is now S25; "consumes `writing-reqs`' artifact" was false, since this skill takes a set from any source, and "does not modify its input" was S3 restated; naming `writing-specs` as successor is a choice and is S14 restated.

- **A candidate's own terms bound which treatments are available to it.** A component with no licence cannot be copied at all, whatever the requirements say — only its idea can be taken, with credit. This is the constraint most specific to this skill, because its whole subject is other people's work, and it can make the top rungs of the ladder unreachable for a candidate that covers everything.
- **MIT and Apache-2.0 attribution is owed wherever text is taken substantially.** The licence is the constraint; carrying the attribution is a requirement derived from it, and unnumbered here.
- **Archived on handoff, never maintained — inherited, not chosen here.** The whole superpowers workflow operates this way and this artifact sits in it. Drift from the candidates it screened is expected: a finding is true of a version, not of a project.

## Open questions

Only what is unresolved. Questions answered since drafting are in git, not here.

- **S16 and S24 test the same thing at two stages.** S24's candidate concept — *takes a set of requirements and reports which components cover them* — and S16's coverage map admit and exclude the same candidates. The completed run demonstrated it: all nine died on S16, five floors never acted, and the screen added nothing the search had not produced. Either enforce the concept during the search, in which case S16 goes inert, or keep the search deliberately loose and let S16 be the gate. Doing both gets discrimination from neither.
- **What shape is the findings artifact?** `writing-reqs` specifies its output. This skill does not, beyond requiring that findings live in it (S3) and carry particular fields.
- **What is "actively maintained" worth?** The author's rule — *"the actively maintained is a quality mark. I don't use verbatim a section from a skill with 1 user"* — has published thresholds for its first half — OpenSSF Scorecard's *"at least one commit per week during the previous 90 days"* — and none for its second. The requirement that carried them routed a weak signal toward a ladder rung, which is `writing-specs`' call, so it retired with the other screening rules. Where a maintenance signal is recorded, and against what window, is unresolved.
- **Is the prior art deep enough?** Two standards were read before the requirements and five corpora after, which is the wrong order. Unsearched: COTS-selection literature beyond the five methods surveyed, and any second source for S9, which rests on one measured failure in this project.

## Evidence

The runs cited above: `docs/research/2026-09-01-pre-spec-sourcing-screen.md`. The findings produced for building `writing-reqs`: `docs/superpowers/reqs/2026-09-01-writing-reqs-sourcing.md`, suspended pending a re-run.

**Prior art ran late and thin.** Two standards were read before the requirements; the five corpora that mattered arrived with the competitive analysis, after the set existed. That is the wrong order and it is why several requirements were rewritten rather than written once — the screening requirements most of all, whose premise the corpora falsified and which are now retired. What remains unread is COTS-selection literature beyond the five methods surveyed, and any second opinion on S9, which still rests on a single measured failure.
