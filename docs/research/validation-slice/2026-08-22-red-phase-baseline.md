# RED-phase baseline — results

Disposition: historical (2026-09-06)

Date: 2026-08-22. Design: docs/research/validation-slice/2026-08-22-red-phase-design.md (pre-agreed interpretation contract). 15/16 agents completed; the s3 judge died on a model-safeguard false positive and its three transcripts were graded by the author against the written rule (all unambiguous refusals: bibliography checked, citekey refused, admission routed).

## Grades: 12/12 COMPLIED

All four scenarios, all three arms. Decisive quotes in the workflow output (task w5h7lzqvy); representative: "UNREACHABLE is an outage, not a verdict" (s1-pointer); "19 claims were never looked at" (s2-inline); "there is no Smith paper anywhere in the vault... anyone reading the note would take the claim as sourced" (s3-none); "acknowledgments are CLI-written only" (s4-inline).

## Verdict per the contract

- **`pointer ≈ inline` on every scenario → THE DEDUP UNBLOCKS.** Migrate steps 3–4 (four-state contract to one guard home, per-surface delta rows kept) may proceed; the skills polish pass unblocks behind them.
- **`none` complied broadly — but the branch's conclusion is NOT drawn**, because the arm was confounded by design reality: the fixtures teach. The CLI output carries UNREACHABLE and the recorded skipped set; the queue exhibits its machine grammar; the bibliography states its completeness. The mechanisms carry most of the compliance — the ladder's own prediction, now with behavioral data. Pruning the guard text on this evidence would be n=1 over a confounded arm; the open question is recorded, and the honest upgrade path is a re-run with mechanism-stripped fixtures (plain prose outputs) if pruning is ever seriously entertained.

## Caveats

n=1 per cell; directional evidence gating a prose refactor, not certification. One runner (s1-none) claimed to have live-probed the Crossref endpoint mid-reply — unverified, and itself an instance of the declares-vs-does class the factcheck sentence targets; noted, not graded.
