# Review package: ebea360..89fe2fe

## Commits
89fe2fe docs: wire oldest_age_days into project-flow and setup-vault reporting
2e23c40 fix: search-log surface added and formatter-ignore patterns anchored

## Files changed
 knowledge_harness/templates/vault/AGENTS.md        |  2 +-
 knowledge_harness/templates/vault/editorconfig     | 44 +++++++++++++---------
 .../templates/vault/markdownlintignore             | 11 ++++--
 knowledge_harness/templates/vault/prettierignore   | 11 ++++--
 skills/project-flow/SKILL.md                       |  2 +-
 skills/setup-vault/SKILL.md                        |  2 +-
 tests/test_templates.py                            | 34 +++++++++++------
 7 files changed, 67 insertions(+), 39 deletions(-)

## Diff
diff --git a/knowledge_harness/templates/vault/AGENTS.md b/knowledge_harness/templates/vault/AGENTS.md
index 4d833db..3482089 100644
--- a/knowledge_harness/templates/vault/AGENTS.md
+++ b/knowledge_harness/templates/vault/AGENTS.md
@@ -18,13 +18,13 @@ These seven are the user-invoked entry points — type the name to run one; an a
 | `find-sources`     | find literature before it is admitted to Zotero          |
 | `import-source`    | import, catalog, refresh, or backfill an admitted source |
 | `verify-citations` | verify citations and run the citation checks             |
 | `factcheck-draft`  | factcheck a draft against its sources before review      |
 | `publish`          | publish, park, correct, or withdraw a project            |
 
 Text between `%%hk-managed%%` markers is regenerated by the bridge — edits there do not survive. Free prose below the managed region is yours and persists.
 
 Machine surfaces (`log/`, `inbox/review-queue.md`, managed regions, `system/bibliography.json`) are owner-written: hand or tool edits are regenerated away or raise a finding.
 
-Formatters are writers too: `.prettierignore`, `.markdownlintignore`, and `.editorconfig` keep them off the machine surfaces.
+Formatters are writers too: `.prettierignore` and `.markdownlintignore` keep them off the machine surfaces; `.editorconfig` disables an editor's own trim/final-newline defaults there instead.
 
 `.harness/` is machine-local: nothing in it travels with the vault.
diff --git a/knowledge_harness/templates/vault/editorconfig b/knowledge_harness/templates/vault/editorconfig
index 02c3ee2..431dac6 100644
--- a/knowledge_harness/templates/vault/editorconfig
+++ b/knowledge_harness/templates/vault/editorconfig
@@ -1,37 +1,47 @@
 # Defends the machine-owned surfaces (literatures/, log/,
-# inbox/review-queue.md, system/bibliography.json) against ANY editor's
-# defaults, including the user's own -- not against this file's, which
-# imposes none. An editor that trims trailing whitespace or appends a
-# final newline on save would corrupt the append-only queue's byte-exact
-# contract or desync bibliography.json from what Better BibTeX wrote.
-# `false` is an active override, so it wins even where the user's own
-# editor is configured to do this globally; omitting the property
-# instead would yield to that global config, which is the threat this
-# file exists to stop. `root = true` makes this file authoritative: no
-# ancestor .editorconfig is read, so these overrides cannot be diluted
-# by one. Every other vault file is left to the user's own editor and
-# conventions -- this file makes no claim about them.
+# inbox/review-queue.md, system/bibliography.json, projects/*/search-log.md)
+# against ANY editor's defaults, including the user's own -- not against
+# this file's, which imposes none. An editor that trims trailing
+# whitespace or appends a final newline on save would corrupt an
+# append-only file's byte-exact contract or desync bibliography.json from
+# what Better BibTeX wrote. `false` is an active override, so it wins even
+# where the user's own editor is configured to do this globally; omitting
+# the property instead would yield to that global config, which is the
+# threat this file exists to stop. Every other vault file is left to the
+# user's own editor and conventions -- this file makes no claim about them.
+# Source of truth for this list: knowledge_harness/lints.py's
+# _is_append_only_path, plus system/bibliography.json (Better BibTeX's export).
+#
+# `root = true` is kept even with no [*] section to protect: it still
+# blocks ancestor-sourced charset/end_of_line values from reaching these
+# paths (closer-file precedence already gives this file's own sections
+# priority over any ancestor .editorconfig for a property both define, so
+# `root = true` is not what protects insert_final_newline/
+# trim_trailing_whitespace below).
 #
 # charset/end_of_line have no boolean "off" in the EditorConfig spec, so
-# they are not set here: with no [*] section and root = true blocking
-# ancestor files, there is nothing in this file's scope for `unset` to
-# undo, so it would be inert. This is a real, accepted residual gap: an
-# editor's own charset/line-ending default (not sourced from
-# EditorConfig) can still touch these four surfaces.
+# they are not set here: with no [*] section, there is nothing in this
+# file's own scope for `unset` to undo, so it would be inert. This is a
+# real, accepted residual gap: an editor's own charset/line-ending default
+# (not sourced from EditorConfig) can still touch these five surfaces.
 root = true
 
 [literatures/**]
 insert_final_newline = false
 trim_trailing_whitespace = false
 
 [log/**]
 insert_final_newline = false
 trim_trailing_whitespace = false
 
 [inbox/review-queue.md]
 insert_final_newline = false
 trim_trailing_whitespace = false
 
 [system/bibliography.json]
 insert_final_newline = false
 trim_trailing_whitespace = false
+
+[projects/*/search-log.md]
+insert_final_newline = false
+trim_trailing_whitespace = false
diff --git a/knowledge_harness/templates/vault/markdownlintignore b/knowledge_harness/templates/vault/markdownlintignore
index 0cb11ad..19ca418 100644
--- a/knowledge_harness/templates/vault/markdownlintignore
+++ b/knowledge_harness/templates/vault/markdownlintignore
@@ -1,6 +1,9 @@
 # Machine-owned surfaces: the CLI, Better BibTeX, and the bridge are their
 # only writers. markdownlint reformatting them would fight the trust machinery.
-literatures/
-log/
-inbox/review-queue.md
-system/bibliography.json
+# Source of truth for this list: knowledge_harness/lints.py's
+# _is_append_only_path, plus system/bibliography.json (Better BibTeX's export).
+/literatures/
+/log/
+/inbox/review-queue.md
+/system/bibliography.json
+/projects/*/search-log.md
diff --git a/knowledge_harness/templates/vault/prettierignore b/knowledge_harness/templates/vault/prettierignore
index cd7781f..ae1621e 100644
--- a/knowledge_harness/templates/vault/prettierignore
+++ b/knowledge_harness/templates/vault/prettierignore
@@ -1,6 +1,9 @@
 # Machine-owned surfaces: the CLI, Better BibTeX, and the bridge are their
 # only writers. Prettier reformatting them would fight the trust machinery.
-literatures/
-log/
-inbox/review-queue.md
-system/bibliography.json
+# Source of truth for this list: knowledge_harness/lints.py's
+# _is_append_only_path, plus system/bibliography.json (Better BibTeX's export).
+/literatures/
+/log/
+/inbox/review-queue.md
+/system/bibliography.json
+/projects/*/search-log.md
diff --git a/skills/project-flow/SKILL.md b/skills/project-flow/SKILL.md
index 5ff9709..f3b6001 100644
--- a/skills/project-flow/SKILL.md
+++ b/skills/project-flow/SKILL.md
@@ -15,21 +15,21 @@ Before framing anything or touching a draft, orient — whether this is a brand-
 1. Read `synthesis/index.md` — what topics the vault already arranges.
 2. Read the recent `log/` entries — what happened lately, across every project.
 3. If `projects/NAME/` already exists, read the project's files — the framed question, the draft frame, whatever is there.
 
 ### Drain the review inbox
 
 ```sh
 python3 -m knowledge_harness inbox --vault PATH
 ```
 
-Report the unacknowledged count and the oldest entry's date, oldest first — the Whittaker inbox-rot guard: a warn queue nobody drains is a silent failure. When the oldest entry is old, lead with it: name it first in your summary and say plainly that it has been sitting, rather than folding it into a routine count — an aging queue earns more prominence the longer it goes untouched. This is display only; nothing here blocks on age, and the inbox stays warn-tier regardless of how old an entry gets.
+Report the unacknowledged count and the oldest entry's date, oldest first — the Whittaker inbox-rot guard: a warn queue nobody drains is a silent failure. `inbox`'s summary reports `oldest_age_days` directly — whole days since the oldest entry, 0 for one filed today, `None` only when nothing is unacknowledged — so read that figure rather than computing the entry's age from its date yourself. When the oldest entry is old, lead with it: name it first in your summary and say plainly that it has been sitting, rather than folding it into a routine count — an aging queue earns more prominence the longer it goes untouched. This is display only; nothing here blocks on age, and the inbox stays warn-tier regardless of how old an entry gets.
 
 Every finding in that queue is another check's non-MATCHED result — UNMATCHED, UNREACHABLE, or SKIPPED (a MATCHED result never files a finding; it mints a `verified` event instead and never appears here). Never re-derive or restate a result from a note's frontmatter yourself, and never describe an UNREACHABLE finding as a failure — it means the check could not run, not that it disagreed. If a person asks whether a citation is trustworthy, route to `verify-citations` rather than guessing from what's in the queue.
 
 ### Surface trust tiers
 
 For every citekey the project's files cite, look up its tier:
 
 ```sh
 python3 -m knowledge_harness trust-tier CITEKEY --vault PATH
 ```
diff --git a/skills/setup-vault/SKILL.md b/skills/setup-vault/SKILL.md
index fa510ee..ccb2dff 100644
--- a/skills/setup-vault/SKILL.md
+++ b/skills/setup-vault/SKILL.md
@@ -24,21 +24,21 @@ Do not create directories or files by hand, substitute custom CI, use `git add .
 
 ## Diagnose
 
 Run doctor after scaffold. Either accepted base override position is valid:
 
 ```sh
 python3 -m knowledge_harness --base URL doctor --vault PATH
 python3 -m knowledge_harness doctor --base URL --vault PATH
 ```
 
-Report every doctor probe, not only failures, plus the inbox count and oldest age. Do not replace this with a `doctor --url` command or environment variable.
+Report every doctor probe, not only failures, plus the inbox count and oldest age. `inbox`'s summary reports `oldest_age_days` directly — whole days since that date, 0 for one filed today, `None` only when the queue is empty — so state that figure rather than estimating the age yourself. Do not replace this with a `doctor --url` command or environment variable.
 
 `doctor` always requires `--vault`; it has no vault-less mode. To check Zotero/BBT reachability before a vault exists, or independent of one, run `python3 -m knowledge_harness probe [--base URL]` instead — it is the vault-less reachability instrument and takes no `--vault` flag.
 
 ## Provision companions
 
 For each candidate in `PROVISION_COMPANIONS`, use this order: **Detect** → **Report** → obtain **per-item consent** → **Install or guide** → **Verify**. Detection and reporting never authorize installation. Do not propose or install unrelated packages.
 
 After per-item consent, the scriptable candidate may use `claude plugin install kepano/obsidian-skills`; tell the person it is restart-to-activate, then verify after restart. If consent is absent, give the install command as an optional next step and do not run it.
 
 Zotero .xpi installs are human-only wizard steps: BBT required; MarkDB-Connect optional. Explain where the person performs the wizard and what to verify afterward, but never download, never install, never click, and never close Zotero for the user.
diff --git a/tests/test_templates.py b/tests/test_templates.py
index e0bbd40..29b5130 100644
--- a/tests/test_templates.py
+++ b/tests/test_templates.py
@@ -136,22 +136,23 @@ def test_markdown_templates_match_canonical_content():
         "| `verify-citations` | verify citations and run the citation checks             |\n"
         "| `factcheck-draft`  | factcheck a draft against its sources before review      |\n"
         "| `publish`          | publish, park, correct, or withdraw a project            |\n"
         "\n"
         "Text between `%%hk-managed%%` markers is regenerated by the bridge — "
         "edits there do not survive. Free prose below the managed region is "
         "yours and persists.\n\n"
         "Machine surfaces (`log/`, `inbox/review-queue.md`, managed regions, "
         "`system/bibliography.json`) are owner-written: hand or tool edits are "
         "regenerated away or raise a finding.\n\n"
-        "Formatters are writers too: `.prettierignore`, `.markdownlintignore`, "
-        "and `.editorconfig` keep them off the machine surfaces.\n\n"
+        "Formatters are writers too: `.prettierignore` and `.markdownlintignore` "
+        "keep them off the machine surfaces; `.editorconfig` disables an "
+        "editor's own trim/final-newline defaults there instead.\n\n"
         "`.harness/` is machine-local: nothing in it travels with the vault.\n"
     )
     assert asset("vault/inbox/review-queue.md").read_text() == (
         '---\ntype: "review-queue"\n---\n'
     )
     assert asset("vault/synthesis/index.md").read_text() == "# Synthesis index\n"
     assert asset("vault/system/templates/literature.md").read_text() == (
         '---\ncitekey: "{{CITEKEY}}"\ntype: "literature"\n'
         'accessed: "{{TODAY}}"\nfixity-sha256:\n'
         'managed-sha256: "{{MANAGED_SHA256}}"\nstatus: "unscreened"\n'
@@ -299,50 +300,61 @@ def test_bases_and_machine_example_match_canonical_shapes():
     machine = json.loads(asset("harness/machine.json.example").read_text())
     assert machine == {
         "mailto": "you@example.edu",
         "path_map": {"D:\\Zotero\\": "/mnt/d/Zotero/"},
     }
     gitignore = asset("vault/gitignore").read_text()
     assert ".harness/\n" in gitignore
     assert ".obsidian/workspace*\n" in gitignore
 
 
+# Anchored (leading "/") gitignore-style form, as knowledge_harness/lints.py's
+# _is_append_only_path names the append-only three (log/, inbox/review-queue.md,
+# projects/*/search-log.md) plus system/bibliography.json (Better BibTeX's
+# export) and literatures/ (the evidence layer). Anchoring matters: an
+# unanchored "log/" also matches a nested projects/<name>/log/, silently
+# widening what a formatter skips.
 _MACHINE_SURFACES = (
-    "literatures/",
-    "log/",
-    "inbox/review-queue.md",
-    "system/bibliography.json",
+    "/literatures/",
+    "/log/",
+    "/inbox/review-queue.md",
+    "/system/bibliography.json",
+    "/projects/*/search-log.md",
 )
 
 
 def test_formatter_ignores_cover_every_machine_surface():
     # prettier and markdownlint both read gitignore-style patterns, so their
-    # ignore files list the four machine surfaces as plain gitignore lines.
+    # ignore files list the five machine surfaces as plain, anchored
+    # gitignore lines.
     for name in ("vault/prettierignore", "vault/markdownlintignore"):
         text = asset(name).read_text()
         for surface in _MACHINE_SURFACES:
             assert surface in text, (name, surface)
 
     # .editorconfig has no gitignore-style ignore mechanism, and ships no
-    # [*] section -- it defends only the four machine surfaces and makes
+    # [*] section -- it defends only the five machine surfaces and makes
     # no claim about any other vault file. `false` is the active override
     # for the two boolean properties: it wins even against a user's own
     # editor-wide setting, unlike `unset` or omission, either of which
     # yields to that global config (spec.editorconfig.org, "unset").
     # charset/end_of_line have no boolean "off" and are deliberately
-    # absent: with no [*] and root = true blocking ancestor files, there
-    # is nothing left in scope for `unset` to undo, so it would be inert.
+    # absent: with no [*] section, there is nothing left in scope for
+    # `unset` to undo, so it would be inert. EditorConfig glob sections
+    # have no gitignore-style leading-slash syntax; an interior "/" is
+    # what anchors them to this file's own directory.
     editorconfig = asset("vault/editorconfig").read_text()
     assert "root = true" in editorconfig
     assert "[*]" not in editorconfig.splitlines()
     for surface in _MACHINE_SURFACES:
-        glob = f"{surface}**" if surface.endswith("/") else surface
+        relative = surface.removeprefix("/")
+        glob = f"{relative}**" if relative.endswith("/") else relative
         assert f"[{glob}]" in editorconfig, (surface, glob)
     assert "= unset" not in editorconfig
     assert "charset =" not in editorconfig
     assert "end_of_line =" not in editorconfig
     assert editorconfig.count("= false") == len(_MACHINE_SURFACES) * 2
 
 
 def test_precommit_hook_is_executable_and_has_exact_contract():
     hook = asset("git/pre-commit")
     text = hook.read_text()
