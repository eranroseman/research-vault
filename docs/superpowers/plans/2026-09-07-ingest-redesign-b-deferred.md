# Part B deferred findings

Minor findings raised by the per-task reviews of `2026-09-07-ingest-redesign-b-compile.md` and
deliberately held out of their tasks' fix loops. None blocks a merge.

Rows are appended by the controller in a pathspec commit that follows the task's review, so the
record is written at the moment the finding is deferred rather than assembled at the end.

**Task 6 Step 1b's whole-branch review dispositions every row.** Each ends **fixed**, naming the
commit, or **declined**, with the reason written into the row. Rows still open after that review
become one repository issue (`gh issue create --label ready-for-agent`) with the open rows as its
body; Part A's #129 is the shape.

| #   | File                                                                               | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Task | Disposition                                                                                                                         |
| --- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `docs/terminology.md:154`                                                          | The reason-codes row carries the prose prefix "the `REASON_CODES` registry at HEAD:" that the plan's printed row lacks. Inert: the registry test captures only lowercase-hyphen backticked tokens, and the row is one physical row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 4    | open                                                                                                                                |
| 2   | `tests/test_skill_contracts.py:609`                                                | `test_terminology_registries_match_the_code` re-imports `re` and `inbox` locally; both are module-scope imports already (lines 9, 16). Verbatim from the plan's printed test (plan-mandated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 4    | open                                                                                                                                |
| 3   | `pyproject.toml:89`                                                                | A `per-file-ignores` entry (`PT018`) for `tests/test_capture_live.py`: the plan's printed write-leg assertion pairs two facts in one `assert`. Reviewer: sound, precedented (`test_cli_live.py` = `UP012`), no action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 5    | fixed — the paired postcondition is two asserts and the `PT018` entry is gone (b78e8b1, the plan owner's reversal of R9 at 5b7bf9a) |
| 4   | `tests/test_lints.py:1361`                                                         | `test_note_identity_needs_data_and_a_complete_tuple` names its `data=None` image `directory` (kind `"directory"`); after R18 narrowed `_note_identity` to the `image.data is None` guard the kind no longer matters, so the name is a holdover that suggests it does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 2b   | open                                                                                                                                |
| 5   | `tests/test_verify_cli.py`, `tests/test_lints.py`                                  | Three evidence-backed departures from the printed brief, recorded here as their durable home: (a) `test_verify_cli.py`'s byte-copied "add" fixture carried a valid machine-class `generated`, so under the amended legs it became an attested add with no row — the fixture is now a genuinely unattested note; (b) `extra["prior_path"]` is compared as its encoded `path-bytes:` string, since `Outcome.__post_init__` encodes a `RepoPath` in `extra` (`research_vault/outcome.py:58-93`); (c) the tuple-less-note pairing test edits both notes' bodies, because `_body_bytes` excludes frontmatter and a re-key alone would let the body pass mask the `continue`→`break` mutant. Reviewer verified all three against the code.                                                                                                                                                     | 2b   | accepted — no change needed; the plan's printed lines were the error (review of 710a820)                                            |
| 6   | `research_vault/compile.py`, `tests/test_compile.py`                               | Four printed lines contradicted standing repo gates and were corrected in-task, recorded here as their durable home: (a) the printed `plan()`/`records_for()` read the clock inline (`datetime.datetime.now(datetime.UTC).date().isoformat()`), which `tests/test_config_validity.py::test_the_package_reads_one_date_clock` forbids outside `clock.py` — now `clock.today(...)`; (b) the printed `ToolMissingError` message spelled the hyphenated skill name `setup-vault`, which `test_the_code_spells_no_hyphenated_skill_name` forbids in a non-docstring literal — the reference is dropped; (c) the printed test file's `import subprocess` was unused (F401) — removed; (d) six printed `assert a and b` statements trip `PT018` — each split in two. The printed `PIN = "32ac5a0"` was dropped as unread (R22, folded at 6c28a8d). Reviewer verified (a)–(d) against the gates. | 2    | accepted — no change needed; the plan's printed lines were the error (review of 36ab665)                                            |
| 7   | `research_vault/compile.py:292-294`                                                | `stable_source_id`'s non-`file` kind branch (`normalized = locator`) has no direct test; every caller passes `file`. The gate's clean pass covers the conditional indirectly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2    | open                                                                                                                                |
| 8   | `research_vault/compile.py:344`                                                    | `fulltext.path_for(vault_root, attachment_key)` is listed under the brief's Consumes but unused; `ledger_record` hand-builds `f"fulltext/{key}.md"` as `notes.py:395` already does. The Consumes line is imprecise rather than the code wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2    | open                                                                                                                                |
| 9   | `skills/capture-source/SKILL.md:73`, `skills/synthesis-conventions/SKILL.md:12,24` | Three departures from the printed skill text, recorded as their durable home: (a) `wiki-ingest` is written unbackticked (R25) — `test_every_skill_name_a_shipped_skill_cites_has_a_skill_directory` reads a backticked kebab token as a route to `skills/<name>/` unless the code spells it, and nothing spells `wiki-ingest`; find-sources:11 is the precedent for a foreign skill in prose; (b) the `## Frontmatter` sentence names the tool's full `PAGE_TYPES` at 32ac5a0 — `source`, `entity`, `concept`, `question`, `comparison`, `session`, `overview`, `meta`, `fold` (`claude_obsidian/page_schema.py:36-46`, read 2026-09-16) — instead of the printed four, which were ad67087's (R26); (c) evidence-conventions:16's edit also turned "from synthesis into a draft" into "from there into a draft", the one word the retired term left dangling. Reviewer verified (a)–(c). | 3    | accepted — no change needed; the plan's printed lines were the error (review of 5deb75e)                                            |
| 10  | `skills/setup-vault/SKILL.md:41`                                                   | "the scriptable candidate may use `claude plugin install kepano/obsidian-skills`" is singular and now precedes a second scriptable companion (the compile tool, three paragraphs later); a reader could take the loop's script-install step as exhausted by the first. Pre-existing line outside the brief's edits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 3    | open                                                                                                                                |
| 11  | `tests/test_zotero.py:272-280`, `tests/fakes.py`                                   | The printed `test_authorize_waits_for_the_person_not_the_network` used `fake.register`, `fake.authorize()`, `c.url` and `call.timeout`, none of which `FakeZotero` offers (`calls` are `(verb, path, headers)` tuples). The test uses `fake.post` and `fake.client.authorize()`, and the fake records a `timeouts` list parallel to `calls` in `_http` and `_rpc_call`; name and assertion (`== zotero.AUTHORIZE_TIMEOUT`) as printed. Reviewer verified no `calls` consumer broke.                                                                                                                                                                                                                                                                                                                                                                                                      | 5    | accepted — no change needed; the plan's printed test was the error (review of b9ad2b4)                                              |
| 12  | `tests/fixtures/lifecycle/README.md:18-19`                                         | The measured-facts bullet adds the clause "(no key store carried over; Always Allow does not silence a later `authorize`)" — a mechanism read from one observation (the dialog re-opened on a second run against a fresh `tmp_vault`, 2026-09-16), stated slightly more generally than the observation itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 5    | open                                                                                                                                |
