# Plan W: Quality Tail — Implementation Plan

Disposition: pending-issue: 42 (2026-09-06)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use `- [ ]`. Split from the pre-slice batch 2026-08-24 (review finding: the batch's header promised a single gating dispatch; this tail is non-gating and runs on main AFTER the batch's Task 21 merge).

**Goal:** The post-merge quality tail: mutmut adoption (author-ruled on the landed pilot), comment hygiene, suite hermeticity + speed, version currency, CI hardening, and the full 26-module mutation baseline.

## Global Constraints

- Runs on main, post-batch-merge. Read this plan from origin/main at each task start.
- **Sequencing amended 2026-09-13** (assembly spec §7.1.1): this plan runs after Part A of the ingest redesign (`2026-09-07-ingest-redesign-a-capture.md`) merges and before Part B (`…-b-compile.md`) dispatches, so Part B's code is measured by a gate that runs. Part A's Task 20 measured why: its mutate4py gate is an 8–17 hour serial run (one forked child per mutant running the whole 69 s suite, ~840 mutants, parallel forbidden by a ~199 MB/s leak), three of the branch's changed modules (`events.py`, `frontmatter.py`, `gitstate.py`) sit on the six-module exclusion list and are measured nowhere, and the gate has still never executed in CI. Consequences: Part A skips its post-merge `--update-baseline` commit when this plan follows in the same working session (Task 25 writes the baseline), and Part A's deferred rows 1–4 (stale `.py.manifest.json` sidecars) are declined there, since Task 25 retires the sidecars wholesale.
- Every test Run begins `source .venv/bin/activate`. Suite green at each task's end. Conventional commits, pathspec form (`git commit -- <files>`). The `Co-Authored-By` trailer names the model that authored the commit; a trailer that differs from any printed example is not a deviation to itemise.
- Task numbering preserved from the batch (23–25 with 24b/c/d) so ledger references stay valid.
- **Deferred findings have a committed home** (added 2026-09-13, the rule `AGENTS.md` states for task reports): every minor a review defers and every concern an implementer cannot act on is appended, as it arises, to `docs/superpowers/plans/2026-09-13-plan-w-quality-tail-deferred.md` on the executing branch — one row per finding with the task, the file and line, and the finding — because the SDD ledger is deleted at Finish and a report is not a durable home. Task 25 Step 2b's results file dispositions every row (fixed with the commit named, declined with the reason, or filed as one `ready-for-agent` issue) before the workspace closes. Task 23's one concern and Task 24's seven deferred minors are the first rows.

______________________________________________________________________

## Part 4: mutmut adoption (author-ruled 2026-08-23 on pilot data — NON-GATING: runs AFTER Task 21's merge; slice Phases 2–6 unblock at Task 21 regardless)

The pilot (report: /home/eranr/kh-mutmut-pilot-report.md; mutmut 3.7.0) measured all three representative modules to completion including events.py — the exit-4 class is structurally absent under mutmut's env-var schemata (collection-time code stays importable; a collection-breaking mutant counts as a kill, verified live). Baseline correspondence sane (all 19 function-level mutate4py survivor locations carried). Cost: two documented tool-defect shims (embedded verbatim in the report): a sitecustomize shim for mutmut's config-load-at-import (false-kill risk otherwise) and a launcher widening pytest 9's re-escaped control-char node ids. Each Part 4 task runs on main after the Task 21 merge, conventional commit per task.

### Task 23: Ruling record + shims land with provenance

**Files:** Modify: `docs/superpowers/specs/2026-08-16-foundation-spec.md` (§10 mutate4py exit-rule entry). Create: `scripts/mutmut_shims/sitecustomize.py`, `scripts/mutmut_shims/run_mutmut.py` (the launcher), `docs/research/2026-08-23-mutmut-defect-reports.md`.

- [ ] **Step 1:** Spec §10 amendment appended to the exit-rule entry: *"CLOSED EARLY by author ruling 2026-08-23 on the landed pilot (its pre-registered method ran; report banked): switch to mutmut 3.7.0. The upstream-responsiveness branch is superseded — the pilot showed complete coverage where the pin had a 23% hole over trust-core modules; the mutate4py upstream reports remain queued for filing as citizenship."*
- [ ] **Step 2:** Copy both shim files VERBATIM from the pilot report into `scripts/mutmut_shims/`, each with a header comment naming the upstream defect it works around and the report as provenance. Draft the two mutmut upstream defect reports (config-at-import; node-id re-escaping) into `docs/research/2026-08-23-mutmut-defect-reports.md` — ready-to-file, the author posts.
- [ ] **Step 3:** Pin `mutmut==3.7.0` in dev extras (3.8.0 is current on 2026-09-13; both shims patch 3.7.0 internals — the `Config` field list and `PytestRunner._pytest_args_regular_run` — verified in the pilot, so a bump is a re-verification this plan does not budget). `[tool.mutmut]` in `pyproject.toml` is Task 24's, not this task's. Commit `feat: adopt mutmut 3.7.0 — ruling, shims, upstream report drafts`.

Pilot registered 2026-08-25 (plugin-comparison validation, blind spot b): route ONE of this plan's controller rulings through an issue comment instead of the direct dialog channel and record the round-trip time against the blocked implementer — the tickets-as-backbone model assumes tracker-mediated rulings are viable and no event has tested it. One ruling, measured, then revert to dialog.

Measured caveat for this Part (Task 19b round 2, 2026-08-24): a `coverage.py` 100%-branch number can be true and misleading — short-circuit sub-expressions inside a single `return` are outside its branch model (the `pretooluse_guard.py:75` fail-open guard sat invisible under 100%). Mutation results are the check on coverage numbers, not the other way around; treat "100% branch" as "100% of what the model sees." Same family, from Task 20: a correct observation can carry a wrong conclusion — "2 of 9 cases only go red under both guards" was true, "so one guard is redundant" would have shipped an uncaught TypeError; honest self-reports are worth more than silence AND still need checking, and the honesty is what makes the wrong conclusion persuasive.

### Task 24: Gate script speaks mutmut

**Files:** Modify: `scripts/mutation_gate.py`, `tests/test_mutation_gate.py`.

- [ ] **Step 1: Failing tests first** — port the existing gate contract to mutmut's result format: survivor extraction from `mutmut results`/its cache, baseline compare (no-new-survivors), `--update-baseline` writes the full-module baseline, refuse-to-write on any module failure (the Q1 guard survives the port), two-class failure attribution retained. **Stale-bytecode guard (corrected 2026-09-13):** `PYTHONDONTWRITEBYTECODE=1` blocks *writing* a pyc, not *loading* one — Part A measured a same-length edit inside one mtime second reusing the previous mutant's bytecode under CPython's `mtime + size` validation — so the gate sweeps `__pycache__` under `research_vault/` and `tests/` before every mutate-run-restore cycle and confirms the tree byte-identical after the restore; pin the sweep in a test, and keep the env var as belt. **What the gate grew since this plan was written, classified for the port:** the `--memory-cap` wrapper (`systemd-run` MemoryMax), the six-module `mutation-exclusions.txt` with its qualified pass line, the `.bak` restorer for a mutant left live on SIGINT, and the memcap failure class are mutate4py-specific and retire with it; the `--out-dir` per-module resume records and the baseline key format (`<relpath>::<func-id>::<mutation>`, `module::` for import-time sites) port. Record in the gate's header what a line-number-free key can and cannot answer: "no new survivor" — never "the rewrite fixed the old ones", because a function rewritten on a branch that keeps a baselined mutation text still reads ok. **Rulings from the executing controller's pre-flight scan (2026-09-13), folded in:** (a) this task owns `[tool.mutmut]` in `pyproject.toml` — `source_paths`, `pytest_add_cli_args`, and an `also_copy` re-derived for this repository (the pilot's list was knowledge_harness's); `mutants/` joins `.gitignore`. (b) mutmut's mutant ids are positional (`<module>.x_<func>__mutmut_<N>`, renumbered by any edit to the function — pilot appendix), so they are not the key: `<func-id>` is `func/<name>` from `orig_function_and_class_names_from_key`, and `<mutation>` is the `-`/`+` body lines of `mutmut.__main__.get_diff_for_mutant(name, path=…)` with hunk headers dropped; results are read from `mutants/<relpath>.meta`'s `exit_code_by_key` through `status_by_exit_code` (0 survived; 5 and 33 no tests; 34 skipped; anything else suspicious or timeout, reported by name and never folded into ok or survivors). (c) The stale-bytecode guard's object moved with the tool: mutmut never edits the source (schemata live in `mutants/`), so the gate sweeps `__pycache__` under `research_vault/`, `tests/` and `mutants/` before each invocation, hashes `research_vault/` and `tests/` before and after and fails if they differ (proof mutmut left the source alone), and keeps `PYTHONDONTWRITEBYTECODE=1`. (d) Named integration risk: the `mutants/` tree adds mutant sibling functions with mutated literals, and `tests/test_config_validity.py:122`, `tests/test_doctor.py:156` and `tests/test_skill_contracts.py:205,334` AST-parse package modules for literals — the pilot needed `only_mutate` for the same reason; the implementer picks the mechanism (skip `__mutmut_<n>` siblings when inventorying, or equivalent), and excluding `scaffold.py` from mutation is not acceptable. (e) The ported gate has no exclusions concept — mutmut measures every module; `mutation-exclusions.txt` is deleted in Task 25 with the sidecars and the pin, and between this task and Task 25 the committed baseline still holds mutate4py keys, so the gate reads FAIL on the branch — accepted, the two land together. (f) The launcher passes `--max-children N` through. (g) From Task 23's report: the launcher's `--help` fails with `FileNotFoundError` when `MUTANT_UNDER_TEST` is unset and no `[tool.mutmut]` exists, because the shim's config seed only runs inside a mutmut run — this task makes `--help` answer before any config or environment is consulted, so Task 23's smoke check 3 passes as literally written once this task lands.
- [ ] **Step 2:** Implement; per-module invocation through `scripts/mutmut_shims/run_mutmut.py`; keep `--out-dir` durability + resume. Full suite. Commit `feat: mutation gate runs mutmut`.

### Task 24b: Comment-hygiene sweep (ruled 2026-08-23 — runs BEFORE Task 25 so the baseline hashes the cleaned tree once; docstrings live in the AST)

**Files:** Modify: `research_vault/*.py`, `scripts/*.py`, `tests/*.py` (comments and docstrings only — zero behavior changes).

- [ ] **Step 1:** Detect mechanically: grep the Python tree for date stamps, ruling language, and plan/task references in comments and docstrings (`2026-`, `ruled`, `Plan [A-Z]\b`, `Task [0-9]`, `previously`, `postmortem`, `superseded`, `renamed from`). Run 2026-09-13: those eight patterns hit 586 sites (27 kept, 45 rewritten, 2 deleted, AST-equal on 26 files), and the review found a same-class residue in sixteen more places they never reach — review-finding labels (`F-N`, `C-N`), standalone `ruling N`, and review-round banners — so the detector is also `\bF-[0-9]\b|\bC-[0-9]\b|ruling [0-9]|review findings`, and a negative grep over both sets closes the step.
- [ ] **Step 2:** Judge each hit against the house doctrine: a comment states a constraint the code cannot show; provenance, history, and correctness arguments belong to git log. Delete what fails; keep load-bearing constraints (e.g. copyright/license/pinned-commit headers in vendored code stay). **One exemption the doctrine must state (2026-09-13):** `AGENTS.md` requires a fact a probe cannot answer to be recorded where it is used, with its method and date — Part A added many such comments deliberately ("Measured 2026-09-07: the local API ignores a `linkMode` query filter", the export-timeout measurement, the pyc-validation note), and Step 1's `2026-` pattern hits every one of them (582 hits across `research_vault/`, `scripts/` and `tests/` at Part A's close). A dated measurement with a method stays; a dated ruling, task reference or history note goes. Record the judged keep-list in the commit body.
- [ ] **Step 3:** Full offline suite green (comments only — any test failure means the sweep touched behavior; revert that hunk). Commit `style: comment-hygiene sweep — history and rulings out, constraints stay`.

### Task 24c: Suite hermeticity + fixture speed (decided 2026-08-24 — runs BEFORE Task 25: the mutation gate re-executes covering test slices per mutant, so every second cut here multiplies by mutant count)

Headline defect, fixed first: the offline suite is NOT hermetic — 97 connects to localhost:23119 in one offline run (unmarked tests in test_verify_cli.py, test_okf.py, test_publish.py do real BBT CSL exports). Offline green was machine-dependent: Zotero up = live calls; Zotero down = each connect eats the 5 s timeout. Measured baseline: serial 67.4 s / 1532 tests; proof of fixability: with sockets patched out, those files pass 233/233 with zero assertions needing a socket. **Status 2026-09-13:** Part A's Task 12 closed the Zotero half — `tests/conftest.py::_no_zotero_socket` (autouse) replaces `ZoteroClient._http` with an immediate refusal for every test not marked `live`, so an unpatched read is an outage, not a connect, and the 5 s timeouts are gone with it. It is a client patch, not a socket block: Step 2 narrows to the external-registry callers (`RV_LIVE_NET` legs and anything else that opens a socket) and to pinning the guard at socket level so a new client class cannot reopen the hole. Current measurement at Part A's close: 1717 tests, 69 s serial, ~14 s under `-n auto`.

**Files:** Modify: `tests/conftest.py`, `pyproject.toml` ([tool.pytest.ini_options]), the fixture sites the steps name; `docs/testing.md` (one doctrine line, same commit as Step 2).

- [ ] **Step 1: `dead_base` fixture** — bind an ephemeral port, close it, hand out `http://127.0.0.1:<port>` (measured: 1 ms refusal vs 5029 ms on port 1 under WSL2). Replace the dead-port test bases; this is the only fix that reaches `test_probe_unreachable`'s subprocess CLI. (−15 s) Status 2026-09-13: `tests/test_zotero.py:401` already carries a local `_dead_port_base()`; `tests/test_cli_live.py:31` still uses port 1 — this step promotes the helper to a conftest fixture and sweeps the remaining sites.
- [ ] **Step 2: Socket-block offline runs** — autouse conftest patch (or pytest-socket) raising on any connect, disabled when RV_LIVE/RV_LIVE_NET is set. Sweep the unmarked live-touching tests onto dead_base/fixtures until offline passes fully blocked. Add one line to docs/testing.md: offline runs are socket-blocked; a test needing network carries a live marker. (−9 s, and offline green stops depending on Zotero)
- [ ] **Step 3: Session-scoped template vault** — build one vault per worker, `copytree` per test (measured 0.319 s vs 0.554 s per 30 builds; `.git` copies fine, function-scope copy keeps isolation). (−8 s)
- [ ] **Step 4b (config audit 2026-08-24):** `addopts = ["--strict-markers"]` in pyproject — verified safe (suite uses only live/live_net/parametrize/skip; the xdist ruling forbids `-n` in addopts, not addopts). Without it a typo'd `live_net` mark runs silently and makes REAL external API calls in offline runs.
- [ ] **Step 4:** `tmp_path_retention_policy = "failed"` in pyproject (−3 s; kills the 249 MB retained pile). Then `-n auto --dist worksteal` on `quality.yml`'s pytest step (it carries no `-n` today, so `--dist` alone is meaningless; the "never in addopts" ruling forbids addopts, not CI's command line, and CI has no Zotero while Step 2 blocks sockets) — tail latency only, safe once the 5 s tests are gone.
- [ ] **Step 5: Acceptance** — offline suite green with sockets blocked and ZERO connects to 23119; serial ≤ ~40 s; live suite (both flags) still green. The `RV_LIVE_NET` leg sends the mailto to Crossref's polite pool, so it runs only with an address the operator supplies for the run (`RV_MAILTO`, or the machine config's `mailto` on the operator's say-so) — never one an agent picks; without it, this step records the registry leg as not run for want of a mailto and Task 25 Step 1's full verification runs it. Commit `perf: hermetic offline suite + fixture speed`. **Measured 2026-09-13 (`f2afa2d`):** serial 66–68 s → 48–53 s, `-n auto` ~20 s → ~10.5 s, zero connects to 23119, both live legs green (1735 passed, 1 skipped, the registry leg with an operator-supplied mailto in the environment only). The ≤ ~40 s target was not met and the miss is attributed, not looped on: Part A had already banked the Zotero connects and two of the three dead-port sites, so Steps 1 and 2 delivered about 5 s here, and what remains is test-body work — git snapshots in `verify_state` and hook or CLI subprocesses at roughly 30 ms per test with no outlier — a lever outside this task's files. Two follow-ups ride Task 24d: the socket block keys on markers alone (the env flags unlock the marked tests, which already skip without them, and must not switch the block off for unmarked tests — as built, `RV_LIVE=1` disabled it entirely, which is why the marker branches at `conftest.py:304-305` were unreachable; the pin test then runs under every flag), and `docs/testing.md:15` stops naming the non-existent `tests/test_dispositions.py`.

### Task 24d: Version currency (checked 2026-08-24 — runs BEFORE Task 25 so the baseline hashes the post-ruff tree; a later ruff bump would force a re-baseline)

- [ ] **Step 1: ruff 0.15.21 → the current release, by the recorded protocol** (0.16.4 was current on 2026-08-24; `main` still pins 0.15.21 on 2026-09-13, actions are still v4/v5 and shellcheck still rides the runner image, so every task below stands but each named target version is re-read at execution) (pyproject's own upgrade note): diff `ruff check --isolated --show-settings` against the deliberately-unselected families (BLE/D/TRY/PL — extend-select cannot unselect defaults); move any newly default-enabled skip into `ignore` with its recorded rationale intact. Three places move together: the dep pin, `required-version`, the pre-commit lane. Land trivial autofixes in the same commit; report anything larger.
- [ ] **Step 2 (sharpened by the 2026-08-24 CI audit — shellcheck is the lane's ONE unpinned tool, riding the runner image; when ubuntu-latest migrates to 26.04 it jumps to 0.11.x and the vault pre-commit template starts failing on a change nobody made):** install shellcheck by the shfmt pattern — pinned version + release URL + `sha256sum -c` digest — and delete the ships-in-the-image exception; the "pinned exact, like every other tool" comment becomes true. Also: `pyproject-fmt` 2.28.0 → 2.28.1.
- [ ] **Step 3: Actions v4/v5 → v7** in `.github/workflows/quality.yml` AND both vault CI templates (`research_vault/templates/ci/{verify,rw-batch}.yml`) — the templates render into every user vault, so stale actions ship to users, not just this repo. Template pins update same commit.
- [ ] **Step 4 (CI-audit folds, same files):** (a) `raven-actions/actionlint@v2` is the lane's one floating third-party tag executing arbitrary code — ELIMINATE it via the pinned-binary-download pattern (actionlint ships release assets; digest-checked like shfmt); SHA-pinning is the fallback only if the binary route fights the job. (b) actionlint's invocation gains the two vault CI templates as file arguments — they render into every user vault and have never been lint-checked. (c) `shfmt`'s install gains its digest check (verified: `fb096c5d1ac6beabbdbaa2874d025badb03ee07929f0c9ff67563ce8c75398b1  shfmt_v3.13.1_linux_amd64`) — a tag pin is not a content pin; v3.13.1 stays even though v3.14.1 is current on 2026-09-13, because shfmt owns the vault pre-commit template's form and a bump is a format-review act outside this plan. (d) Add the concurrency group (`${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress` ONLY for pull_request — cancelling a push-to-main run would abandon a landing). (e) `persist-credentials: false` on checkout (verified: nothing after checkout fetches); fetch-depth comment names the mutation-gate consumer.
- [ ] **Step 4c (pyproject audit folds, 2026-08-24):** (a) version single-sourced: `dynamic = ["version"]` reading `research_vault.__version__` — eliminates the one untested copy; the two remaining are test-pinned. (b) `pytest` pinned exact (lane doctrine: dev tools pin exact; it currently floats across a major); `pypdf>=4` keeps its floor WITH a comment (user-installed feature-detected extra — floors are the design there). (c) mypy gains `python_version = "3.11"` and ruff's floor comment updates two→three movers; mypy `files` grows to cover `scripts/` and `hooks/` (the gate logic and shipped hooks are type-unchecked today) — report rather than land if the new surface yields non-trivial errors; the mutmut shims monkeypatch untyped internals, and a `mutmut.*` `ignore_missing_imports` override lands if that is all it takes. (d) `[project]` gains `license` (SPDX form) + `urls` matching plugin.json — coupled: SPDX string needs `setuptools>=77`, so the build floor bumps in the same commit, with one comment on why the backend floor floats at all.
- [ ] **Step 4d (riders from Task 24c's review, 2026-09-13):** `tests/conftest.py::_no_socket` drops its `RV_LIVE`/`RV_LIVE_NET` short-circuit so the block is decided by the `live`/`live_net` markers alone, and `tests/test_config_validity.py`'s pin for it loses its skip under the live flags; `docs/testing.md:15` drops the reference to `tests/test_dispositions.py`, which does not exist.
- [ ] **Step 5:** Suite green; commit `chore: version currency + CI hardening — every tool pinned and digest-checked`. **Landed 2026-09-13 (`d402858`, `7324e11`; review clean):** ruff 0.15.21 → 0.16.7, whose default rule set grew from 59 to 413 — held by `ignore` prefixes (`BLE`, `D`, `PLC`, `PLR`, `TRY`) plus explicit `E4`/`E7`/`E9`/`F` selection and `extend-exclude` for `*.md` (0.16 formats Python fences), with every one of 0.15.21's 59 defaults present in the effective set and the `D` prefix verified not to swallow `DTZ`; pyproject-fmt 2.28.0 → 2.29.4, which changed the canonical form itself (sub-tables hoisted, keys sorted) and so re-formatted the untouched file too; pytest pinned exact at 9.1.1; `dynamic = ["version"]` from `research_vault.__version__`; SPDX `license` with `setuptools>=77`; mypy over `hooks`, `research_vault`, `scripts` at `python_version = "3.11"`, clean; actions v7 in `quality.yml` and both vault CI templates with the test contract updated; shellcheck v0.11.0 and actionlint 1.7.12 as pinned, digest-checked binaries (digests confirmed against GitHub's asset digest field), shfmt's digest, the concurrency group, `persist-credentials: false`. Step 4d landed with the guard gaps the implementer found in its own work: the pytest pin joins the pinned-tools test, and four new ruling comments join `RULING_ANCHORS` so pyproject-fmt's reordering cannot detach them. Three minors deferred to the register.

### Task 25: Full baseline over every module — the hole closes

- [ ] **Step 1:** Blanket baseline over ALL modules under `research_vault/` — 34 after Part A, which added `addons`, `capture`, `captured`, `clock`, `lifecycle`, `propagate`, `paths` and `stamp` and deleted `archive` (26 when this plan was written) — including the six mutate4py could not measure (`events`, `frontmatter`, `gitstate`, `outcome`, `pathcodec`, `selectors`; three of them carry Part A changes that have never been mutation-tested), under the 8 GB cap; record per-module wall-clock and the one-time stats-build cost in the task report. Scale, measured by Task 24's integration run (2026-09-13): the stats phase is a one-time ≈607 s; 17,153 mutants across the 34 modules (about eight times mutate4py's site count); per-module rates from 0.41 mutations/s (`clock.py`) to 10.9 (`stamp.py`), so the blanket run is hours, not minutes — run it in the executing worktree with `--out-dir` so it resumes, drive it module by module with `--only` if a session boundary falls inside it, and stop the mutate4py run in `/tmp/rv-gate` first if it is still going. **From Task 24's run, for this step:** `rm -rf mutants/` before the blanket run — mutmut's stats cache re-collects only for new test names and snapshots the git commit, so a tree that changed `pyproject.toml` or `.gitignore` since the last full run keeps stale cached results behind a warning until rebuilt; drive it with the `--max-children` count Task 24 measured safe on this machine; expect the survivor volume to be far larger than the mutate4py list (mutmut's string wraps, argument-to-`None`, keyword drops and `continue`/`break` swaps — the class-level ruling on #42 governs the triage); the `no tests` set is small (34 mutants at the stats preview — `__main__` 23, `verify` 7, `webapi` 4 — the subprocess-only coverage the gate's header documents) and is reported by name, never folded into ok or survivors; a module with no function would read as `error` today (mutmut asserts on an empty name pattern), which is the loud failure wanted until a constants-only module exists. **Triage input (2026-09-13):** Part A's full mutate4py run at `9bd1f1c` (`/tmp/rv-gate.log`; its per-module survivor lists are copied into Part A's Task 20 report) is the only record of the survivors Part A *introduced* — capture, captured, lifecycle, propagate, addons, clock, stamp, paths and the rewritten `__main__`, `verify`, `notes`, `inbox` — since this task's blanket baseline cannot tell them from older ones afterwards. Before writing the baseline, disposition that list per survivor: killed by a test in this task, or accepted into the baseline with the reason recorded in the task report; a survivor that names a real test gap (a poll deadline compared with `>=` where `>` also passes; a batch limit `> 50` where `>= 50` also passes) is killed, not baselined. **Scope of per-survivor triage, ruled 2026-09-13 as Task 25 started** (17,153 mutmut mutants make "every survivor" thousands of dispositions; the #42 ruling's classes (a)–(c) are accepted by class everywhere with counts, three samples and the grep basis per file): per-survivor means (i) mutmut survivors in the functions Part A's mutate4py list names, mapped by `func/<name>`; (ii) the six functions Part A changed in the three modules mutate4py could not measure — `events.py` `_extra_lines`, `record_pass`, `current_failures`, `trust_tier`; `frontmatter.py` `render_field`; `gitstate.py` `_allowed_manifest_path` — since nothing has ever mutation-tested them; (iii) any survivor of the named gap shapes (boundary operators, numeric limits, boolean and control-flow flips) in a Part A module. Pre-Part-A modules enter the baseline wholesale, which is what `--update-baseline` has always meant. Measured outcome of the triage (2026-09-14, Phase 2): 3,420 survivors → 415 in class (a), 0 in (b) (mutmut does not mutate docstrings), 155 in (c), 2,850 behaviour; 892 in scope, 675 killed by 175 new tests across 27 files and 217 accepted with reasons; the one remainder is `verify.py`'s snapshot-driven core — thirteen functions, fifty in-scope survivors — which is recorded in the results file with the function list and filed in the `ready-for-agent` issue rather than dispositioned here, the sanctioned outcome for that module. Behaviour survivors in Part A's eight new modules outside (i)–(iii) are **not silently baselined**: they enter the baseline with a per-module count in the results file and are filed, with that count, in the one `ready-for-agent` issue this plan's dispositions produce, so brand-new code's test gaps stay visible.
- [ ] **Step 1b (CI audit 2026-08-24 — the mutation gate has NEVER actually executed: push runs no-op by construction and this repo has zero PR runs, so every claim in the gate's comment block is reasoned, not observed):** after the port and baseline land, `gh workflow run quality.yml --ref <branch-touching-core>` and OBSERVE a real gate execution end-to-end. Then set `timeout-minutes` from the measured bound — do not invent the number before the first real run measures it. Ordering and side effect, ruled 2026-09-13: Step 2's commit lands first, the executing worktree branch (never `main`) is pushed to origin for the observed run — a sanctioned remote side effect, since the run needs a branch touching core — and Step 3's commit carries `timeout-minutes` from the measurement. **What the observed run measures, ruled 2026-09-14 as it started (run 34822363722, 08:22Z):** gate mode diffs the branch against `origin/main`, so this branch's run mutates nearly all 34 modules at two children on the runner — the whole-branch integration case, not a PR's. If it finishes inside the 360-minute job limit, that is the ceiling for an integration-sized diff and `timeout-minutes` takes it with headroom. If it hits the limit, the measurement is "an integration-sized diff exceeds the job limit", and `timeout-minutes` is set instead from a second observed run on a PR-sized diff (a `workflow_dispatch` on a branch that changes one or two modules — or the local per-module rates scaled from six children to two, stated as the basis). Either way the steady state is small diffs, and the oversize case gets a mechanism rather than a silent timeout: the gate gains a CI budget (`--max-mutants N`, from the measured per-mutant cost and the job limit) and, when the changed set's mutant count exceeds it, prints a qualified line — `not measured: <N> mutants across <M> changed modules exceed the CI budget of <B>; run the gate locally` — and exits without mutating anything, so the advisory lane reads "unmeasured" instead of "timed out" (the four-state rule: an unrun check never reads as a pass or a failure). `quality.yml` passes the budget; Phase 3 lands both. **First observed run, 2026-09-14 08:22–08:44Z (run 34822363722): FAILURE in the gate step, self-inflicted, and the measurement it banked is real.** Everything before the gate was green on the runner — tests and coverage in 53 s under `-n auto --dist worksteal`, the pinned actionlint, shellcheck and shfmt, CRAP under the ceiling — and the changed set was ten modules, not thirty-four. The gate then ran with `--child-address-space 3GiB`, which sets `RLIMIT_AS` soft and hard on the mutmut process; mutmut's stats phase executed the gate's own test `test_run_mutmut_invokes_the_launcher_with_pattern_env_and_cwd` in-process, that test calls `_run_mutmut` with the default 4 GiB cap, the new pre-check raised `ChildLimitError` (hard 3 GiB < 4 GiB), stats failed, and every changed module classed `error` after about two minutes each — nothing mutated. The lesson generalises: a mutation run executes the gate's own tests inside the gate's own limits, so those tests must not depend on the inherited process limits. Phase 3a: the gate's tests take the limit through a seam (an explicit cap argument or a monkeypatched `getrlimit`), the hard limit stays at the cap so a real nested gate above the outer cap still refuses, `--max-mutants` lands; review; push; observe again. Phase 3b: `timeout-minutes` from the second observed run, the results file, the deferred dispositions, the mutate4py retirement, the Step 3 commit. **Phase 3a landed and was observed (2026-09-14):** `1a71be3` (the gate's tests read the limit through a seam; seven failed RED under a 3 GiB `ulimit -v`, then the whole suite passed serially under the cap) and `17e19b2` (`--max-mutants`, counting in memory through mutmut's own mutator with the count matching every cached `.meta`; `quality.yml` passes a provisional 9,000 = (360 min × 0.75 − 10 min) × 0.6 mutants/s, to be replaced by the measured rate); review clean. Observed run A on this branch (34827049015): success in 2 min 41 s, gate step 51 s — `not measured: 10194 mutants across 10 changed modules exceed the CI budget of 9000; run the gate locally`, exit 0 — the budget mechanism seen end to end. Observed run B measures the runner's rate on a PR-sized diff: a throwaway branch `plan-w-observe-rate` (this branch plus one comment fix in `scaffold.py`, its workflow diffing against this branch) so the changed set is `scaffold.py` alone, 1,308 mutants at two children — its gate time is the per-mutant cost the budget and `timeout-minutes` are set from; the throwaway branch is deleted from origin once the numbers are banked. **Run B's result (34827110718):** the gate step took 41 min 02 s for 1,308 mutants at two children — about 0.53 mutants/s step-inclusive, about 0.6/s in the mutant phase after roughly five minutes of fixed cost — so the provisional rate held and the budget and `timeout-minutes` derive from it (the arithmetic recorded in the results file). Its verdict was FAIL with five new survivors against the committed baseline, and all five were real test gaps the local run had masked, which is the measurement's second yield: four git-subcommand mutants (`ls-files` and `check-ignore` mutated to invalid subcommands) read `timeout` locally, because an invalid subcommand makes git walk `PATH` and under WSL2 that exceeds mutmut's limit, and the gate never baselines a timeout — on the runner the same calls exit 1 in milliseconds and the mutants survive, since under `check=False` an invalid subcommand reads as "not tracked" or "not ignored" and no test asserted the positive case; the fifth (`_installed_plugins`'s `encoding="utf-8"`) was killed locally only because the one test reaching it read the developer's real `~/.claude/plugins/installed_plugins.json`. Two lessons become text and one a mechanism: a local `timeout` verdict is environment-dependent and can mask a survivor, so the runner is the arbiter and a locally timing-out mutant is killed by a test when a test can reach it; and no test may depend on the developer's home — Phase 3b adds an autouse fixture in `tests/conftest.py` that points `HOME` (and `XDG_CONFIG_HOME`) at a per-test temporary directory for every test not marked `live`, the same hermeticity Task 24c gave sockets, with the `_installed_plugins` test given its own registry fixture. Phase 3b kills all five (`bf19870`) and re-measures `scaffold.py`.
- [ ] **Step 2:** Commit `mutation-baseline.txt` (now complete — the exclusion list and its gate logic RETIRE in the same commit); update `.github/workflows/quality.yml` to the mutmut path: drop `--lcov lcov.info` (the ported gate reads no LCOV), replace `--max-workers 1` with `--max-children <N>` as measured, keep the `[dev]` install (mutmut) and nothing else, since the gate sets `PYTHONPATH` to the shims itself. **Measured on the blanket run (2026-09-14, recorded here as the "8 GB cap" behaviour):** a `systemd-run` scope's `MemoryMax` bounds the sum, not one child — a runaway-allocation mutant in `selectors.py` (the same site class mutate4py timed out on at `_norm_with_map`) grew one child to 2.0 GB and, on the retry, five children to 1.6–2.2 GB each, and the kernel's memcg OOM took the whole scope down twice (02:21Z, 02:22:51Z) instead of the one mutant. The remedy that worked: a per-process address-space cap (`ulimit -v`, `RLIMIT_AS`) sized so `children × cap < MemoryMax`, under which the runaway mutant raises `MemoryError`, pytest exits 1, and the mutant counts as killed — `selectors.py` alone at `--max-children 2` with 2.5 GiB, the rest at 6 children and 4 GiB; the false-kill cost applies only to a test that genuinely needs more than the cap, and the suite runs in under 1 GB. So this step also lands the cap in the gate rather than in a shell wrapper: `scripts/mutation_gate.py` gains `--child-address-space <bytes>` (default 4 GiB), applied to every mutmut child through `resource.setrlimit(RLIMIT_AS)` in a `preexec_fn`, and `quality.yml` sets `--max-children` so that `children × cap` fits the runner's memory (a 7 GB `ubuntu-latest` runner gets one or two children at 2.5–4 GiB, not six); the results file records both OOMs, the remedy and the per-module child counts used. **Blanket run complete 2026-09-14T02:45:26Z:** 34 of 34 modules ok; 17,151 mutants — 13,546 killed, 3,563 survived, 34 with no tests, 8 timeouts flagged by name; scratch baseline 3,420 keys; wall clock 2 h 06 min including the two OOM restarts and the one-time stats phase; per-module times and counts in the results file. **And one containment mechanism, from the same run (2026-09-14T02:13Z):** a `scaffold.py` mutant with its vault argument mutated to a non-vault (`None`, `""`, `"."`) ran the vault-hook install with cwd inside the worktree's `mutants/` tree, `git rev-parse --git-path hooks/pre-commit` resolved to the repository's shared common hooks directory, and the vault pre-commit template was written to `/home/eranr/research-vault/.git/hooks/pre-commit` — the mutant was killed, the side effect escaped the temp vault and blocked every commit on `main` until removed by hand. The gate therefore isolates every mutmut child from the real repository's git state — as built in `f57e944`, which measured and rejected this paragraph's first proposal (a `core.hooksPath` override sends the hook the *unmutated* scaffold tests install into one shared directory, nine of them fail on its location, and that hook would then run on every commit a child makes): before every invocation the gate makes `mutants/` its own git repository (`git init --template=` with an empty template, so a user's `init.templateDir` cannot seed hooks into it) and exports `GIT_CEILING_DIRECTORIES=<repo root>` to the mutmut process, so discovery from anywhere under `mutants/` stops at `mutants/.git` and from anywhere else under the root fails loudly instead of reaching this repository, while mutmut's own git calls from the root and the tests' `tmp_path` repositories are unaffected. Two gaps the Phase 2 review found in that isolation close in the same task, not the deferred register, because the mechanism exists to stop a real escape: the child environment drops an inherited `GIT_DIR` or `GIT_WORK_TREE` (either bypasses the ceiling), and `mutants/.git` is re-initialised or its common dir verified rather than trusted when it already exists; a `setrlimit` failure in the preexec surfaces as the gate's own ABORT line, not a traceback. The product-side guard — `scaffold._copy_if_absent` refusing a hook path outside the vault's own `.git`, and `_prepare_repository` refusing a destination that is a worktree of another repository — is filed in the one `ready-for-agent` issue, not fixed here. The spec §10 re-baseline-checkpoint task for "closing the six" is satisfied — note it in the commit body. Task 22's Step 4 stale-manifest allowance dissolves here too (mutate4py sidecars retire wholesale).
- [ ] **Step 2b:** Write `docs/superpowers/specs/2026-09-13-plan-w-quality-tail-results.md` beside the plan's consumers (the assembly spec §7.1.1 names it), in the shape of `docs/superpowers/specs/2026-09-06-import-redesign-part-a-results.md`: date, method, spec, a binds-nothing line; what landed per task with commits; the measured numbers this plan exists to produce (per-module mutmut wall-clock, the stats-build cost, the survivor triage counts, the suite's serial and parallel times before and after Task 24c, the observed CI gate run and the `timeout-minutes` it set); what remains. Commit it with Step 3.
- [ ] **Step 3:** Retire the mutate4py pin (drop from dev extras; venv patches die with the venv; the sidecar manifests and any mutate4py-only gate branches removed). The mutate4py upstream reports stay queued for the author. Commit `feat: complete 26-module mutation baseline; mutate4py retired`. **Landed 2026-09-14 (`a1347dd`, review clean; the commit says 34-module, the count the tree has):** the 24 sidecars removed, the pin dropped, `requires-python`'s comment now names the floor's real reasons, the foundation spec's §10.5 bullet gains its dated closing sentence; `47a1642` adds the per-test `HOME`; the results file is on the branch; register rows 31–35 hold Phase 3b's minors for the final wave.

### Task 26: Finish — the whole-branch review, the results file's last section, the merge (added 2026-09-14)

This plan was written for a tail on `main` and ran on a worktree branch (setup ruling 1), so it needs the closing steps Part A's Task 20 had.

- [ ] **Step 1:** Whole-branch review over `merge-base(main, HEAD)..HEAD` with the results file and the deferred register as inputs; one fix wave for what it marks fix-before-merge; one scoped re-review. The register's five "final wave" rows ride that wave.
- [ ] **Step 2:** The results file gains the exemplar's "What moved in the spec" section (the foundation spec's §10.5 closing sentence, the assembly spec's §7.1.1 and §15 item 06, the ingest spec untouched) and its review section records the whole-branch verdict; every register row ends fixed with its commit, declined with its reason, or in one `ready-for-agent` issue whose body lists them (the scaffold hook containment and verify's thirteen-function remainder among them). Commit both on the branch.
- [ ] **Step 3:** Full verification on the branch (offline suite on a swept tree, form owners, CRAP, the read-only live legs), `git status --porcelain` empty in the main checkout, an explicit heads-up to the plan owner, then `merge --no-ff` into `main` with a message naming the baseline's size, the measured runner rate, the budget and the timeout, and the retired tool; `git push origin main` in the same motion (AGENTS.md); `git worktree remove` the plan worktree and delete its branch; comment on #42 with the merge commit and close it, since Task 25 Step 1b's observed run is what #42's close was gated on.
- [ ] **Step 4:** Completion message to the operator: what landed with commits, the measured numbers, what remains (the issue), and that Part B may dispatch from the merged `main` (assembly spec §7.1.1).
